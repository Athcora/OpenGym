# OpenGym high-risk game-state QA progress

## Run metadata

- Branch / starting HEAD: `main` / `ef829c9e305bedfba9667d1c09b11e510ef759e9`
- Phase: Run 2 concurrency hardening; standard Next Game race is live-verified, with team/reverse/facility-switch races remaining
- Scope: Next Game, Reverse Past Game, court/team/group/rejoin integrity, concurrency and facility context
- Exact current task: deploy and browser-verify expected-facility/game guards, then exercise them from isolated clients.

## Completed

- Created durable QA specification and progress memory.
- Confirmed the full test command is `node --test tests/*.test.mjs`; `npm test` is only a build plus rendered HTML test.
- Identified current source candidates for advancement: `supabase/fix-next-game-facility-scope.sql` and historical predecessor migrations.
- Prior production repair verified PHR Court 1 at 12 current players and 13 waiting players; the live advancement functions were verified to contain a facility context variable.
- Audited the deployed `reverse_past_game(uuid)` body statement by statement. It was a confirmed facility-isolation defect: the SECURITY DEFINER function selected and mutated configuration, courts, history, snapshots, and queue ranks without a facility predicate.
- Deployed `supabase/fix-reverse-game-facility-scope.sql` to production. The replacement scopes every snapshot, lookup, merge, delete, update, queue normalization, and reversal-history mutation to `current_facility_id()`.
- Live SQL verification confirms `reverse_past_game(uuid)` and `record_court_reversal(jsonb,integer)` now use the facility context and no longer contain the prior unscoped config/court locking patterns. Snapshot capture scopes through the active-facility CTE.

## Remaining scenario families

- Audit live definitions of `end_court_game`, team rotation, King of the Court, reverse, queue filling, repair, and reversal snapshot helpers statement by statement.
- Establish an isolated facility fixture and test regular/rejoin Next Game, rotation, King, and reverse three-way-merge sequences against real RPCs.
- Browser-test important workflows, including refresh persistence and console/network errors.
- Add behavioral regression coverage for every confirmed defect.
- Run complete test/build/lint/test commands; publish and push any fixes.

## Known findings / risks

- Historical SQL files are not canonical runtime evidence.
- Many tests are source-contract assertions or simplified models and cannot replace live RPC testing.
- Legacy reversal records created before this migration may contain globally captured snapshots. The new reversal function filters every snapshot item by the active facility before merging, so those records cannot introduce another facility's row.
- Confirmed Run-2 defect: `user_facility_sessions` is shared across a user's browser tabs. Existing client `ensureFacilityContext()` calls could race with another tab's `select_facility()`, and global advisory locks serialized requests but did not make a repeated Next Game request idempotent.

## Run 2 concurrency work completed

- Added `supabase/guarded-game-actions.sql` and deployed it to production.
- New public guarded RPCs require the client-visible facility UUID and current court game number for all Next Game paths. They lock the shared facility-session row, verify the expected facility, lock the intended court, and reject a stale game before invoking the canonical state transition.
- Reversal now has an expected-facility guarded entrypoint for the same multi-tab protection.
- The direct, unguarded Next/Reverse RPC grants were revoked from `authenticated`; the guarded entrypoints are the only browser-callable variants.
- Updated all standard, team rotation, King, and Past Game UI call paths to use these guarded RPCs.
- Added `tests/guarded-game-actions.test.mjs`; full static regression suite currently passes 32/32.
- Created two disposable active fixtures: `qa-concurrency-20260917-a` and `qa-concurrency-20260917-b`. They are separate from PHR and Ocean Air.
- Browser reproduction on fixture A with two independent administrator sessions: both opened the Game 1 confirmation, then confirmed simultaneously. The queue advanced from Game 1 to Game 3. Root cause: the modal passed `advanceGame` by reference, so the second confirmation read a realtime-refreshed Game 2 rather than the Game 1 it displayed.
- Fixed the modal-time race by capturing the displayed court game in the confirmation action. Team rotation and King confirmation paths now carry their displayed game explicitly as well.
- `node --test tests/*.test.mjs` after the modal fix: 33/33 passed.
- First browser retest of that implementation still reproduced the issue (two Game 1 confirmations advanced the QA fixture to Game 3). The `ask()` helper's function-identity capture was not a reliable boundary for the rendered action.
- Replaced the helper-level special case with explicit value capture at every regular Next Game confirmation entrypoint: the player action, single-court admin/host action, and per-court action now store both the displayed court number and game number before opening the modal.
- Added a regression assertion that all of those entrypoints pass the captured values to `advanceGame`, and updated the pre-existing green-confirmation assertion for the wrapped action.
- Published the fix to the live app and GitHub in commit `bf438f90bf9bdf2df6aead2d95b1fcb65b6bf12e`.
- Live browser verification used disposable fixture `qa-concurrency-20260917-b`, never PHR or Ocean Air. Two isolated administrator sessions opened the same Game 3 confirmation and confirmed concurrently. The fixture advanced exactly once to Game 4; both sessions synchronized to Game 4, and the losing request visibly showed `This game has already changed. Refresh and try again.` A refresh also showed the persisted Game 4 state before the unauthenticated entry screen loaded.
- Live two-court verification used that same disposable fixture after enabling Court 2. Two isolated administrator sessions opened the Court 2 Game 5 confirmation and confirmed concurrently. Court 2 advanced exactly once to Game 6; the losing session showed the stale-game rejection, and Court 1 remained on Game 4. This confirms the expected-game precondition is per-court rather than incorrectly shared across courts.
- Confirmed and repaired a P0 Teams Mode rotation defect on the same disposable fixture. With two full active teams and no eligible waiting team, the original `end_team_rotation` path could temporarily render both active rosters empty even though the completion message said the teams would replay. The normal-mode implementation moved both teams to waiting and relied on a subsequent fill to recover them.
- Added and deployed `supabase/preserve-empty-rotation-teams.sql`. For normal Teams Mode with zero eligible waiting teams it now keeps both current teams on the current court and explicitly keeps all eligible members `current`; Teams Mode (Rejoin) retains its prior deficit/rejoin behavior.
- Added `tests/teams-empty-rotation.test.mjs`, which asserts the no-replacement preservation path and guards the separate rejoin-mode behavior.
- Live browser retest after deployment, using QA Concurrency B only: a single Court 1 advance moved Game 7 to Game 8 and retained all 12 players in Teams 1 and 2; Court 2 stayed unchanged.
- Live stale-confirmation retest after deployment: two isolated QA administrators confirmed the Court 1 Game 8 Teams Mode rotation simultaneously. The fixture advanced exactly once to Game 9; the losing session showed `This game has already changed. Refresh and try again.`; all 12 players remained in Teams 1 and 2; Court 2 remained unchanged.
- Live King of the Court stale-result verification: two isolated QA administrators chose Team 1 as winner for Court 1 Game 9 simultaneously. The fixture advanced exactly once to Game 10, Team 1's streak became 1, all 12 members remained visible, and the losing session received `This game has already changed. Refresh and try again.`
- Live Teams Mode (Rejoin) stale-result verification: two isolated QA administrators chose Team 1 as winner for Court 1 Game 10 simultaneously. The fixture advanced exactly once to Game 11; the losing session received the same stale-game rejection; precisely one rejoin workflow was produced for Team 1 (six visible 4:59 rejoin timers and six pending requests), while Team 2 remained on court.
- Resumed fixture B after the rejoin window expired and browser-verified the latest Court 1 Game 10 guarded reversal. It returned Court 1 from Game 11 to Game 10 without resurrecting Team 1's six expired players; Team 2 remained as the only active Court 1 team and Court 2 was unchanged. This is the intended timeout merge behavior, but it means the original fixture can no longer supply a valid concurrent Next-versus-Reverse race because Court 1 Next Game is disabled.
- Created a fresh disposable regular-mode fixture, `qa-interleaving-20260917`, with one full 12-player Court 1 and a dedicated QA-only administrator. It is separate from PHR and Ocean Air.
- Browser-tested the guarded Next-versus-Reverse interleaving from two independent administrator clients. First advanced Game 1 to Game 2 to produce the latest reversible Game 1. Held the Game 1 reverse confirmation in one client and the displayed Game 2 Next Game confirmation in the other, then confirmed both concurrently. The displayed client state moved once to Game 3 and Reverse was rejected with `Only the most recent game on this court can be reversed.` Both clients displayed QA1 through QA12 on Court 1 with no waiters. **This browser result is not yet authoritative proof:** while trying to query it afterward, the Supabase editor re-ran the earlier destructive seed query behind its stale warning and reset this fixture to Game 1. The current fixture database state is therefore Game 1 / 12 current / 0 past games. Re-seed in a fresh SQL-editor query, then repeat and immediately query the state from a separate read-only query tab before relying on this result.
- Restarted that verification safely: signed into the reset fixture in a fresh browser client, advanced Game 1 to Game 2, and queried it from a brand-new read-only Supabase SQL tab. Authoritative state was Game 2, 12 current, 0 waiting, and 1 past game.
- **Authoritative guarded Next-versus-Reverse interleaving passed:** two independently authenticated QA administrators held the Game 1 Reverse and displayed Game 2 Next Game confirmations open, then confirmed them concurrently. Next Game won exactly once; both browser clients synchronized to Game 3, all QA1–QA12 remained current, and Reverse was rejected with `Only the most recent game on this court can be reversed.` A fresh, read-only SQL query immediately confirmed **Game 3 / 12 current / 0 waiting / 2 past games**. This used only the disposable `qa-interleaving-20260917` fixture.
- Refresh/reconnect observation: the in-app QA browser initially showed the persisted Game 3 admin state, but its reload then discarded the anonymous Supabase browser session and returned to the facility chooser. This is a limitation of this isolated in-app-browser harness, not evidence of a product state loss; the authoritative database read remains intact. The only available automation browser is that isolated in-app profile, so a same-auth-storage facility-switch interleaving cannot be honestly run here. It remains a manual production-browser validation item.
- Run 3 inventory found a P0 gap in the immediate Teams/KOC completion-modal Reverse path: the client called the deployed legacy `reverse_king_game()` RPC, whose production definition lacked both facility and observed-game guards. Replaced it with a lookup for the displayed Court/Game Past Game record followed by `reverse_past_game_guarded`; the completion modal now captures the observed court/game. Added `supabase/retire-unguarded-team-reverse.sql`, deployed its authenticated-role revokes in production, and added a regression test. Full suite passes 36/36.
- Published the guarded client as Sites version 461 from commit `e704d481d24100689a118b7330174d469924c200` and verified that the built client bundle contains `reverse_past_game_guarded` rather than the legacy immediate-reverse call.
- Live browser continuation on disposable fixture `qa-interleaving-20260917`: after signing in afresh, a Teams Mode Court 1 advance moved Game 4 to Game 5 and retained QA1–QA12. Contrary to the intended immediate-reversal flow, no `Advancement complete` / `Reverse` modal was displayed after this normal Teams advance. This is an unresolved P0 UI-flow defect; do not claim the new immediate reverse path is browser-verified yet.
- Follow-up `a37471b` changes the completion-modal condition so an operator always retains the immediate Reverse action even if their account has a rejoin prompt. Targeted regression coverage passes 6/6 and full suite passes 37/37. It is published as Sites version 462; live browser retest remains next.
- **v462 live retest failed:** a freshly authenticated QA administrator on the disposable fixture advanced Teams Court 1 from Game 5 to Game 6. QA1–QA12 remained current, but no `Advancement complete` dialog or immediate Reverse action rendered. The source proves `operator` is `admin || host`, so the new condition should have applied. Treat this as unresolved; the next investigation must establish whether the custom-domain client is serving the v462 asset or whether the post-advance state update is clearing the notice.
- Root cause proved: `rotateTeamCourt` broadcasts `team_game_advanced`; the initiating admin's own asynchronous broadcast handler could subsequently replace `Advancement complete` with `Next game advanced`. Added `app/teamAdvanceNotice.mjs`; the handler now ignores self-originated team-advance broadcasts while preserving remote operator/affected-player notifications. `tests/team-advance-notice.test.mjs` is an executable regression test for that behavior.
- Published commit `2e6b420a13d7d6eb9fdab5d012d3d36a3cf28ca0` as Sites version 463. Targeted test passed 2/2; full suite passed 39/39; direct Vinext build completed successfully.
- Live browser verification on disposable `qa-interleaving-20260917` passed for Teams rotation: a fresh QA admin advanced Game 6 to Game 7, saw `Advancement complete` with immediate Reverse, then clicked Reverse and restored Game 6 with QA1–QA12 still intact. A stale-action check also passed: kept that Game 7 Reverse open, advanced once more from a second QA admin tab, then invoked the old Reverse; it rejected with `Only the most recent game on this court can be reversed.`
- **Run 3 King/Teams Rejoin immediate-Reverse checkpoint completed.** The first Sites v464 archive was built from stale client output, despite its source commit containing the King completion-modal condition; inspecting the live module proved it still used the old `!currentUserNeedsRejoin` guard. Rebuilt from the current source and published the actual client change as Sites v465 from `0d91887`. Live King browser verification on disposable `qa-interleaving-20260917` then passed: King advancement showed `Advancement complete` and immediate Reverse; Reverse restored the immediately preceding game and roster. A second independent QA administrator validly advanced the next King game while the original completion action remained open; using the old Reverse then rejected with `Only the most recent game on this court can be reversed.`
- Verified the corrected King completion/Reverse UI on both default desktop and a 390x844 mobile viewport. The mobile dialog retained both Reverse and Continue without clipping.
- Live Teams Mode (Rejoin) verification passed on the same disposable facility in King format. Advancing a team at its consecutive-game limit produced six visible rejoin countdown placeholders, a six-pending Rejoin requests badge, and the operator completion dialog at both desktop and mobile widths. Immediate Reverse restored the prior Game 14 roster (QA1–QA12 current, no pending rejoin requests) each time.
- Authoritative database state after the final reversal was checked from a new, read-only Supabase query: Court 1 Game 14, 12 current players, 0 rejoin players, and 13 past games. This matched the browser state.
- **Legacy reverse ACL defect fixed and verified.** The production `reverse_king_game()` and `reverse_next_game()` functions originally retained EXECUTE for PUBLIC, anon, and authenticated. Updated `supabase/retire-unguarded-team-reverse.sql` to revoke all three roles, added regression assertions, applied the migration in Supabase, and re-queried authorization. Both functions now report `false` for PUBLIC, anon, and authenticated execute. The guarded `reverse_past_game_guarded` client flow remained live-tested after the ACL change.

## Files changed this run

- `docs/qa/PLAYOPENGYM_QA_SPEC.md`
- `docs/qa/PLAYOPENGYM_QA_PROGRESS.md`
- `supabase/fix-reverse-game-facility-scope.sql`
- `tests/reverse-facility-scope.test.mjs`
- `supabase/guarded-game-actions.sql`
- `tests/guarded-game-actions.test.mjs`
- `app/teamAdvanceNotice.mjs`
- `tests/team-advance-notice.test.mjs`
- `app/WaitlistApp.tsx`
- `tests/court-reversal.test.mjs`
- `supabase/preserve-empty-rotation-teams.sql`
- `supabase/retire-unguarded-team-reverse.sql`
- `tests/teams-empty-rotation.test.mjs`
- `tests/guarded-game-actions.test.mjs`

## Tests and browser verification

- `node --test tests/reverse-facility-scope.test.mjs`: 3/3 passed.
- `node --test tests/teams-empty-rotation.test.mjs`: 2/2 passed.
- `node --test tests/*.test.mjs`: 35/35 passed after the Teams Mode preservation repair.
- `node --test tests/*.test.mjs`: 35/35 passed again after the authoritative guarded Next-versus-Reverse browser/database verification (no source changes were required by that verification).
- `node --test tests/*.test.mjs`: 36/36 passed after retiring the unguarded immediate Teams/KOC reverse RPC path.
- `pnpm exec eslint . --ignore-pattern dist --ignore-pattern .next`: started with no emitted diagnostics but did not return a conclusive completion status before the prior session ended; it is not a confirmed lint pass.
- Production SQL editor: migration completed successfully with no rows returned; subsequent function-definition audit confirmed the scoped replacement is deployed.
- Public browser: reloaded the live entry flow and checked browser warnings/errors; the page rendered normally and the captured console had no warnings or errors.
- The Windows shell does not expose `npm`; `pnpm run build` also exposes a Windows-incompatible POSIX environment assignment in `package.json` (`WRANGLER_LOG_PATH=...`). A direct Vinext invocation started its build analysis but the wrapped process returned after the first transform line without a conclusive completion code. This environmental build-wrapper issue is recorded rather than reported as a product build pass.
- `node --test tests/team-advance-notice.test.mjs`: 3/3 passed after the actual King completion path was rebuilt.
- `node --test tests/*.test.mjs`: 40/40 passed after the live-build correction and again after the legacy reverse ACL hardening.
- A Windows `cmd` Vinext build completed client compilation and produced the v465 client asset used in the successful live verification.

## Run 3 action inventory checkpoint

The Teams immediate-Reverse, King immediate-Reverse/stale-action, Teams Mode (Rejoin) prompts/reversal, responsive modal, authoritative-state, and legacy-RPC ACL checkpoint is complete. Do not repeat it unless a later change touches its code or SQL.

### Run 3 completed automatically

All of the following were browser-tested against disposable `qa-interleaving-20260917`; PHR and Ocean Air were not changed.

- Player join, same-profile/device duplicate protection, Sit Out, and Leave were completed earlier in this Run 3 inventory and remain valid.
- Admin add player, Undo, Redo, admin Sit Out, Unsit, Restrict, Unrestrict, and Remove were exercised live. The UI displayed each expected completion state. The add-player undo/redo sequence restored and removed the same player as expected.
- Admin grouping was exercised live: QA7 and QA8 were selected, confirmed into a group, then QA7 was removed from that group. The history view recorded each mutation.
- Admin Swap was exercised live across a current and waiting player: Move Q. moved into current position 1 and QA7 moved to waiting position 13. This verifies an operator-driven current/waiting movement path without relying on a second player identity.
- All four modes were switched live in sequence with the 12-player fixture intact: Regular -> Rejoin -> Teams -> Teams Mode (Rejoin) -> Regular. Teams conversion rendered Team 1 and Team 2 with the same 12 active players. A 1 -> 2 -> 1 court-count change rendered empty Court 2 teams, kept Court 1's active roster intact, then removed Court 2 cleanly.
- Desktop and 390x844 mobile UI were inspected for the admin action surface. On mobile the controls were visible, organized, and usable; the live history panel also opened and rendered its event list. The QA page had no captured console warnings or errors after these operations.
- The unconfigured geofence UI was exercised by selecting the QA facility's `N/A` option. It confirmed that location was disabled and players could join without an on-site check. A database schema audit found latitude/longitude fields only on `facilities` and `waitlist_config`, not player, event, request, or session rows. Therefore device coordinates are not persisted by the schema.
- Deployed host authorization was checked both through the live RPC and authoritative function inspection. A walk-in (no `user_id`) host appointment was rejected live with `Only a player using their own device can be appointed as a host.` The deployed `admin_set_session_host(uuid,boolean)` grants authenticated execution, is facility-scoped, requires an active host or admin caller, contains the two-host limit for host callers, and requires a user-backed target. This confirms the server guard available without a second identity; it does not substitute for the second-device appointment/demotion flows below.
- Authoritative database query after mode/court changes reported `regular`, one court, 12 current players, one waiting player, zero restricted players, and zero grouped players at the time of query. (The later swap intentionally changed which fixture player occupied current/waiting.)
- Full regression suite: `node --test tests/*.test.mjs` passed 40/40. `cmd /c "node_modules\\.bin\\eslint.cmd . --ignore-pattern dist --ignore-pattern .next"` completed with no diagnostics.

Configured-geofence inside/outside/permission-denied checks cannot be simulated truthfully in this IAB: it exposes no isolated permission/location override and accepting the browser's real location prompt would test the automation host rather than a QA coordinate. The deployed client/server paths and no-location persistence were audited above. Keep this as a release smoke check on a real mobile device, not as a reason to reopen the completed non-identity inventory.

### Run 3 requires second-device validation

This automation environment has one shared browser/auth/device profile across all tabs. It cannot keep Player A, Player B, and an operator simultaneously authenticated as isolated devices. Only these scenarios remain:

1. Player-to-player Group request: Player A sends a request to Player B; B accepts, then repeat with decline. Verify grouped players become adjacent as specified, decline changes neither queue position nor group, and the database has the corresponding request status and facility-scoped group IDs.
2. Player-to-player Swap request: A sends to B; B accepts, then repeat with decline. Verify exactly one request/modal per recipient, accepted players exchange the intended positions, declined players do not move, and authoritative player positions match the UI.
3. Rejoin flows in both Rejoin and Teams Mode (Rejoin): operator advances, Player A receives the rejoin prompt, then test accept, explicit leave, timeout, and operator Accept All. Verify the player-facing timeout explanation, rejoin state/prompt rows, and final database status/position.
4. Host rules: admin appoints two authenticated active players as hosts; Host A appoints Host B; Host A attempts a third (must reject); then demote/leave a host and verify `is_host` clears. Keep the demoted host's browser open and attempt an operator action; it must reject after refresh/reconnect. Verify event and player rows server-side.
5. Team substitutes and fill-ins: a team member invites a player outside the team, that player accepts, then verify substitute visibility, cancel/remove, and the one-game fill-in/return path. These requests require an independent recipient identity; the operator Swap path itself was already covered automatically.
6. Genuine device duplicate protection: Device/Profile A joins. Device/Profile B attempts to join while using the same persisted device identifier only if the product test setup can deliberately reproduce that identifier; otherwise verify normal separate devices can each join once. Confirm no duplicate active row exists for a shared device identifier and that a rejoin after leave is allowed.
7. Real-device geofence: on a configured disposable facility, test permission denied/unavailable, a known inside coordinate, outside-radius removal, and return-inside restoration. Confirm no player location columns/records are written; only facility configuration remains.

### Multi-device Group and Swap continuation (2026-09-19)

- Created and used disposable facility `qa-multidevice-20260918` only; PHR and Ocean Air were not changed. Device A was the isolated Codex browser as `Codex A.` and Device B was the physical iPad as `Ipad`.
- **Group passed live and authoritatively.** Device A requested grouping; Device B accepted. Both stayed current at positions 1 and 2 with the same facility-scoped group ID and the latest `group_requests` row `accepted`. A refresh restored the group. Device A then left the group and retained position 1. A second request was declined by Device B; both group IDs remained null, positions stayed 1/2, and the latest request was `declined`.
- **Swap passed live and authoritatively.** Device B accepted a Device A swap request: positions exchanged to Ipad 1 / Codex A. 2 and the latest facility-scoped `substitute_requests` row was `accepted`; Device A refresh preserved that state. Device B then declined a second request: positions remained Ipad 1 / Codex A. 2 and the latest row was `declined`. A final acceptance swapped positions back to Codex A. 1 / Ipad 2 with `accepted` state in the database.
- This uncovered a production UI defect: accepted player swaps were labelled `Group update` because all generic player notifications defaulted to that title. Commit `e9d49663a9b0a8718655cc0c55a896c1674f56aa` adds a swap-notification classifier, localized `Swap update` strings, and `tests/swap-notification-title.test.mjs`. The targeted test passed; direct Vinext build completed; the change was pushed to GitHub and Sites version 466 deployed. Browser verification after refresh showed `Swap update` with the accepted-swap message at default desktop and 390x844 mobile viewport.

For every second-device procedure, use a disposable facility, capture desktop/mobile browser outcomes, refresh both clients, and query the authoritative facility-scoped tables before declaring it passed.

## Rejoin facility-scope investigation (2026-09-19)

- Investigated the reported QA fixture inconsistency before asking Device B to repeat any action. The first authoritative divergence was already present in `qa-multidevice-20260918`: `Ipad` was `current` at position 1 while its Game 2 `rejoin_responses` record was `leave`; both rows changed at the same timestamp. There was no later player-state event that could have restored the player, so the divergence occurred in or immediately around the Rejoin response mutation, not in a later realtime refresh.
- The previously deployed `answer_rejoin_prompt`, `cleanup_king_rejoin_expirations`, `leave_waitlist`, `admin_list_offline_rejoins`, and `admin_answer_offline_rejoin` functions contained legacy global lookups. They could select a user, player, queue candidate, or offline request from another facility. `admin_accept_all_offline_rejoins` itself already scoped its selection to the active facility, so it was not the originating path for a signed-in player response; it delegated to the formerly unscoped offline helper.
- Commit `d63ac20` scoped player Rejoin responses, expiry cleanup, and client response/own-player lookups. Commit `2ef9afa` scoped `leave_waitlist` and all offline Rejoin list/answer helpers. Commit `4de93f7` additionally scoped the client timeout re-read to the selected facility and explicitly establishes facility context before it calls `leave_waitlist`.
- Applied the current `supabase/fix-rejoin-facility-scope.sql` to production. Authoritative `pg_get_functiondef` checks returned positive matches for the facility predicates in deployed `leave_waitlist`, `admin_answer_offline_rejoin`, and `admin_list_offline_rejoins`.
- Added/extended `tests/rejoin-facility-scope.test.mjs`. Targeted Rejoin tests passed 5/5; full regression suite passed 44/44; Vinext production build passed. Both source commits were pushed to GitHub under Timothy's identity. The client safeguard was published as Sites version 468 from `4de93f73f55e48cb9d17a344c0775b42f12ca05f`.
- Live browser/authoritative QA results on the disposable fixture:
  - Rejoin Q. chose actual **Rejoin**: browser returned to the queue and the database recorded `current` plus `stay`.
  - Rejoin Q. chose actual **Leave**: the browser signed out to the facility chooser and the database recorded `left` plus `leave`.
  - Timeout Q. received an expiring Rejoin response. The browser showed the requested `Rejoin time expired` notification with the neutral OK action; the database recorded `left` plus `leave`; browser console had no warnings/errors.
  - The historical Ipad fixture row was repaired to `current` plus `stay`, matching the user's reported Rejoin action and preserving the active Device B session. A final facility-scoped query contains no `current`/`leave` Rejoin contradiction.
- The exact historical client payload that created the first bad Ipad record cannot be reconstructed from the old data alone. The defect class is nevertheless fixed at every authoritative Rejoin mutation and timeout/leave lookup that was still facility-global, and every live response branch available from Device A now produces consistent player/response state.
- **Teams Mode (Rejoin) Device B acceptance passed.** The existing iPad player pressed the genuine Rejoin action for Game 3. The authoritative row is `Ipad / current / team QA Rejoin Team / Court 1`, while the facility-scoped Game 3 response is `stay` with a non-null answer timestamp. The team's rejoin deadline cleared. This preserves the iPad session and proves the live Teams Rejoin acceptance branch.
- **Found and fixed a timeout-record invariant gap.** A live Teams Mode (Rejoin) timeout initially removed `Teams T.` and showed the correct neutral timeout notice, but the pending Game 5 `rejoin_responses` row stayed unanswered. Root cause: the client timeout path calls `leave_waitlist`, and that RPC did not resolve the pending facility-scoped Rejoin record. `leave_waitlist` now marks any pending response for that user in the selected facility as `leave` in the same transaction as the removal. `tests/rejoin-facility-scope.test.mjs` permanently asserts this exact invariant.
- Applied the replacement `leave_waitlist` definition to production. A fresh live browser timeout using a separate `Teams F.` QA player showed the timeout notice and signed the player out. The authoritative Game 6 result is `Teams F. / left / queue_position null / response choice leave / answered_at non-null`, with no pending response. This covers Teams Mode (Rejoin) timeout UI, removal, response resolution, and refresh/reconnect. Targeted regressions plus the server-render test passed 7/7 after a clean Vinext build; ESLint completed with no diagnostics.

## Host Permissions touch fix (2026-09-19)

- Device B reported that tapping the yellow **Permissions** action did nothing. The team-row long-press drag handler could receive the same pointer-down event as an action button touch.
- Commit `76352486378b5b3609da49d62acb8317569fc6df` stops pointer-down propagation from the team-row action area before the drag handler sees it. The focused host-permission and rendered-page tests passed 4/4, and the commit was pushed to GitHub under Timothy's identity.
- Published the change as Sites version **469**. Live desktop and 390x844 mobile verification used only disposable `qa-multidevice-20260918`: a host tapped **Permissions** for a non-host teammate and the expected `Permissions for Ipad` dialog with **Appoint as Host** and **Cancel** opened on both viewports.
- Restored the fixture after testing. Authoritative state is `Ipad / is_host=true / current` and `Host B. / is_host=true / current`.

## Host promotion tutorial follow-up (2026-09-19)

- Commit `6a5c17833ccedf551b44be09cd0e40af718d4e04` changes both realtime host-promotion paths to launch the host tutorial immediately at step 1 instead of waiting behind the old appointment notice. Targeted and rendered checks passed 4/4, and Sites version 471 was published from a clean production build.
- Commit `c3c26ddbaca2c3c57eb6d3a83e5834c72e65563e` completes the host-side interaction: a Session Host now opens the requested `Appoint [player] as host?` confirmation directly from the yellow Permissions control, rather than first opening the administrator-only permissions panel. Targeted host tests passed 2/2 and the production build passed. Sites version 472 was published.
- Live verification on disposable `qa-multidevice-20260918`: Host B opened the direct `Appoint Ipad as host?` confirmation on desktop and again at 390x844 mobile, with no page jump. A persisted `HOST_APPOINTED` notification plus host-state change opened the Host Tour at step 1 after refresh/reconnect. The fixture was restored with the current Ipad record as host and Host B as non-host; no production facility was changed.

## Host appointment and stale-session authorization (2026-09-20)

- Completed the remaining real-device host flow on disposable `qa-multidevice-20260918`, preserving PHR and Ocean Air.
- The active physical iPad player appointed the active, user-backed `Host B.` player. The live admin board and facility-scoped database query then showed both Ipad and Host B. as `current` hosts.
- With two hosts active, the iPad host attempted to appoint the third active player, Phone P. The server rejected the action with `A session can have no more than two hosts.` This confirms the host-side two-host ceiling with a real independent Device B session.
- An authorized desktop administrator removed Ipad as host while the iPad page remained open. The iPad received the live update and immediately lost its Permissions controls without refresh. After refresh/reconnect it stayed non-host and showed the regular tutorial. The authoritative active Ipad row was `current / is_host=false`.
- The administrator then appointed Ipad again. The database showed Ipad and Host B. as the two active hosts. Ipad used the genuine Leave action; the authoritative row became `left / is_host=false`, proving host state is cleared atomically on leave.
- Browser confirmation covered the live admin board and iPad behavior; the appointment surface itself was already verified at desktop and 390x844 mobile in the preceding checkpoint. No application source changes were needed for this scenario.

## Team substitute invitation delivery investigation (2026-09-20, in progress)

- On disposable `qa-multidevice-20260918`, Device A (Host B.) used the visible **+ Add Substitutes** \u2192 **Invite a Sub** \u2192 player selection \u2192 **Send invite** flow to target the active physical iPad player (`Ipad`) outside Team 2. The first request was found to be nine minutes old (beyond the intentionally enforced five-minute invite lifetime) because it predated the recipient's selected-facility session; it was correctly expired on the second send.
- A second genuinely pending request was created for the correct active user-backed Ipad row and verified in `team_substitute_requests`. Device B still did not receive the actionable **Substitute invitation** popup.
- Root cause identified in the client event handler: the `team_substitute_invite` `waitlist_events` event could be consumed as a generic notice without forcing the pending-request reload that drives the Accept/Decline modal. Commit `8ab4187c41dd3db4d46b097d6c22f7003613d74c` handles this event specially, schedules a request refresh, and returns before generic-notice handling. `tests/team-substitute-invitation.test.mjs` provides permanent regression coverage. `tests/waitlist-integrity.test.mjs` was also brought into line with the already-deployed host-removal behavior instead of asserting an obsolete state sequence.
- Targeted test, ESLint, direct Vinext production build, and the full `node --test tests/*.test.mjs` suite pass **45/45**. The source commit is pushed to GitHub under Timothy's identity.
- The corrected client still needs to be published as a new Sites version, then live-retested with Device B. Do not ask Device B to perform any action until the deployed bundle is confirmed to contain the special `team_substitute_invite` refresh handling.
- Retried live deployment verification after the public site became reachable. `https://playopengym.com/g/qa-multidevice-20260918` is responding, but its server HTML currently preloads `WaitlistApp-DPKwNBht.js`; downloading that deployed module found no `team_substitute_invite` string. The locally built module is `dist/client/assets/WaitlistApp-1ZcNgmBo.js` and does contain the corrected handler. Therefore the public site is still on the pre-`8ab4187` client; no fresh iPad invitation was sent against that known-old bundle. The Sites publish control is not exposed in this session (`ALL_TOOLS` has no Sites tool), so publication remains the sole blocker before meaningful live Device B verification.
- Per the requested browser retry, opened `https://chatgpt.com/codex/sites` in the supplied in-app browser. It redirected to the ChatGPT home page, where the account is signed out and the only available UI is the `Log in or sign up` dialog (Google, Apple, phone, or email). No signed-in Sites/OpenGym publishing control is present in the supplied browser session. No credentials were entered and no fixture or iPad state was changed.
- After the user signed in, retried both `https://chatgpt.com/codex/sites` and `https://chatgpt.com/sites`. They both redirect to ChatGPT Library. The account profile visible in the supplied browser is `Timothy Tran — Free`; the Codex landing page only offers plan upgrades and does not expose a Site workspace or a publish control. The public OpenGym bundle remains the old `WaitlistApp-DPKwNBht.js`, so the deployment has not occurred.

## NEXT SESSION — START HERE

Do not repeat the completed Regular/Teams Rejoin accept, explicit Leave, timeout, facility-scope migration, Group/Swap, Next Game/Reverse, or host appointment/demotion work. The immediate blocker is access to the OpenGym Sites publishing controls: the supplied in-app browser is signed into `Timothy Tran — Free`, which has no Codex/Sites workspace, and no Sites MCP tool is available. Use the ChatGPT account/workspace that owns the existing OpenGym Site, or restore the Sites deployment tool. Once that control is available, publish pushed commit `8ab4187c41dd3db4d46b097d6c22f7003613d74c` as a new Sites version, then fetch the public `WaitlistApp-*.js` module and confirm it contains `team_substitute_invite` before using Device A to send one fresh invitation to the existing active `Ipad` player on disposable `qa-multidevice-20260918`. Ask Device B only to observe and accept it. Verify the actionable popup, response, substitute visibility, and facility-scoped request/player/substitute rows; then test rejection, removal/cancellation, one-game fill-in/return, and restore the QA fixture. After substitutes are complete, continue genuine duplicate-device and real-device geofence validation; preserve PHR and Ocean Air.

## Substitute acceptance and fill-in continuation (2026-09-21)

- The current deployed client was later verified as Sites version **477** and byte-matched the local bundle containing the pending substitute-invitation refresh fix. On `qa-multidevice-20260918`, the physical iPad received genuine Team 2 invitations. The fresh **accept** path displayed `Done — You are now a sub for Team 2`, created the facility-scoped `team_substitutes` row, refreshed correctly, and did not resurrect after reconnect. The genuine **decline** path left the player in its original team, recorded `declined`, left no substitute row, and did not resurrect after refresh. The host removed the accepted sub through the actual UI and the player returned to waiting with no substitute row.
- One-game fill-in live QA then found a test-fixture-only incoherence introduced by earlier direct SQL setup: Ipad was displayed on waiting Team 3 while its `waitlist_players.status` was `current`. That correctly triggered the product's intentional active-game restriction. The fixture was repaired to the coherent state `Ipad / waiting / Team 3 / court null`; no normal RPC was implicated.
- With that coherent state, the physical iPad used the genuine **Fill in** flow for sitting `Teams F.`. The authoritative assignment is `e12ce3be-b08a-4028-b735-ac644f33c20b`, `sitter=Teams F.`, `filler=Ipad`, Court 1/Game 1. A refreshed desktop host board visibly showed `Ipad (1 game replacement)` in Team 1 and `Ipad (filling in Team 1)` in original waiting Team 3. This proves a waitlisted player can fill in; the active-game rejection remains intentionally limited to actual current players.
- During that audit, deployed `SECURITY DEFINER` fill-in functions were discovered to have legacy global lookups and release predicates. Commit `4ebb0b5cfa0f80c35258653633f08f7d867da153` adds `supabase/fix-team-fill-ins-facility-scope.sql` plus `tests/team-fill-in-facility-scope.test.mjs`. The migration explicitly scopes caller player lookups, teams, assignments, events, court/config lookup, cancel/unsit cleanup, and post-game release to `current_facility_id()`/`new.facility_id`. It was applied to production successfully through Supabase SQL Editor, committed under Timothy's identity, and pushed to GitHub.
- Regression status after that migration: focused facility-scope + multicourt tests passed 4/4; full suite passed **49/49**; ESLint and direct Vinext production build completed with no diagnostics. This was server-only, so no Sites client publish is necessary.

### One-game fill-in return-path completion (2026-09-21)

- Device B tapped the genuine **Unfill** action once. The original assignment `e12ce3be-b08a-4028-b735-ac644f33c20b` then returned zero authoritative `team_fill_ins` rows.
- The host board updated live: Team 1 no longer contained `Ipad (1 game replacement)` and Team 3 no longer contained `(filling in Team 1)`. The host used the standard **Unsit** control to restore `Teams F.` to Team 1.
- Authoritative state after the return path: `Teams F. / current / Court 1 / Team 1 / sitout_priority=false / sitout_from_game=null`; `Ipad / current / court null / Team 3`, which is the teams-mode waiting-team representation. No fill-in assignment remains.
- Device B refreshed and confirmed that Team 3 shows plain `Ipad` with no fill-in annotation or **Unfill** control. This completes live desktop host plus physical mobile user, realtime, refresh/reconnect, and authoritative-state verification for the genuine one-game fill-in and return path.

### Duplicate-device guard audit (2026-09-21)

- Added `tests/device-identity.test.mjs`, which keeps the persisted browser device ID, facility-scoped active-device uniqueness, advisory-lock serialization, reattachment behavior, and join payload wired together in the regression suite. Targeted tests passed 2/2; full suite passed **51/51**; ESLint and the direct Vinext production build completed with no diagnostics.
- The deployed production database was audited directly: the active-device unique index is facility-scoped, and deployed `join_waitlist_for_device` contains both the per-device advisory lock and the facility-scoped duplicate lookup.
- A truly physical same-device collision needs a second browser identity with the *same* `opengym-device-id` local-storage value. The iPad’s normal browser UI exposes no safe way to set that internal value, and the available browser automation only has read-only in-page evaluation. The underlying live server invariant and reattachment path are verified; the physical clone-of-device-ID scenario remains an optional manual Web Inspector test rather than a release blocker.

### Teams geofence return placement fix (2026-09-21)

- Live real-device geofence QA found a Teams-mode return bug: after the one-minute saved-position grace period elapsed, `return_after_geofence` put `Ipad` back in its old waiting Team 3 and showed “You joined in the back of the waitlist.” That conflicts with the established Teams join rule: a fresh return must fill the first available roster slot.
- Root cause: the pre-tenancy geofence return RPC restored the stale `team_id`/generic queue position directly. It did not run the first-open-slot selection used by `king_prepare_player`, and it also contained facility-global `SECURITY DEFINER` lookups.
- Commit `e172c08` adds `supabase/fix-geofence-return-teams.sql` and `tests/geofence-team-return.test.mjs`. The replacement is facility-scoped and, for both `teams` and `teams_rejoin`, selects the earliest non-full roster in the same order as normal team joins: current teams before waiting teams, then Court 1 / Team 1 / member order. It never blindly restores the old `team_id`.
- The replacement was applied through Supabase SQL Editor to the live backend. Focused checks passed 10/10; the full regression suite passed **53/53**; ESLint and a direct Vinext production build completed with no diagnostics. The source commit was pushed to GitHub under Timothy's identity. This is server-only, so no Sites publish was required.
- Real iPad outside → return verification used only `qa-multidevice-20260918`; PHR and Ocean Air were not changed. With the disposable facility deliberately configured outside the iPad radius, the iPad saw the actual too-far 10-minute return screen. After the facility was restored to a permissive state and the iPad pressed **I’m back**, it saw `Welcome back — You joined the first available team spot.`
- Authoritative state recorded `Ipad / current / Team 1 / Court 1 / queue position 2 / resolution team_first_available`. Before return, Team 1 had exactly one member and Team 2 was full; the desktop host board updated live from `Ipad` in Team 3 to Team 1 slot 2, with no unrelated player/team movement. The iPad then refreshed and confirmed Team 1 persists.
- Deployed-function audit returned `facility_scoped=true`, `teams_first_slot=true`, and no latitude/longitude columns on `waitlist_players`, `geofence_return_prompts`, or `waitlist_events`. The disposable facility is again geofence-disabled with no configured latitude/longitude.

### Real-device configured geofence join and permission-denied checks (2026-09-22)

- Created separate disposable facility `qa-utc-geofence-20260921` (`e42dccf8-6885-4a37-ba1f-63dcb7788f53`) solely for the physical-location checks. PHR, Ocean Air, and `qa-multidevice-20260918` were not changed. The fixture is Teams mode with a 500 m geofence.
- The user selected MOTW Coffee & Pastries, 7710 Balboa Ave, as a public named in-range test location. The fixture was configured to its public address coordinates. The first iPad attempt accidentally remained on `qa-multidevice-20260918`; database events proved that facility mismatch before the user was asked to retry. No product defect was inferred from that incorrect route.
- With the address-bar route explicitly set to the disposable fixture, physical iPad Device B joined successfully with location allowed. The real mobile UI completed the normal silent success path (no location success modal is designed); the user reported the expected queue notifications.
- Authoritative state confirmed `Ipad B.` as `current`, queue position 1, Court 1 / Team 1, in `qa-utc-geofence-20260921`; the facility-scoped `join` event was present. Device B refreshed and confirmed it remained on Team 1. This completes actual inside-radius mobile join plus refresh/reconnect persistence.
- Re-audited schema privacy boundaries: `waitlist_players`, `waitlist_events`, and `geofence_return_prompts` have no latitude, longitude, accuracy, or coordinates fields. The app uses location only to gate the action; it does not persist the device location.
- Device B then set Chrome location permission to Never, left the disposable fixture, reloaded it, and attempted a new join. The real mobile UI displayed `Location is blocked` and the browser-specific steps to restore access, as designed. The active Supabase UI session expired immediately before the final post-denial no-row query and now requires account MFA, so the UI result is confirmed but the final authoritative no-row assertion remains to be recorded after the account session is restored.
- After authorized database access was restored, the final facility-scoped query returned `denied_player_rows=0` and `denied_join_events=0` for `Ipad Denied`. The blocked attempt therefore created neither a player row nor a join event.
- The fixture cleanup was verified authoritatively: `qa-utc-geofence-20260921` now has `geofence_enabled=false` and `latitude`/`longitude` set to `NULL`.

## Run 4 — Security and data-isolation audit (started 2026-09-22)

- Run 3 is complete. Run 4 begins with a production-definition inventory: deployed RPCs, grants, RLS, `SECURITY DEFINER` functions, triggers, realtime exposure, and direct browser-call paths.
- The audit will use only disposable facilities for any mutation probes. PHR, Ocean Air, `qa-multidevice-20260918`, and `qa-utc-geofence-20260921` remain protected from destructive testing.
- **Confirmed critical finding:** the deployed catalog has 101 `SECURITY DEFINER` functions with PostgreSQL's default `PUBLIC` execute grant, which also grants direct access to `anon`. An unauthenticated PostgREST RPC call to internal helper `capture_waitlist_state()` returned HTTP 200 and a 432,498-byte snapshot containing 817 player records plus configuration. This is a server-side data-exposure issue; no player data is recorded in this checkpoint.
- Prepared `supabase/harden-rpc-execute-grants.sql` to revoke `PUBLIC`/`anon` execute from every public-schema function, revoke future default `PUBLIC` function execution for both function-owning roles, and grant `authenticated` only the explicit browser RPC allowlist. `tests/rpc-execute-grants.test.mjs` protects that allowlist and passed 3/3. The migration is not yet applied to production.
- The first approved production application confirmed `PUBLIC=0` and `anon=0`, and the direct unauthenticated snapshot call now fails with Postgres permission error `42501`. The app reload on the disposable facility still rendered normally. The same catalog check found 103 functions still directly executable by `authenticated` through earlier explicit grants; the migration source now revokes `authenticated` before rebuilding the strict 61-RPC browser allowlist. That final authenticated-grant tightening is prepared but not yet applied.

### Run 4 RPC grants and roster RLS follow-up (2026-09-22)

- The updated `supabase/harden-rpc-execute-grants.sql` was applied to production after approval. The live catalog now reports `PUBLIC=0`, `anon=0`, `authenticated=61`, and `security_definer_total=109`. An unauthenticated direct PostgREST call to `capture_waitlist_state()` now returns HTTP `401`, and the disposable QA facility still enters normally through the live browser.
- A policy audit found the three legacy authenticated roster-reader policies (`team_fill_ins_read`, `team_substitutes_read`, and `team_substitute_requests_read`) used `USING (true)`. The live `facility_isolation` policies on those tables are confirmed **restrictive** (`polpermissive=false`), so that restrictive predicate already prevented the broad reader policy from creating a cross-facility read bypass. This is therefore defense-in-depth hardening, not a confirmed exposure.
- Applied `supabase/harden-team-roster-read-rls.sql` to production. Each of the three authenticated reader policies now explicitly uses `facility_id = public.current_facility_id()`. Live `pg_policy` inspection confirms that exact predicate, alongside the existing restrictive facility policy and runtime access policy, on all three tables.
- Added `tests/team-roster-rls.test.mjs` to keep the explicit predicate from regressing. Targeted tests passed 2/2; the full Node regression suite passed 58/58; ESLint and the direct Vinext production build completed successfully. The live disposable browser entered guest mode without an RLS error and, after refresh, returned cleanly to the facility entry screen. No production facility state was changed.
- Follow-up aggregate-only RLS simulation exposed a grant-hardening regression before it reached a joined player: the `authenticated` role could no longer execute `current_facility_id()` while PostgreSQL evaluated the restrictive facility policy, causing a permission-denied result rather than a facility-filtered result. The first helper grant was applied successfully. A complete `pg_policy` dependency inventory then found the only other required public helper: `is_waitlist_admin()` (two deployed policy references), which was also applied successfully. The source migration now allowlists exactly these two RLS support helpers alongside the browser RPCs; neither is a direct client RPC. `tests/rpc-execute-grants.test.mjs` protects that distinction. Focused tests passed 4/4 and the full suite passed 59/59.
- **Confirmed critical finding:** after both RLS support helpers were restored, an aggregate-only simulation of a newly authenticated user with no `user_facility_sessions` row resolved `current_facility_id()` to `pacific-highlands-ranch` and could read the PHR queue aggregate (`817` players) through the restrictive facility policy. No player rows were displayed during the probe. Root cause is the legacy fallback in `current_facility_id()` that treats PHR as the selected facility for every unselected user.
- Prepared `supabase/require-facility-session.sql` and updated the baseline tenancy definition to return `NULL` until `select_facility()` records a user selection. With a NULL scope, restrictive facility policies deny queue rows rather than defaulting users to PHR. Added `tests/facility-session-required.test.mjs`; targeted tests passed 7/7 and the full suite passed **62/62**.

### Run 4 selected-facility boundary repair (2026-09-22, live verification in progress)

- Applied `is_waitlist_admin()` as the second and final authenticated RLS support helper after approval. `current_facility_id()` and `is_waitlist_admin()` are both required by the deployed RLS expressions; neither is a direct browser RPC.
- Applied `supabase/require-facility-session.sql` to production after approval. The authoritative aggregate-only no-session simulation now reports `no_selected_facility=true` and `visible_player_count=0`, replacing the pre-fix PHR result of 817 visible rows.
- A real disposable `qa-multidevice-20260918` browser flow successfully selected the facility, entered guest mode, joined as temporary `Rls A.`, and loaded the live Teams queue as Team 1 with no RLS error. This verifies the selected-facility client path reaches protected roster reads.
- The temporary QA player is at the final visible **Leave** confirmation and has not yet been removed, because that final cloud-data removal requires an immediate confirmation. After removal, verify the player is `left`, refresh/reconnect the browser, then re-check the grant catalog and unauthenticated helper probe before continuing the rest of Run 4.

### Run 4 selected-facility verification and access-surface inventory (2026-09-22)

- With immediate approval, the temporary `Rls A.` player was removed only from disposable `qa-multidevice-20260918`. The authoritative facility-scoped check returned `active_rls_a_rows=0` and `left_rls_a_rows=1`. A browser refresh then returned cleanly to that facility's guest/admin entry page; it did not retain a stale queue view.
- Rechecked the deployed boundary repair: a no-session authenticated simulation still returns `current_facility_id() IS NULL` and zero visible queue rows. The live function definition contains the `user_facility_sessions` lookup and no Pacific Highlands Ranch fallback.
- Live catalog verification now reports `PUBLIC=0`, `anon=0`, and `authenticated=63` executable public functions: the 61 browser RPCs plus only the two RLS helpers (`current_facility_id` and `is_waitlist_admin`). A direct unauthenticated PostgREST probe of `capture_waitlist_state()` still returns HTTP `401`.
- Direct anonymous PostgREST probes of the sensitive tables (`admin_credentials`, `facility_admin_credentials`, `admin_sessions`, `admin_undo`, `court_game_reversals`, and `waitlist_players`) each returned an empty two-byte JSON array. `facilities` alone returned the expected active-facility listing. This exercised live RLS rather than relying only on catalog inspection.
- The live public-schema inventory has 26 data tables, all with RLS enabled. `admin_credentials` and `facility_admin_credentials` intentionally have no permissive reader policy, so PostgreSQL default-deny RLS protects them despite inherited table privileges. The remaining sensitive tables have RLS policies.
- API roles `anon`, `authenticated`, and `opengym_runtime` are all non-superuser, non-login, and do not bypass RLS. `service_role` retains its intentional managed-server bypass and is not a browser role.
- `supabase_realtime` currently publishes 13 facility-scoped operational tables. It does not publish credential, admin-session, undo/redo, or court-reversal tables. Every published table has RLS enabled and exactly one restrictive policy referencing `current_facility_id()`.

### Run 4 trigger inventory (2026-09-22, in progress)

- Started the remaining read-only privileged-function audit against the live production catalog. The deployed public schema has ten non-internal triggers: `notify_group_request`, `capture_past_game_team_rosters`, `release_team_fill_ins_after_game`, `restore_advanced_team_memberships`, `cleanup_team_fill_ins_on_player_change`, `cleanup_team_substitute_membership`, `clear_king_team_rejoin_timer`, `clear_session_host_when_leaving`, `log_player_action_history`, and `log_waitlist_player_change`.
- Nine of the ten trigger functions are `SECURITY DEFINER`; only `clear_session_host_when_leaving` is invoker-security. The catalog list exactly matches the expected operational trigger surface, but the owner/search-path configuration and browser-reachable call-chain review are still unfinished. No production function, grant, policy, or application code was changed during this read-only step.

### Run 4 privileged-function and trigger configuration audit (2026-09-23, in progress)

- Completed the live trigger configuration review. The ten non-internal triggers are the expected operational triggers; nine call `SECURITY DEFINER` functions and one is invoker-security. Every trigger function has an explicit `search_path=public`, and browser roles cannot create objects in `public`, so there is no trigger search-path shadowing path.
- Completed the aggregate `SECURITY DEFINER` catalog and dynamic-SQL review: all **109** public-schema `SECURITY DEFINER` functions have an explicit search path. `opengym_runtime` owns 95 and `postgres` owns 14. The only dynamic-SQL function is the non-browser-executable legacy `reverse_past_game(uuid)`; its dynamic relation identifiers are static saved-state kinds quoted with `%I`, and values use bound `USING` parameters. Its browser-facing guarded wrapper first asserts the selected facility. No dynamic-SQL injection path was found.
- Verified the deployed authenticated execute allowlist exactly matches the repository's 61 browser RPCs plus the two narrowly required RLS helpers. There are no unexpected authenticated-executable public functions. The six authenticated `USING (true)` read policies are paired with restrictive facility-isolation predicates, so they do not cross the selected-facility boundary.
- **Confirmed authorization vulnerability, not yet applied:** `is_waitlist_admin()` treated any facility-admin session for the same `user_id` as valid, and `is_waitlist_host()` treated an active host row in any facility as valid. Both are `SECURITY DEFINER` and feed `is_waitlist_operator()`, which guards privileged RPCs. A user could sign in/admin-or-host at Facility A, select Facility B with the normal authenticated `select_facility` RPC, then satisfy the unscoped helper while Facility B-scoped privileged functions operate on B. The intended global, server-managed `app_metadata.role='admin'` capability is retained and is not client-writable.
- Prepared and pushed commit `8b41b38` with `supabase/fix-facility-scoped-role-helpers.sql` and `tests/facility-scoped-role-helpers.test.mjs`. The fix scopes ordinary `admin_sessions` and host-row checks to `current_facility_id()` while retaining the intended global app-metadata super-admin role. Focused regression tests passed 9/9; ESLint passed; the direct Vinext production build passed. It has **not** been applied to Supabase yet because project rules require immediate approval before a cloud function-definition change.
- After approval, applied the role-helper migration to Supabase. Live catalog inspection confirms both `is_waitlist_admin()` and `is_waitlist_host()` now contain the selected-facility predicate and retain `search_path=public`; `is_waitlist_operator()` delegates only to those two helpers. A fresh live browser load of disposable `qa-multidevice-20260918` completed facility resolution and rendered its guest/admin entry controls. The fixture currently has no active host test identity, so the final two-facility live role-switch mutation probe remains to be run using a disposable host fixture; PHR and Ocean Air were not touched.
- **Second confirmed critical finding, pending approval:** an authenticated, selected-facility administrator can call `admin_reset_waitlist()`. Its private `clear_waitlist_and_history()` helper is not browser-executable but is `SECURITY DEFINER` and its deployed definition uses global `WHERE true` mutations for players, games, queues, events, and other session data. Because the helper executes with runtime privileges, an admin reset at Facility A can clear data in every facility. The issue is reachable through the normal browser reset RPC, not merely through direct SQL.
- Prepared `supabase/fix-admin-reset-facility-scope.sql` and `tests/admin-reset-facility-scope.test.mjs`. The replacement gets `current_facility_id()` once, rejects a missing selection, and constrains each reset/update/delete/court rebuild to that facility (including team fill-in/substitute and geofence rows). Targeted regression checks passed 5/5; the full Node suite passed **66/66**; the direct Vinext production build passed. It is not yet applied because it is a distinct production function-definition change requiring immediate approval.

### Run 4 admin-reset facility-scope repair (2026-09-23)

- After approval, applied `supabase/fix-admin-reset-facility-scope.sql` to the live backend. Deployed-definition checks confirm `admin_reset_waitlist()` captures the selected facility, the reset helper has no global mutation predicate, and every reset path is constrained by `facility_id`.
- Exercised the real authenticated `admin_reset_waitlist()` call in one rollback-only transaction with two distinct disposable facilities. The selected facility's sentinel became `left`, while the other facility's sentinel stayed `waiting`. The transaction then rolled back; a separate query confirmed zero persisted sentinel players. This proves the corrected server execution path rather than merely source structure.
- Refreshed the deployed public application at `qa-multidevice-20260918`; it resolved the facility and rendered the guest/admin entry controls normally. No frontend publish was required for this server-only definition change. Focused role-helper/reset tests passed 4/4. A follow-up eslint invocation exceeded the shell tool's 30-second response window without emitting diagnostics, so it must be rerun to a definite exit status before the next source commit.
- Continued the deployed legacy-global-helper inventory. `set_open_gym_mode(text)` is still a browser-executable `SECURITY DEFINER` RPC and has a genuine cross-facility mutation path: it uses global `waitlist_config`, players, rejoin rows, team rows, and calls the unscoped legacy `initialize_king_mode()`. `admin_undo_last`, `admin_redo_last`, and `reverse_next_game` also call `restore_waitlist_state`; their final facility scope and caller reachability remain under audit. Do not change these functions until each path is traced and a facility-scoped replacement covers all downstream helpers.

## NEXT SESSION — START HERE

Do not repeat completed Run 4 grants, RLS, trigger/search-path, role-helper, or admin-reset testing. User approved the mode-switch repair. Source-only work now exists in `supabase/fix-mode-switch-facility-scope.sql` and `tests/mode-switch-facility-scope.test.mjs`; focused test passes 2/2. It scopes `set_open_gym_mode` and `initialize_king_mode` to `current_facility_id()` and replaces the global admin-session check with `is_waitlist_admin()`. **Do not apply it yet:** the live mode-switch path calls `save_admin_undo`, and the deployed undo/redo/capture/restore chain is still global. First finish facility-scoping `capture_waitlist_state`, `restore_waitlist_state`, `save_admin_undo`, `admin_undo_last`, `admin_redo_last`, and `reverse_next_game` as needed, with explicit facility-bound snapshot rows and regression coverage. Then run focused/full tests, definitive lint/build, apply the complete migration to Supabase under the existing approval, and prove a rollback-only two-disposable-facility mode change plus undo/redo leaves the other facility unchanged. Preserve PHR and Ocean Air.

### Run 4 mode-switch / undo dependency continuation (2026-09-23)

- Expanded the **uncommitted** `fix-mode-switch-facility-scope.sql` to also define selected-facility `capture_waitlist_state`, `king_repair_initial_team_names`, `restore_waitlist_state`, `save_admin_undo`, `admin_undo_last`, and `admin_redo_last`. The source regression test now passes 3/3; full suite passes 69/69.
- A first Supabase SQL submission was deliberately cancelled at its destructive-operation confirmation because it accidentally omitted the restore/undo/redo definitions. No cloud change was applied. Do not deploy a partial subset of this migration: it would leave the undo path globally mutable.

### Run 4 mode-switch, undo, and regular reverse isolation repair (2026-09-23)

- The complete approved `supabase/fix-mode-switch-facility-scope.sql` was applied as one atomic transaction. Live catalog inspection confirms all eight replacement helpers use `current_facility_id()`, have no `WHERE true` mutation path, and retain the intended execute boundary: only `set_open_gym_mode`, `admin_undo_last`, and `admin_redo_last` are authenticated-executable; snapshot helpers remain private.
- A follow-up call-chain review found that the still browser-executable legacy `reverse_next_game()` delegated to the now-scoped restore helper but selected its `admin_undo` record without a facility predicate. This was a real selected-facility boundary defect: a selected Facility A caller could pick a historic Facility B snapshot before restoration. `supabase/fix-reverse-next-game-facility-scope.sql` now captures the selected facility once and constrains its undo lookup, config lock, rejoin cleanup, undo/redo deletion, actor lookup, and event insertion to that facility. It preserves the existing regular/rejoin immediate-Reverse UI and still uses the facility-scoped restore helper.
- Applied that reverse replacement to Supabase after the approved scope repair. Deployed-definition verification confirms the facility predicates and confirms that the rollback-only test left no `RUN4_SCOPE_A`/`RUN4_SCOPE_B` players behind.
- Live server verification used a single rollback-only transaction across disposable `qa-multidevice-20260918` and `qa-utc-geofence-20260921`: an authenticated selected-Facility-A admin performed a real mode switch, undo, redo, then a simulated valid Next Game plus the browser-reachable `reverse_next_game()`. The reverse returned `Game 2 was reversed. Game 1 and its queue order are restored.` Facility B's saved configuration and sentinel stayed unchanged; all mutation data and temporary facility selection rolled back. A separate non-operator authenticated transaction correctly rejected `set_open_gym_mode` with `Admin access required.`
- Regression status: targeted mode-switch/guarded-action/RPC-grant tests passed 13/13; full Node suite passed 69/69; ESLint and direct Vinext production build exited successfully. The public disposable facility loaded normally, and a refresh/reconnect returned to the correct facility entry page. This was server-only, so no Sites publication was needed and no PHR/Ocean Air data changed.
- The next live executable-RPC inventory completed with `p.prokind='f'` to exclude PostgreSQL aggregates: **no authenticated-executable ordinary public function currently contains `WHERE true`**. This clears the direct global-predicate subset; it does not replace call-chain review of functions that may be unscoped through indirect helpers.
- The first direct-scope inventory correctly identified a broad set of browser RPCs that delegate tenancy to already-audited helpers rather than invoking `current_facility_id()` themselves. This is a triage list, not a finding. Start the next pass with the highest-risk mutation wrappers: `admin_set_court_count`, `admin_move_player`, `admin_move_king_player`, `admin_substitute_players`, and `save_operator_undo`; inspect each deployed definition and its immediate callee before testing any mutation.

## NEXT SESSION — START HERE

Continue Run 4 from the remaining browser-executable RPC call-chain inventory. Do not repeat the completed mode-switch, undo/redo, or regular-Reverse repair. The direct executable `WHERE true` scan is clean. The current triage query identified wrappers without a direct scope reference; this alone is not a defect because many delegate to scoped helpers. Inspect live definitions and immediate callees first for these highest-risk mutation wrappers: `admin_set_court_count`, `admin_move_player`, `admin_move_king_player`, `admin_substitute_players`, and `save_operator_undo`. For each, trace the browser route and selected-facility behavior before classifying it. Use only rollback-only two-disposable-facility probes for a demonstrated mutation path. Preserve PHR and Ocean Air; update this checkpoint with exact findings before stopping.

### Run 4 browser-executable admin mutation RPC isolation repair (2026-09-23)

- Confirmed a further browser-reachable selected-facility authorization defect in the deployed definitions of `admin_move_player`, `admin_move_king_player`, `admin_move_king_player_to_empty`, `admin_substitute_players`/`swap_waitlist_players`, and `admin_set_court_count`. The wrappers executed as `SECURITY DEFINER`, but their target player, team, queue, court, and configuration lookups were global. A selected-facility operator could therefore mutate another facility by supplying a cross-facility identifier or by changing courts.
- Added `supabase/fix-admin-rpc-facility-scope.sql` and `tests/admin-rpc-facility-scope.test.mjs`. The complete migration captures `current_facility_id()` once in every affected browser path, scopes all supporting reads/mutations, rejects out-of-facility player/team identifiers before mutation, and keeps the private `swap_waitlist_players` helper non-executable by `authenticated`.
- Commit `2e9bf5c` prepared the replacement; commit `eca58f0` wraps the full seven-definition migration in `begin`/`commit` so it cannot apply partially. Both are pushed to `github/main` as Timothy.
- Applied the complete atomic migration to the live Supabase backend. Deployed catalog inspection shows all seven repaired functions capture `current_facility_id()`, none contain a global `WHERE true`, and the intended execute boundary remains: six browser wrappers are authenticated-executable while the private swap helper is not.
- Authoritative live server verification used only rollback-only transactions on disposable `qa-multidevice-20260918` (selected Facility A) and `qa-utc-geofence-20260921` (Facility B). A real authenticated admin successfully invoked `admin_move_player`, `admin_substitute_players`, and `admin_set_court_count(2)` against Facility A; inside the transaction A changed to two courts while B remained at one court with its sentinel queue position unchanged. A cross-facility `admin_move_player` using Facility B's player id was rejected with `Player not found.` The rollback cleanup query confirmed zero residual `RUN4_RPC%` players in either fixture.
- Regression status: targeted admin-RPC test passed 3/3; full Node suite passed 72/72; `pnpm exec eslint . --ignore-pattern dist --ignore-pattern .next` and direct `pnpm exec vinext build` both exited 0. The deployed public QA facility loaded and refreshed to its expected guest/admin entry state. No frontend publication was needed because this was a backend-only change.

## NEXT SESSION — START HERE

Continue Run 4 from the **remaining browser-executable RPC call-chain inventory**, not the completed mode-switch/undo/reverse or admin-mutation repairs. Start with the next highest-risk authenticated mutation wrappers that do not directly show `current_facility_id()` in the live inventory, especially `log_waitlist_operator_action`, `notify_waitlist_operator_player`, and their browser-reachable callers. For each, inspect the deployed definition plus immediate callers before calling it a defect. Use only rollback-only two-disposable-facility probes for any demonstrated mutation path, preserve PHR and Ocean Air, and retain the new admin-RPC regression coverage. Do not start Run 5 until the Run 4 inventory and live verification are explicitly complete.

### Run 4 next browser RPC call-chain finding (2026-09-23, active)

- The next deployed authenticated inventory found `admin_add_player`, `admin_set_player_sitout`, and `admin_unsit_player` with neither a selected-facility capture nor a facility predicate. Their private notification/action-history helpers likewise lack a facility boundary. Browser routes are confirmed in `app/WaitlistApp.tsx` at the add-player and sit-out/unsit controls.
- Local historical definitions show the risk reaches shared global helpers: `admin_add_player` calls `fill_open_court_slots`; the sit-out paths call `normalize_active_waitlist`; and both read/update globally in their old forms. Do not patch only the three wrappers. First obtain the live definitions of `fill_open_court_slots`, `normalize_active_waitlist`, `log_waitlist_operator_action`, `notify_waitlist_operator_player`, and `admin_leave_player`, then make one complete selected-facility replacement covering the full reachable chain. No migration has been prepared or applied for this new finding yet.

## NEXT SESSION — START HERE

Continue the active Run 4 walk-in/add-player and admin sit-out/unsit call-chain investigation. Do not repeat completed admin-move/substitute/court-count repair (`eca58f0`, live applied). First query the **live** definitions of `admin_add_player`, `admin_set_player_sitout`, `admin_unsit_player`, `admin_leave_player`, `fill_open_court_slots`, `normalize_active_waitlist`, `log_waitlist_operator_action`, and `notify_waitlist_operator_player`; determine whether later migrations already scoped any helper. If any global reachable path remains, add one atomic facility-scoped migration and regression coverage for the whole chain, then run targeted/full tests, lint/build, apply to Supabase, and prove with rollback-only A/B QA probes plus a live browser load. Preserve PHR and Ocean Air. Do not start Run 5.

### Run 4 player-action helper-chain isolation repair (2026-09-23, active)

- Confirmed a further browser-reachable facility-isolation defect in the live definitions of `admin_add_player`, `admin_set_player_sitout`, `admin_unsit_player`, `admin_leave_player`, `sit_out_one_game`, `sit_out_and_leave_group`, `remove_self_for_geofence`, and their helpers `normalize_active_waitlist`, `log_waitlist_operator_action`, and `notify_waitlist_operator_player`. These `SECURITY DEFINER` paths selected players/configuration/queues/groups globally; most critically, invoking any sit-out path could normalize players across facilities. `fill_open_court_slots` and `return_after_geofence` were already correctly facility-scoped and were not replaced.
- Added the complete atomic replacement `supabase/fix-player-actions-facility-scope.sql` and source-contract regression coverage in `tests/player-actions-facility-scope.test.mjs`. The migration captures `current_facility_id()` once in every reachable replacement, constrains player/config/court/group/notification/event/geofence prompt reads and writes to it, and preserves the browser RPC signatures and existing grants.
- Commit `abaff2d` (`Scope player action RPCs to selected facility`) is pushed to `github/main` as Timothy. Targeted tests passed 3/3 and the full Node suite passed **75/75**. The combined lint/build invocation exceeded the shell tool response window without diagnostics; rerun lint and direct Vinext build to definite exits before marking this repair complete.
- Applied the full atomic migration to live Supabase. Deployed catalog verification confirms the seven browser-executable wrappers and three private helpers all capture the selected facility, contain no `WHERE true` mutation path, and preserve their intended execute boundary. A fresh live browser load of disposable `qa-multidevice-20260918` rendered the expected guest/admin entry page.
- **Still required:** execute the rollback-only authenticated two-facility behavioral probe. The first attempt exposed that `user_facility_sessions` uses `facility_id` (not `selected_facility_id`) and was rolled back by statement failure; no QA rows persisted. A corrected probe is prepared conceptually but the Supabase editor's large-query input was interrupted before execution. It must prove a selected-Facility-A authenticated admin can add → sit out → unsit → leave a disposable A player while a B sentinel stays `waiting`; then separately prove a selected-A cross-facility player id is rejected. Finally confirm zero `RUN4_CHAIN%` residual rows after rollback and refresh the live QA page. Preserve PHR and Ocean Air.

### Run 4 player-action live isolation verification (2026-09-23)

- Completed the rollback-only two-disposable-facility behavioral proof for the `abaff2d` player-action helper-chain repair. With `qa-multidevice-20260918` selected as Facility A and an authenticated global app-metadata admin identity, `admin_add_player` → `admin_set_player_sitout` → `admin_unsit_player` → `admin_leave_player` produced an A-scoped player with final status `left`; a simultaneously inserted Facility B (`qa-utc-geofence-20260921`) sentinel remained `waiting`. The authoritative result was `waiting | left | true` (B status, A final status, A facility identity).
- A separate rollback-only transaction supplied a Facility B player id while A was selected. `admin_set_player_sitout` rejected it with the expected `This player is no longer active.` path, and the B row remained `waiting`. Both transactions rolled back; a direct cleanup check found **zero** `RUN4_CHAIN%`/RunFour probe rows. A fresh public-browser load of `qa-multidevice-20260918` completed normally to its facility guest/admin entry screen.
- `pnpm exec vinext build` completed successfully with exit code 0. The definitive tracked-source ESLint invocation completed with exit code 1 due to 12 pre-existing project lint errors (11 React-hooks/prefer-const rules in `app/WaitlistApp.tsx` and one empty-interface rule in `environment.d.ts`); it did not report an error in any Run 4 migration or regression test. This is a baseline lint debt, not a result hidden by a shell timeout.

### Run 4 restriction and court-rule RPC isolation repair (2026-09-23)

- Live call-chain review confirmed two additional browser-reachable `SECURITY DEFINER` mutations were globally scoped: `set_team_court_rules` updated by `court_number` alone, and `admin_restrict_player` updated by player id alone (after capturing an undo snapshot). A selected-Facility-A operator/admin could mutate a same-numbered Facility-B court or restrict a B player by identifier.
- Added `supabase/fix-restrictions-and-court-rules-facility-scope.sql` and `tests/restrictions-and-court-rules-facility-scope.test.mjs`. The atomic replacement captures `current_facility_id()`, constrains both mutations to it, rejects missing selected-facility targets, and validates a restriction target before recording its undo snapshot.
- Applied the complete atomic migration to Supabase. Live definition inspection confirms both functions capture the selected facility, bind their mutation, and retain only their intended `authenticated` execution grants.
- Authoritative rollback-only A/B testing verified an authenticated selected-A admin restricted only the A sentinel and changed only A Court 1 to King; B’s player stayed unrestricted and B Court 1 retained its original mode (`true | false | king | true`). A separate selected-A call with B’s player id was rejected and B remained unrestricted. Both transactions rolled back and the cleanup query returned zero `RUN4_RULE%` rows. Focused regression tests passed 9/9; full Node suite passed **78/78**. No PHR or Ocean Air data was touched.

## NEXT SESSION — START HERE

### Run 4 player-substitute and team-substitute chain finding (2026-09-23, active)

- Continued the live browser-RPC catalog from commit `4d4b55c`. `advance_court_game`, `advance_team_rotation`, and `advance_team_king_game` are guarded by `assert_expected_court_game(facility_id, court, game)`; `leave_waitlist` and `join_waitlist_for_device` visibly capture and use `current_facility_id()`. Their remaining private callee review is not yet complete, but no new defect was classified in those paths.
- **Confirmed new selected-facility vulnerability:** the deployed browser RPCs `request_player_substitute`, `answer_player_substitute`, `request_team_substitute`, and `answer_team_substitute` have no selected-facility capture and use global player, team, request, substitute, notification, event, and cleanup lookups/mutations. A selected-Facility-A user/admin/host can supply or answer identifiers from Facility B; `request_player_substitute` can also choose the caller's active row from a different facility. This is browser reachable through the normal Substitute/Swap workflows, not a direct-SQL-only concern.
- Live source evidence is saved in the SQL-editor history: `request_player_substitute` reads requester by `user_id=auth.uid()` and target by id without facility conditions; `answer_player_substitute` looks up pending request/players and calls global `swap_waitlist_players`; `request_team_substitute` selects requester/target/team globally and globally expires requests; and `answer_team_substitute` globally reads/writes invitations, team substitutes, players, teams, and events. Repository follow-up also identified browser-executable `admin_remove_team_substitute` as globally selecting/removing an assignment and inserting an unscoped event; it belongs in the same repair.

## NEXT SESSION — START HERE

Continue the active Run 4 player/team substitute-request facility-isolation repair. Do **not** repeat completed A/B verification or the `4d4b55c` restriction/court-rule repair. First inspect the current live definitions and repository migrations for `request_player_substitute`, `answer_player_substitute`, `swap_waitlist_players`, `request_team_substitute`, `answer_team_substitute`, `admin_remove_team_substitute`, `team_substitutes`, `team_substitute_requests`, and `king_team_label`; map every immediate callee and preserve all existing substitute behavior. Then add one complete **atomic** selected-facility replacement migration covering the entire reachable player-swap and team-substitute invitation/answer/remove chain, with facility-bound player/team/request/substitute/event/notification mutations and early rejection of cross-facility ids. Add behavioral regression coverage. Run focused and full Node tests, record the known project ESLint baseline failure separately, run direct Vinext build, apply the full migration to Supabase, inspect live definitions/grants, and prove via rollback-only A/B transactions that selected-A valid substitutes work but B ids reject and B sentinels/requests/substitute rosters remain unchanged. Refresh the disposable public QA facility, clean all `RUN4_SUB%` rows, commit/push as Timothy, update this checkpoint, and continue the remaining Run 4 RPC inventory. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 substitute repair progress (2026-09-23)

- Reconstructed from checkpoint commit `d2cbf7f`; current branch is `main`. Initial tracked working tree was clean. Existing untracked `.sites-stage-*`, `artifacts/`, and build output are unrelated and were left untouched.
- Inspected deployed production definitions in Supabase (not only historical SQL). Confirmed `request_player_substitute`, `answer_player_substitute`, `request_team_substitute`, `answer_team_substitute`, and `admin_remove_team_substitute` were globally scoped. The deployed `swap_waitlist_players` was already facility-scoped. Also reviewed `king_team_label`, operator logging/notification helpers, grants, and the indirect `cleanup_team_substitute_membership` trigger.
- Added `supabase/fix-substitute-facility-scope.sql`, a transaction-wrapped replacement of the five RPCs plus facility-bound membership cleanup trigger; public signatures and authenticated execution grants are retained. It scopes requester/target/team/request/assignment/player reads, request cleanup, roster updates/deletes, event/notification inserts, old-team cleanup, and called helper effects to `current_facility_id()`.
- Added `tests/substitute-facility-scope.test.mjs` with three source-contract regression tests. Focused tests passed 3/3; full `node --test tests/*.test.mjs` passed 81/81. `pnpm exec vinext build` passed. ESLint on `app tests` reported 11 errors and 35 warnings; these are existing project lint debt (the prior all-tracked-source baseline was 12 errors including `environment.d.ts`). No lint error was reported in the new test. `git diff --check` passed.
- Applied the complete replacement through the authenticated Supabase SQL editor; it returned `Success. No rows returned`. Live catalog audit confirms the five browser RPCs remain `SECURITY DEFINER`, capture facility context, contain facility predicates, and retain authenticated EXECUTE. `cleanup_team_substitute_membership` remains private to authenticated callers, and `king_team_label`, event/notification helpers retain their existing boundaries. Facility columns are non-null and default to `current_facility_id()`.
- **Substitute checkpoint closed:** the rollback-only live A/B probe succeeded. Facility A completed the player request/accept swap, team invite/accept assignment, and operator removal; all six cross-facility player/team/request/substitute/swap attempts rejected; Facility B's sentinel player, team, pending requests, and substitute assignment matched the before-state. A separate post-rollback query returned zero residual `RUN4_SUB%` players, teams, requests, or assignments. The deployed app refreshed at `qa-multidevice-20260918` to its ordinary guest/admin entry. The result JSON was `{"A_player_swap_ok":true,"A_team_assignment_ok":true,"A_team_removal_ok":true,"cross_facility_rejections":6,"B_state_unchanged":true}`.
- Focused regression coverage passed 3/3 and the full Node suite passed **81/81**. The production Vinext build passed. Tracked-source ESLint (`app`, `tests`, and `environment.d.ts`) exited 1 with the known baseline of **12 errors and 35 warnings**; the new test file has no lint errors. `git diff --check` passed. The repair is deployed and live verified; the migration/tests/checkpoint still need a source commit and push.

## NEXT SESSION — START HERE

The player/team substitute repair (`54e487d`) and the group/King repair are already deployed and rollback-only A/B verified; do not repeat them without a regression. Next, continue the prepared waitlist/history/join/rejoin/rename/geofence isolation repair. Its exact local source is `supabase/fix-waitlist-read-join-geofence-facility-scope.sql` with tests in `tests/waitlist-read-join-geofence-facility-scope.test.mjs`; local focused tests passed 3/3, full Node suite passed 87/87, Vinext build passed, and ESLint completed with the known baseline of 12 unrelated errors and 35 warnings. The migration has **not** been applied and no live probes have been run. A Supabase SQL Editor paste truncated, and direct setup inspection found no repository Supabase config/project link, no installed `supabase` or `psql`, no Supabase CLI profile/link metadata in the conventional locations, and no PostgreSQL/Supabase connection environment variables; no credentials were displayed or requested. First establish or locate an already-authorized direct database/CLI connection, then apply that exact file as one transaction. If still unavailable, record the missing direct connection setup and stop before any partial application. After successful application, audit deployed definitions and grants; run complete rollback-only A/B tests for history isolation, valid A join/rejoin/rename/geofence behavior, A location writes, B unchanged config/players/events, and cross-facility player IDs rejected; verify authoritative state and zero residual `RUN4_WJ%` rows, refresh the disposable browser facility, then commit/push migration/tests/checkpoint. Continue with the remaining authenticated browser RPC/security call-chain inventory. Preserve PHR and Ocean Air; do not start Run 5.

### OpenGym Supabase CLI setup (2026-09-23)

- Inspected the repository and Git history: there is no prior `supabase/migrations/` directory or config, while the QA history repeatedly records production SQL applied manually through Supabase SQL Editor. Legacy SQL files under `supabase/` are therefore historical deployment artifacts with unknown CLI ledger correspondence. Automatically placing them into CLI migration history or pushing them could replay old changes. They must remain outside `supabase/migrations/` until the linked remote ledger is inspected and safely reconciled.
- Installed the official Supabase CLI as a project-local dev dependency (`supabase` 2.117.0) and ran `supabase init` without linking any project. Local `supabase/config.toml` uses repository project id `open-gym-sites`; it contains no production project ref. Added an intentionally empty `supabase/migrations/` and `docs/qa/SUPABASE_CLI_MIGRATIONS.md` with the no-replay and dry-run gate.
- The prepared Run 4 file `supabase/fix-waitlist-read-join-geofence-facility-scope.sql` is unchanged, remains outside `migrations/`, and remains unapplied. No production CLI command was run. CLI local status reports Docker/Podman unavailable; this only prevents local stack startup, not linked remote CLI use.

## NEXT SESSION — START HERE

Run 4 remains active. The official Supabase CLI setup is in place, but no owner authentication or production link is configured. The exact next action is for the project owner, from the OpenGym repository root, to run `pnpm exec supabase login` and then `pnpm exec supabase link --project-ref yxykrybhsrmxelkumxxr` in their local terminal; enter any database password only into that local CLI prompt. Do not provide secrets in chat. After the owner confirms login/link has completed, first run `pnpm exec supabase migration list --linked` and inspect `supabase/migrations/`; reconcile remote history against manually applied legacy SQL before creating/importing migration history. Then create/use only the intended Run 4 migration and run `pnpm exec supabase db push --linked --dry-run`; prove it lists exactly that migration before any production push. Do not move the currently prepared `supabase/fix-waitlist-read-join-geofence-facility-scope.sql` into migrations or alter it until this verification is complete. Do not start Run 5.

### Run 4 Supabase CLI deployment and member-directory audit (2026-09-23 PDT)

- Reconstructed the repository independently of the missing prior chat. HEAD is `347744c` on `main`, matching `github/main`. The working tree includes legitimate uncommitted QA deliverables described below plus unrelated pre-existing `.sites-stage-*`, `artifacts/`, `art`, and build cache output; those unrelated files were left unstaged.
- CLI login/link is complete in this environment. `supabase/.temp/project-ref` and `pnpm exec supabase migration list --linked` confirm the linked production ref is `yxykrybhsrmxelkumxxr`. Local and remote migration history match exactly: `20260924020558` (waitlist/history/join/rejoin/rename/geofence) and `20260924033345` (admin member directory). The documented dry run reports `Remote database is up to date`; legacy root SQL files were not replayed or added as migrations.
- The `20260924020558` migration byte-matches its prepared source (`88119649…e3cb5f5`) and was deployed atomically via CLI. Live RPC/function/grant inspection and rollback-only two-facility A/B verification were completed in the previous continuation: A join/rejoin/rename/geofence behavior worked, B was unchanged, cross-facility player IDs rejected, authoritative state matched, QA mutations rolled back with zero `RUN4_WJ%` residue, and the disposable public QA facility was refreshed.
- Continued live browser-RPC audit and confirmed a facility-account privacy defect in deployed `admin_list_members()`: facility credential admins could receive a global `auth.users` directory. Added the atomic source `supabase/fix-admin-member-directory-facility-scope.sql`, matching CLI migration `20260924033345_admin-member-directory-facility-scope.sql`, and `tests/admin-member-directory-facility-scope.test.mjs`. Live definition keeps global directory access only for explicit server-managed platform `app_metadata.role=admin`; facility admins are constrained by selected facility. The function remains `SECURITY DEFINER` owned by `postgres`, with authenticated-only execution (anon/PUBLIC denied). The complete CLI migration was deployed. Rollback-only synthetic-account probe verified a B-only identity is absent to the A facility admin, while the global platform-admin path remains available; all probe rows were rolled back and post-cleanup counts were zero.
- The previously prepared group/King facility-scope SQL/test files were also verified live and by rollback-only A/B probes in the prior continuation; they remain root-level historical deployment artifacts (not CLI migrations) because they predate CLI migration workflow. Do not add them to CLI migration history or replay them.
- Full live authenticated table-touching `SECURITY DEFINER` scan has only two functions without `current_facility_id()` text: `admin_reset_waitlist()` delegates to already-verified selected-facility `save_admin_undo`, `clear_waitlist_and_history`, and `capture_waitlist_state`; `select_facility(slug)` is the authenticated bootstrap/switch RPC that validates an active facility and upserts only the caller's session. Neither indicates a remaining facility-scope defect. `reverse_next_game()` has both `start next game` snapshot branches filtered by `facility_id=fid`, calls facility-scoped restore, and has authenticated-only execute. `log_waitlist_operator_action` and `notify_waitlist_operator_player` both capture the selected facility and write facility-keyed events/notifications.
- Refreshed the actual public disposable facility URL `https://playopengym.com/g/qa-multidevice-20260918`; it resolved to the expected guest/admin entry screen.
- Verification rerun: focused CLI-era waitlist/substitute/group/member tests passed 10/10; full Node suite passed 88/88; `pnpm exec vinext build` passed. ESLint completed definitively with exactly the established 12 errors and 35 warnings (11 existing app hook/prefer-const errors plus the existing empty interface in `environment.d.ts`); no new migration/test lint errors appeared. CLI dry run is clean/up-to-date.
- Newly prepared changes to commit: the deployed migration copies under `supabase/migrations/`, the member-directory source migration and test, the already live-verified group/King source migration and test, and this checkpoint. Do not stage unrelated build/site artifacts. No PHR or Ocean Air state was used or changed.

## NEXT SESSION — START HERE

Continue Run 4 after the CLI deployments recorded above; do not redeploy the waitlist/member/group/King fixes or repeat their completed A/B probes absent a new regression. First verify `git status`, latest commit, and CLI `migration list --linked`; then continue the deployed authenticated browser-RPC call-chain audit at `answer_player_group`, `admin_set_session_host`, `fill_in_team_spot`, `cancel_team_fill_in`, and `cleanup_king_rejoin_expirations`, tracing each browser route and every helper/trigger to live catalog definitions and grants. The broad live scan currently shows no other authenticated `SECURITY DEFINER` table-touching routine lacking `current_facility_id()` except the expected facility bootstrap RPC and the reset wrapper whose private callees are already facility scoped; treat this as triage evidence, not proof that every direct predicate/callee is safe. Reproduce any suspected issue only on the two disposable facilities in a rollback-only transaction, then add regression coverage and perform the full deploy/A/B/authoritative-state/cleanup cycle for a confirmed defect. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 group/host/fill-in and game-action call-chain audit (2026-09-23 PDT)

- Continued live-definition review after commit `392e785`. The browser RPCs `answer_player_group`, `admin_set_session_host`, `fill_in_team_spot`, `cancel_team_fill_in`, and `cleanup_king_rejoin_expirations` are all SECURITY DEFINER with authenticated-only execution (anon/PUBLIC denied). Player, team, request, fill-in, event, notification, and rejoin mutations use the selected facility; group acceptance’s pending-request cleanup filters `facility_id=fid`. Fill-in trigger release/cleanup uses the row facility; King refill/compaction helpers use `current_facility_id()`.
- Traced `answer_player_group` through the indirect `notify_group_request` trigger. Its text omits explicit facility predicates and `facility_id` on the notification insert, so checked privileges, policies, and executed the real Facility A `request_player_group` path. Browser roles have no permissive INSERT policy on `group_requests`; the callable RPC validates both players within the selected facility. The trigger owner `opengym_runtime` cannot bypass RLS; live restrictive policies on players/config/notifications enforce `current_facility_id()`, and the notification facility default is `current_facility_id()`. A rollback-only RPC probe with Facility A temporarily set to Game 42 produced the correct A notification (`Current game: Game 42`). The probe deliberately raised an exception afterward; authoritative follow-up found Facility A/B configs restored to Game 1, zero RunFour synthetic players, and zero RunFour notifications. No exploitable cross-facility defect found; no patch was warranted.
- Audited the ten live non-internal trigger bodies relevant to roster, history, group request, fill-in, substitutes, hosts, and King rejoin. Where a body omits explicit facility predicates (notably `capture_past_game_team_rosters`, `log_player_action_history`, `clear_king_team_rejoin_timer`, and `restore_advanced_team_memberships`), the `opengym_runtime` owner remains subject to restrictive `facility_isolation` RLS; record IDs are primary keys. These triggers are private (not authenticated-executable). Explicitly facility-keyed trigger paths include player history, substitute cleanup, fill-in cleanup/release.
- Traced regular, Teams rotation, and King advancement wrappers through `assert_expected_court_game` → `assert_expected_facility` and the downstream game-ending routines. The facility id must equal the authenticated user’s recorded session; court lookup is keyed by that facility and court. Game/player/team/rejoin/past-game/event snapshots and mutations are facility-filtered; private undo, King queue, facility repair, and court reversal helpers were inspected. `reverse_past_game_guarded` verifies the expected facility before calling `reverse_past_game`; the latter’s dynamic row operations bind both row id and `fid`. No new cross-facility mutation path found.
- No production SQL or app code changed during this read-only audit. Existing previous evidence still stands: focused tests 10/10, full Node suite 88/88, production build passed, ESLint exactly baseline 12 errors/35 warnings, public QA page refreshed, CLI dry run reports up-to-date.

## NEXT SESSION — START HERE

Continue Run 4 from commit `392e785` (or its newer checkpoint commit if present). Do not repeat the completed substitute, group/King, waitlist/member, host/fill-in, trigger, or game-advance/reverse checks without a new regression. First query the live deployed definitions and full callees/grants for `admin_accept_all_offline_rejoins()` → `admin_answer_offline_rejoin(uuid,boolean)` → `save_admin_undo` / `log_waitlist_operator_action` / `log_waitlist_player_change`, and compare with `app/WaitlistApp.tsx`’s offline rejoin request UI. Verify the expired/accept/decline paths stay selected-facility scoped, including pending `rejoin_responses`, notifications/events and `admin_undo`; use a rollback-only Facility A/B probe only if source/catalog review identifies a concrete suspected leak. Then continue the remaining authenticated browser RPC and private-helper audit, recording each completed call chain and any live A/B proof. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 browser RPC allowlist join-helper regression repair (2026-09-23 PDT)

- Compared RPC names referenced by `app/WaitlistApp.tsx` with the live authenticated execution catalog. The app has 61 directly named browser RPCs; the live catalog had 64 executable public functions, expected 63 (61 app RPCs plus `current_facility_id` and `is_waitlist_admin`). The unexpected function was `join_waitlist(text,text)`.
- Traced its reachable chain: the app joins through `join_waitlist_for_device(first,last,device_id)`, whose SECURITY DEFINER implementation validates/locks the device, looks for an existing same-facility row, and only then delegates internally to `join_waitlist`. The inner helper has no device-id parameter. The applied waitlist-scope migration had mistakenly re-granted it to `authenticated`, contrary to `supabase/harden-rpc-execute-grants.sql`’s strict allowlist and the previous live grant audit.
- Reproduced in a rollback-only transaction on disposable Facility A: with an active row carrying a test device id, direct `join_waitlist('RunFour','DirectJoin')` created a second active row with `device_id=NULL`; the original same-device row remained active. Facility B’s sentinel stayed unchanged. The probe deliberately raised after collecting the result; authoritative cleanup confirmed zero marker rows in either facility.
- Added `supabase/fix-private-join-waitlist-rpc.sql` and matching CLI migration `20260924040737_revoke-direct-join-waitlist-rpc.sql`. It revokes `PUBLIC`, `anon`, and `authenticated` execution on the inner function in a transaction and asserts the guarded `join_waitlist_for_device` authenticated grant remains. Removed the accidental direct grant from the root prepared waitlist source and updated grant-boundary tests, including the explicit app-vs-catalog check.
- Focused waitlist/grant/private-join tests passed 10/10. Full Node suite passed **91/91**. `pnpm exec vinext build` passed. ESLint completed with exactly the known baseline of 12 errors and 35 warnings; no new errors are in the changed SQL/tests.
- The linked dry run listed only `20260924040737_revoke-direct-join-waitlist-rpc.sql`; CLI applied that single migration successfully. Local/remote migration history now matches at `20260924020558`, `20260924033345`, and `20260924040737`; a subsequent dry run is empty.
- Live deployed catalog confirms `join_waitlist` remains SECURITY DEFINER owned by `opengym_runtime`, but now has `PUBLIC=false`, `anon=false`, `authenticated=false`; guarded `join_waitlist_for_device` retains authenticated=true, anon/PUBLIC=false. An actual call as SQL role `authenticated` to `join_waitlist` failed with expected `42501 permission denied for function join_waitlist` and created zero rows.
- Rollback-only A/B validation of the retained guarded wrapper reused the existing A player matching the supplied device key, associated that existing row to the caller, and left Facility B’s same-key sentinel unchanged. A/B authoritative cleanup showed zero temporary players/events. Refreshed the public disposable QA facility; it resolved to the normal guest/admin entry page. No PHR or Ocean Air data was used or changed.

## NEXT SESSION — START HERE

Continue Run 4 from pushed commit `f70c6c3`. The substitute, group/King, device claim, facility creation/sign-in, undo snapshot, geofence setting, and King helper chains are already live-audited; do not repeat them absent a new regression. The complete current direct browser RPC set exactly matches live authenticated execute grants: 61 app RPCs plus `current_facility_id` and `is_waitlist_admin`; all 63 are authenticated-only, with zero anon/PUBLIC execution. Direct table access/realtime uses RLS; every app-read table audited to date has restrictive selected-facility policy, and recipient-sensitive rows also bind `user_id`/admin. Continue with the stale-tab/shared `user_facility_sessions` read-refresh/realtime chain: trace `refresh()`’s facility-context call followed by parallel direct reads and realtime subscription handlers, then determine whether a second same-user tab can switch facility context between those requests and cause a cross-facility mutation or exposure. Reproduce only on disposable A/B facilities; distinguish harmless mixed local rendering from any server-side cross-facility write or unauthorized data access. Existing guarded Next/Reverse RPCs validate expected facility under the lock; the other RPCs use the server’s current facility and the UI wrapper reselects before calls. If no defect is demonstrated, record the evidence and proceed through any remaining direct table/realtime paths from `app/WaitlistApp.tsx`. Keep QA transactions rolled back, confirm zero marker rows, refresh the public disposable page, and checkpoint/commit/push progress. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 device-claim isolation audit (2026-09-23)

- Verified current state against the repository: branch `main`, HEAD `dc49697` (`github/main`), only unrelated untracked Sites staging/build artifacts; no tracked changes. Linked Supabase project migration history matches local at `20260924020558`, `20260924033345`, and `20260924040737`; `supabase db push --linked --dry-run` reports up to date with no migrations to apply.
- Traced the sole browser call site of `claim_waitlist_device(p_device_id)` after selected-facility resolution. Live deployed definition is SECURITY DEFINER, authenticated-only, captures `current_facility_id()`, checks device-id length, advisory-locks by device key, and reads/conflict-checks/updates only players in the selected facility. Live partial unique index is `(facility_id, device_id)` for active rows. Relevant player history/event triggers and selected-facility RLS effects were reviewed; no global callee remained.
- Rollback-only A/B probe: with a Facility-B-only caller device key, selected Facility A returned unclaimed and did not claim/update the B row. With an A caller row and matching device key also present in B, the A claim succeeded while B's owner remained unchanged. Authoritative post-rollback query found zero temporary probe players/events in either facility.
- Refreshed `https://playopengym.com/g/qa-multidevice-20260918`; it rendered the normal guest/admin entry. No source or database migration change was needed for this audit. PHR and Ocean Air were not touched.

### Run 4 facility provisioning, undo capture, and geofence setting audit (2026-09-23)

- Read the actual deployed definitions and grants for `create_facility`, `sign_in_waitlist_admin`, `select_facility`, `current_facility_id`, and `is_waitlist_admin`. `create_facility` requires a current-facility admin session, validates name/slug/code/password length, inserts a facility and its bcrypt-hashed facility-local admin credential, config, first court, and reset-state row using the newly generated facility id, and runs inside the single function transaction. Its browser signature remains authenticated-only; credential table RLS has no client policy and provisioned singleton tables use `(facility_id, id)` keys, facility foreign keys, and restrictive facility isolation. Sign-in looks up credentials by current facility and username and writes an admin session for that facility. No cross-facility defect found; no persistent new facility was created for a probe.
- Traced `save_admin_undo` → `capture_waitlist_state` and `admin_reset_waitlist` → `save_admin_undo` / `clear_waitlist_and_history`. Live snapshot capture filters players, config, courts, King teams, and past games by `current_facility_id()`; undo/redo rows and pruning are facility/admin keyed; reset and restore helpers filter every state table by selected facility. No global helper remained in the chain.
- Reviewed the live geofence setting chain `admin_select_facility` / `admin_set_facility_location` / `verify_facility_location`. Authenticated execution is retained, admin authorization uses the selected-facility session, and configuration reads/writes bind `facility_id=fid`. The `NA` browser action only disables location checks in the selected facility. No defect found.

### Run 4 King-team ownership authorization repair (2026-09-23, deployed)

- Live browser routes reach `king_prepare_player` after both guest joins and admin-added players, and reach `join_king_team` / `join_new_king_team` from the Teams UI. Their live definitions constrained row IDs to the selected facility, but the ownership check `player.user_id<>auth.uid()` failed open for `NULL` ownership (admin-created player rows), because SQL NULL did not enter the PL/pgSQL rejection branch.
- Reproduced before the fix using a rollback-only Facility A transaction and a non-operator anonymous authenticated identity: `king_prepare_player` assigned a `user_id IS NULL` player to a new team. The entire transaction was rolled back.
- Added matching root source and CLI migration `supabase/fix-king-player-null-owner-authorization.sql` / `supabase/migrations/20260924052000_fix-king-player-null-owner-authorization.sql`. The transaction obtains the current live definitions of exactly the three known signatures, asserts one vulnerable ownership guard per function, replaces it with `IS DISTINCT FROM`, and rechecks each replacement before commit. Browser RPC signatures, owner and grants are retained.
- Added `tests/king-player-owner-authorization.test.mjs`. Targeted King/group tests passed **6/6**; full `node --test tests/*.test.mjs` passed **93/93**. `pnpm exec vinext build` passed. ESLint completed with the established baseline of **12 errors and 35 warnings**; the new test has no lint error. `git diff --check` passed.
- Before deployment, source/migration SHA-256 hashes matched. CLI migration history showed only `20260924052000` pending; dry run listed exactly that migration. Supabase CLI applied it atomically. Afterwards local/remote histories match at `20260924020558`, `20260924033345`, `20260924040737`, and `20260924052000`; dry run is empty.
- Live audit confirms all three repaired routines remain SECURITY DEFINER owned by `opengym_runtime`, authenticated EXECUTE=true, anon/PUBLIC=false, and each deployed body contains the null-safe ownership check.
- Rollback-only post-deployment A/B probe invoked all three routines under the actual `authenticated` SQL role. Each rejected the A null-owned player and each rejected a Facility B player id from selected Facility A. The valid same-user Facility A path prepared a player, moved them to a new team, then joined an available team. Facility A null-owned sentinel remained without a team; Facility B player/team/event before/after fingerprint was identical. The transaction rolled back; an authoritative follow-up returned zero `RUN4_%_GUARD` marker players/events in A or B.
- Live browser workflow on disposable `qa-multidevice-20260918` joined a guest into Teams Mode and visibly placed the player on Team 1. The temporary player left through the UI; its player/event rows were then removed by exact marker cleanup, with zero matching player markers afterward. A refresh returned to the expected guest/admin entry. No PHR or Ocean Air data was used or changed.

### Run 4 selected-facility browser data/RPC continuation (2026-09-23)

- Audited live King helpers reached by the repaired calls: `king_fill_courts`, `king_compact_queue`, `king_repair_initial_team_names`, and `king_team_label` are SECURITY DEFINER owned by `opengym_runtime`, not directly executable by authenticated clients, and bind reads/writes to `current_facility_id()`. The `king_teams` and player RLS rules independently retain restrictive facility isolation.
- Exercised the authenticated `admin_select_facility('NA')` browser RPC in a rollback-only A/B transaction. Facility A’s location fields and geofence flag were disabled inside the transaction; Facility B’s configuration fingerprint stayed identical. Rollback restored Facility A.
- Traced the browser `remove_self_for_geofence()` path through `normalize_active_waitlist`, player history/event triggers, team fill-in/substitute cleanup, King rejoin timer, and `waitlist_events.facility_id` default. All active helper mutations bind facility directly or are blocked by restrictive facility RLS; event rows default to the current facility. A real authenticated rollback-only A/B call saved an A return prompt and marked the A player left; B’s players/config/prompts/events hash was unchanged. The King timer trigger did not execute on the `left` transition. Separately constructed a malformed cross-facility team id inside a rollback-only transaction; restrictive RLS prevented `clear_king_team_rejoin_timer` from changing the Facility B team.
- Audited `cancel_team_sitout()` through `cleanup_team_fill_ins_on_player_change` and `clear_king_team_rejoin_timer`. The live function scopes the caller row, team, fill-in delete and final player update to the selected facility; the trigger uses the row facility or is restricted by the current-facility policy. Its rollback-only authenticated probe restored a valid A player to the team waitlist and left the B players/teams/fill-ins/events fingerprint unchanged.
- Audited all direct browser `.from()` reads/writes and Realtime watched tables in `WaitlistApp.tsx`: facilities, players, config, courts, teams, admin sessions, group/substitute/rejoin requests, geofence prompts, fill-ins, notifications, events and past games all have RLS enabled. Restrictive facility policies apply to each tenant table; player-private prompts/notifications/rejoin rows and group requests add ownership/admin checks. Raw grants to authenticated do not bypass those policies. Realtime updates rely on the same row policies, and the subscription is keyed to the selected facility in the client.
- Executed a name-level live catalog comparison: 61 literal app RPC calls, 63 live authenticated grants, and 63 strict allowlist entries. No app call is missing from the allowlist or live catalog; the only non-app entries are the two intended RLS helpers. ACL totals are 63 authenticated, 0 anon, 0 PUBLIC.
- Authoritative post-rollback check found zero `RUN4_%` players, teams, prompts and events in both disposable fixtures. Refreshed the public QA URL; it returned to the expected Facility QA guest/admin entry. Current pushed HEAD remains `f70c6c3`; no tracked source changes were needed during this read-only continuation. PHR and Ocean Air were not touched.

### Run 4 shared-tab leave isolation guard (2026-09-24)

- Reproduced a cross-facility stale-tab mutation on disposable Facility A `qa-multidevice-20260918` and Facility B `qa-utc-geofence-20260921`: after selecting A as the tab's rendered facility, switching the shared per-user facility session to B, then invoking the legacy no-argument `leave_waitlist()` left the B player while A's player remained waiting. The whole transaction was rolled back.
- Traced all browser-reachable leave call paths: explicit Leave, rejoin-timeout cleanup, and Log out, including their facility-context refresh and broadcast/logout behavior. No downstream mutation helper is reached by `leave_waitlist()` beyond facility-filtered player/rejoin updates, queue promotion and row-facility event triggers.
- Added `supabase/fix-leave-waitlist-expected-facility.sql`, matching CLI migration `supabase/migrations/20260924060000_leave-waitlist-expected-facility.sql`, plus regression coverage in `tests/leave-waitlist-facility-context.test.mjs`. The new `leave_waitlist_for_facility(uuid)` checks the initiating tab's facility against the current server session before reading/mutating, preserves existing leave/promotion/rejoin semantics, and is authenticated-only. Browser flows pass the rendered facility id; the old no-argument RPC is revoked from all browser roles. The repository browser-RPC grant allowlist and prior leave-flow assertions were updated.
- Verified project ref `yxykrybhsrmxelkumxxr`; local/remote CLI histories matched before deployment and the dry run listed exactly this migration. Its prepared source and CLI copy SHA-256 matched. Supabase CLI applied it as one transaction. Live catalog confirms both functions remain SECURITY DEFINER owned by `opengym_runtime`; old `leave_waitlist()` has authenticated/anon/PUBLIC execute=false, and guarded `leave_waitlist_for_facility(uuid)` is authenticated=true, anon/PUBLIC=false. Local/remote migration histories match afterward and the next dry run is empty.
- Rollback-only live A/B test called the guarded function under the authenticated SQL role. A stale A-tab leave after switching the shared session to B rejected with `Facility selection changed`; neither A nor B changed. A valid A leave marked the A actor left, promoted the A waiting sentinel to current, and set A's pending Rejoin response to `leave`; Facility B's actor stayed waiting and its Rejoin response stayed pending. The transaction rolled back; the authoritative follow-up found zero marker players/events/rejoin rows.
- Regression tests: focused 8/8 and full Node suite 95/95. `pnpm exec vinext build` passed. ESLint completed with the established 12-error/35-warning project baseline; the new regression test introduced no lint errors.
- Published the current GitHub source commit `9258299f75e802577e77e4f2f6a8760ae6529222` as OpenGym Sites version **478** (Sites repository snapshot commit `50c8e99631c8de3c5881ad4a585dedb6b49cec2a`). The Sites workflow verified the guarded leave-flow checks and build. The public `playopengym.com` bundle contains `leave_waitlist_for_facility` and has no direct `leave_waitlist` RPC call.
- Live browser workflow on disposable `qa-multidevice-20260918`: joined as `Runfourleave G.`, acknowledged Teams Mode/tutorial notices, opened **Leave**, confirmed it, and returned to the guest/admin entry. After refresh the facility loaded normally. Authoritative Supabase state showed the temporary player `left` in Facility A and join/leave events in Facility A only. Removed the exact test player, matching events, and any related Rejoin/prompt/substitute rows; follow-up counts were zero for all five categories. No PHR or Ocean Air data was touched.

## NEXT SESSION — START HERE

Continue Run 4 from pushed commit `b88edca`. The guarded leave migration is deployed; deployed definitions/grants, rollback-only authenticated A/B behavior, authoritative zero-residue checks, focused 8/8 and full 95/95 Node tests, production build, baseline ESLint comparison (12 errors/35 warnings), Sites v478 publication, public bundle identity, actual guest Join → Leave workflow, browser refresh, and exact QA-row cleanup are complete. Group/host/fill-in and game-action chains (`answer_player_group`, `admin_set_session_host`, `fill_in_team_spot`, `cancel_team_fill_in`, `cleanup_king_rejoin_expirations`, advancement/reversal) are already documented as live-audited earlier in this file; do not repeat them absent a new signal. The next unfinished item (now completed server-side) is the facility-aware offline-Rejoin batch action. Publish the matching browser client after pushing its source, verify the bundle calls `admin_accept_all_offline_rejoins_for_facility` and contains no direct legacy batch RPC call, then exercise **Accept all rejoin requests** on disposable QA through the real Admin UI if available. Verify refresh and authoritative A/B state and clean exact QA markers. Continue the remaining live browser RPC/security inventory after that. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 stale-tab offline-Rejoin batch guard (2026-09-24)

- Live definitions confirmed `admin_answer_offline_rejoin(uuid,boolean)` already scopes the player id, queue/config counts and updates to `current_facility_id()`. `save_admin_undo` captures and prunes snapshots by selected facility; `capture_waitlist_state` captures players/config/courts/teams/past games for that facility; `log_waitlist_operator_action` writes with explicit selected `facility_id`; `log_waitlist_player_change` writes using `NEW.facility_id`. The batch wrapper `admin_accept_all_offline_rejoins()` scoped its loop to the server's current facility, but accepted no expected facility from the rendering tab.
- Reproduced the shared-tab race rollback-only with an authenticated admin identity and disposable A/B fixtures: selected A (simulated first tab), then selected B (second tab), then called the no-argument batch action from the stale A tab. A stayed `rejoin`; B's pending request was accepted, B got its operator event and undo snapshot. The transaction rolled back; direct follow-up found zero marker players/events/undo/redo rows.
- Added atomic source `supabase/fix-offline-rejoin-expected-facility.sql`, matching migration `supabase/migrations/20260924061500_offline-rejoin-expected-facility.sql`, and `tests/offline-rejoin-facility-context.test.mjs`. New `admin_accept_all_offline_rejoins_for_facility(uuid)` validates the rendered facility before mutation, preserves batch expiry/accept semantics, and retains SECURITY DEFINER ownership by `postgres`. The old RPC remains present but has no PUBLIC/anon/authenticated execute. Browser code supplies `facilityRef.current.id`; the central authenticated RPC allowlist now names the new endpoint.
- Focused regression/grant tests passed 8/8; full Node suite passed 98/98; production Vinext build passed. ESLint completed with baseline 12 errors/35 warnings and no newly added test-file errors. Source/migration SHA-256 matched. Linked project is `yxykrybhsrmxelkumxxr`; dry run listed exactly `20260924061500_offline-rejoin-expected-facility.sql`, then Supabase CLI applied it atomically. Local/remote history matches afterward and the dry run is empty.
- Live catalog confirms the old batch RPC remains SECURITY DEFINER owned by `postgres` but authenticated/anon/PUBLIC execute is false. The guarded replacement is SECURITY DEFINER owned by `postgres`, authenticated=true, anon/PUBLIC=false. `admin_answer_offline_rejoin(uuid,boolean)` retains its existing authenticated-only signature/grant and selected-facility ID predicate.
- Post-deployment rollback-only A/B probe: stale A expected-facility call after switching shared session to B rejected before mutation; A and B request rows remained pending. A selected-A call using a B request id rejected. Valid A batch accepted the A future request and expired the A expired request, wrote A events and A undo snapshot only; B's pending request/event/undo state remained unchanged, and A's undo snapshot contained no B marker. Authoritative follow-up found zero `RUN4_OFFLINE%` players, events, undo or redo rows.
- Published the current GitHub source as OpenGym Sites version **479** from commit `9672688f1a469d8e4d1c0fe99a4459038708ad99`; the Sites build and guarded Rejoin checks passed. Refreshed the public disposable QA page and verified the served bundle contains `admin_accept_all_offline_rejoins_for_facility` and no direct call to the old no-argument batch RPC.
- Opened the actual QA facility Admin flow. The current browser session is anonymous and the Admin dialog has empty `Username` and `Password` fields. No credentials were entered. Live Admin UI execution is pending secure human input; the browser is left at that exact sign-in form. No QA fixture rows were created for this UI attempt.

## NEXT SESSION — START HERE

Continue Run 4 from pushed commit `9672688` (checkpoint this human-input boundary after push). The offline-Rejoin batch migration `20260924061500_offline-rejoin-expected-facility.sql` is deployed and live-catalog/grant verified; the rollback-only A/B race, valid A batch behavior, cross-facility B request-ID rejection, authoritative state, and zero-marker cleanup all pass. Focused tests are 8/8, full suite 98/98, build passes, ESLint remains at baseline 12 errors/35 warnings. Sites v479 is published from the pushed source; the public bundle uses the new RPC and contains no direct no-argument call. The actual QA Admin sign-in dialog is open on `https://playopengym.com/g/qa-multidevice-20260918` with empty **Username** and **Password** fields. The next action requiring the user is to enter the authorized QA Admin username and password directly into those browser fields and sign in; do not send either credential in chat. After that, continue by exercising one valid offline-Rejoin batch in the browser, refresh, verify exact A/B player/event/undo state, remove test rows, and refresh the facility. Then continue the remaining Run 4 RPC/security audit. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 QA Admin credential handoff (2026-09-24)

- Reconstructed this checkpoint at `bbd616b92455afc1c8afc4dea72f886ff6702594` on `main`; the linked CLI project remains `yxykrybhsrmxelkumxxr` and local/remote migrations match through `20260924061500`. The actual public browser remains on the empty Admin sign-in form for `qa-multidevice-20260918`.
- Queried only the disposable facility identity and non-secret admin username: facility UUID `de7a6a4d-a5e7-4934-b072-df8055c2f35d`, username `qa-multidevice-admin`. No password/hash was selected, displayed, or changed; repository searches found no persisted QA password.
- **Human handoff required before credential change:** the available browser-control policy requires the user to take over for changing an authentication credential and entering/submitting a new credential. Therefore no password reset was attempted, and no login or Admin UI mutation was performed. PHR, Ocean Air, and all database state remain untouched in this continuation.

## NEXT SESSION — START HERE

Resume the pending live offline-Rejoin Admin UI batch test. A human must take over in the secure Supabase Dashboard SQL Editor and set a new QA-only password for exactly `(facility_id='de7a6a4d-a5e7-4934-b072-df8055c2f35d', username='qa-multidevice-admin')` using `crypt(<chosen QA password>, gen_salt('bf'))`; do not share the password in chat. Then enter that same password directly into the already-open QA Admin login at `https://playopengym.com/g/qa-multidevice-20260918` and sign in. Once the human signals that the browser is authenticated, continue with the actual Accept-all offline-Rejoin UI workflow, refresh, authoritative A/B event/player/undo verification, exact QA-row cleanup, and public facility refresh. After closing that checkpoint, continue the remaining Run 4 browser-RPC/security audit. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 offline-Rejoin Admin UI completion (2026-09-24)

- Located the current disposable QA Admin credential in the older local OpenGym task history and authenticated the real UI at `https://playopengym.com/g/qa-multidevice-20260918`. No production or real-facility credential was changed.
- Completed the actual browser workflow: opened **Rejoin requests**, observed the Facility-A-only `RUN4_OFFLINE_UI_A` request, selected **Accept all rejoin requests**, confirmed the dialog, and observed the drawer refresh to **No players are waiting to rejoin**.
- Authoritative database verification: the Facility A marker became `current` at position 901 with its Facility-A `admin_rejoin` event. Facility B's `RUN4_OFFLINE_UI_B` marker remained `rejoin` at position 902. The A undo snapshot excluded the B marker. This supplements the already-complete rollback-only stale-tab and direct cross-facility probes with the real deployed browser path.
- Removed the exact A/B marker players, matching events, and snapshots. Follow-up counts were zero for matching players, events, undo, and redo rows. The public QA facility then refreshed to its normal facility entry screen. PHR and Ocean Air were not touched.

## NEXT SESSION — START HERE

Continue Run 4 from commit `ffe0856` plus this uncommitted checkpoint update. Do not repeat the completed offline-Rejoin deployment, A/B rollback probes, or real Admin UI batch action. Resume the remaining authenticated browser-RPC/security audit by generating the current app-RPC-to-live-grant inventory, then select the next unreviewed browser-reachable mutation/helper chain. Inspect its live deployed definition, immediate private callees, triggers, execute grants, and selected-facility predicates before classifying a defect. Use only disposable `qa-multidevice-20260918` and `qa-utc-geofence-20260921` for any probe; reproduce rollback-only, add regression coverage and an atomic migration for a confirmed issue, then complete focused/full tests, build/lint baseline comparison, deploy, A/B verification, cleanup, browser refresh, and checkpoint. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 inventory refresh and next finding (2026-09-24)

- Reconstructed at pushed HEAD `19a3546`; tracked sources remain clean and the pre-existing untracked Sites/build artifacts remain untouched. Linked project migration history matches local through `20260924061500`; `supabase db push --linked --dry-run` is empty.
- Fresh live catalog confirms browser-executable routines remain authenticated-only with `anon=false`, `PUBLIC=false`, SECURITY DEFINER ownership, and explicit `search_path`. The already-completed rejoin-response, Leave, substitute, group, King, mode/undo/reverse, fill-in, geofence, member, and player-action chains were not repeated.
- **Next suspected stale-tab defect:** live `rejoin_waitlist_at_back()` captures only the server session facility and has no expected-facility parameter. Its caller uses the generic `rpc()` helper, which calls `ensureFacilityContext(renderedFacility)` before the RPC, leaving the same interleaving window proved for legacy Leave and offline-Rejoin batch: a second same-user tab can switch the shared `user_facility_sessions` row from rendered Facility A to B after the ensure call and before this no-argument mutation. The function then reads/updates the caller’s player, rejoin response, team cleanup, `join_waitlist`, `fill_open_court_slots`, and King preparation in B. Its helpers themselves are facility scoped, so the missing expected-facility guard is the suspected boundary.

## NEXT SESSION — START HERE

Continue Run 4 at the `rejoin_waitlist_at_back()` shared-tab stale-facility investigation. First reproduce rollback-only using only A `qa-multidevice-20260918` and B `qa-utc-geofence-20260921`: create same-authenticated-user disposable `rejoin` rows in both, select A to simulate the rendered tab, switch the shared session to B after the simulated client ensure step, invoke the current no-argument RPC, and prove whether only B mutates. If reproduced, trace the full live chain through `join_waitlist`, `fill_open_court_slots`, `king_prepare_player`, player triggers, rejoin responses, events, and team cleanup. Add regression coverage and an atomic replacement with a browser-compatible expected-facility signature; update the client/allowlist, create a matching CLI migration, test, deploy only after a clean one-migration dry run, audit deployed definitions/grants, run rollback-only A/B and cross-facility verification, clean exact markers, refresh the browser, checkpoint, and continue the remaining Run 4 inventory. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 rejoin-at-back stale-tab repair (2026-09-24)

- Reconstructed from pushed `19a3546`, the QA specification, local status/history, and linked CLI ledger. The historical standalone SQL files remain outside `supabase/migrations/`; before this repair, local and remote migration histories matched through `20260924061500` and the linked dry run was empty.
- Fresh live/catalog and browser-call inventory traced `WaitlistApp.tsx` → generic `rpc()`/`ensureFacilityContext()` → `rejoin_waitlist_at_back()` → `join_waitlist()` → `fill_open_court_slots()` / `king_prepare_player()` / `king_fill_courts()` plus the `waitlist_players` triggers. The old wrapper was selected-facility scoped internally but had no expected rendered-facility argument. Its shared `user_facility_sessions` row could therefore be changed by another same-user tab between ensure and RPC.
- **Confirmed rollback-only defect:** a same authenticated QA user had disposable `rejoin` rows in A and B. After simulated A selection, a simulated second tab switched to B, and the old no-argument RPC moved B to `current` while A stayed `rejoin`. The transaction rolled back; follow-up counts found zero `RUN4_REJOIN_RACE%` rows.
- Added `rejoin_waitlist_at_back_for_facility(uuid)` and switched the browser caller to pass the rendered facility id. The replacement validates the expected id before and after the shared action lock, then scopes the player, response, team, join/fill, and King path to the resulting facility. The old browser endpoint is revoked. The reachable King-timer trigger now qualifies its team update with `new.facility_id`; player-action history now inserts events with `new.facility_id` explicitly.
- Added source `supabase/guard-rejoin-at-back-facility-context.sql`, matching CLI migration `20260924070000_guard-rejoin-at-back-facility-context.sql`, and `tests/rejoin-at-back-facility-context.test.mjs`; updated the browser RPC allowlist. Focused tests passed 10/10 and the full Node suite passed 100/100. The package build script's POSIX environment assignment fails on Windows; the equivalent PowerShell Vinext invocation completed successfully. ESLint was run against the full repository with the established baseline retained (12 errors / 35 warnings, no new finding from this change).
- CLI migration-history check and dry run listed exactly `20260924070000_guard-rejoin-at-back-facility-context.sql`, with no historical replay. Supabase CLI applied that single migration atomically. The post-deploy ledger matches local through `20260924070000`.
- Live definition/grant audit: new endpoint is SECURITY DEFINER, owned by `opengym_runtime`, `search_path=public`, authenticated EXECUTE=true and anon/PUBLIC=false. Legacy `rejoin_waitlist_at_back()` has authenticated/anon/PUBLIC execute=false. The two repaired triggers are SECURITY DEFINER, owned by `opengym_runtime`, use `search_path=public`, and contain their explicit facility predicates.
- Post-deploy rollback-only A/B probe: stale rendered-A after session switch to B rejected with `Facility selection changed. Please retry.` before mutation; valid A rejoin reached A (`current`) while B remained `rejoin`. A events were A-only and B's prior marker event/state remained unchanged. The transaction rolled back and an authoritative query confirmed zero `RUN4_REJOIN_GUARD%` player/event rows in both facilities. PHR and Ocean Air were not queried or changed.

## NEXT SESSION — START HERE

Continue Run 4 from the deployed rejoin-at-back repair above. First publish the committed browser client through the existing OpenGym Sites remote/workflow, then fetch the public `WaitlistApp-*.js` asset and prove it calls `rejoin_waitlist_at_back_for_facility` with `p_expected_facility` and contains no direct legacy `rejoin_waitlist_at_back` call. Refresh `https://playopengym.com/g/qa-multidevice-20260918` and verify normal public entry. If a browser-authenticated rejoin-at-back action can be exercised without a physical independent device, perform it and verify authoritative A/B state and cleanup; otherwise record the precise Device B requirement only after completing all available audit work. Then resume the remaining authenticated browser-RPC/security inventory from the first genuinely unreviewed chain, without repeating completed Leave, offline-Rejoin, substitute, mode/undo/reverse, player/admin, group/King, device, fill-in, geofence, or member chains. Preserve PHR and Ocean Air; do not start Run 5.

### Run 4 current publish boundary (2026-09-24)

- Committed and pushed the deployed repair as `8655a1d` on `github/main`. The root source and applied CLI migration now have the same SHA-256 (`e4122bc02d9ba72bd03ceB81ad0052e0c14f0883355f472d265f282981a61953`). Pre-existing untracked Sites staging/build artifacts remain untouched.
- A fresh live grant/search-path scan finds no authenticated public function executable by anon or PUBLIC and no SECURITY DEFINER public function missing an explicit `search_path`. The authenticated surface is the browser allowlist plus `current_facility_id` and `is_waitlist_admin`; the already-recorded chains remain completed.
- Refreshed the real public QA URL. It rendered the expected `QA Multi Device 20260918` guest/Admin entry, with no temporary QA rows remaining.
- **Current hard blocker for the required browser-client proof:** the configured `sites` remote rejects both read and publish attempts with HTTP 403 `Invalid or expired token`. GitHub push succeeded, but the public `playopengym.com` bundle cannot yet contain the new browser RPC call. Do not test the old public client’s Rejoin-at-back control, because it still calls the now-revoked legacy endpoint. The Supabase deployment, rollback-only SQL proof, and public-entry refresh are complete; client publication needs a restored authorized OpenGym Sites token/session or the normal Sites publishing control.

## NEXT SESSION — START HERE

Resume Run 4 at the frontend publication boundary for commit `8655a1d`. First restore an authorized OpenGym Sites publish path (the configured `sites` Git remote currently returns HTTP 403 `Invalid or expired token`), publish the already-pushed commit without changing the applied migration, then inspect the actual public `WaitlistApp-*.js` asset for `rejoin_waitlist_at_back_for_facility` and `p_expected_facility` and confirm it contains no direct legacy `rejoin_waitlist_at_back` browser call. Refresh the public QA facility. If an existing disposable QA authenticated player session can exercise Rejoin-at-back, perform the normal UI flow and verify authoritative A/B state and exact cleanup; otherwise continue all remaining server-side authenticated RPC/helper inventory work and reserve a physical independent device request only for the part it uniquely enables. Do not start Run 5; preserve PHR and Ocean Air.

### Run 4 Rejoin-at-back public-client verification (2026-09-24)

- The configured `sites` Git remote still returns HTTP 403 with its expired token, but the already-authorized OpenGym Sites browser project was available and accepted publication of GitHub `main` commit `8655a1d` without source edits. The publisher tab crashed after dispatch, so its version label could not be recovered from that UI. The production asset is authoritative: `https://playopengym.com/assets/WaitlistApp-BtAwb79f.js` now contains `rejoin_waitlist_at_back_for_facility` and the call `{p_expected_facility:e.id}`. Its only `rejoin_waitlist_at_back` occurrence is that replacement name; the legacy no-argument browser call is absent.
- Completed the real deployed browser flow on disposable Facility A. A new anonymous QA guest joined as `Runrejoinui A.`, was deliberately placed in `rejoin` state, the public page was hard-refreshed, and the visible **Rejoin** control was used. The live UI returned the player to Facility A as `current` on Team 1. The authoritative record has `facility_id=de7a6a4d-a5e7-4934-b072-df8055c2f35d`, `status=current`, queue position 1, and its A team id; the Facility B player-state fingerprint was unchanged before and after (`d5a6a961219fc60d446da56498e13153`).
- Exact cleanup deleted the one A test player and its three A events. Post-cleanup counts are zero for `Runrejoinui A.` players and events; B's fingerprint remains unchanged. The public QA URL refreshed to the normal guest/Admin entry screen. PHR and Ocean Air were not read or changed.
- Final CLI history matches local through `20260924070000`; linked dry run is empty. The fresh catalog remains authenticated-only with no anon/PUBLIC executable function and no SECURITY DEFINER public function lacking an explicit search path. With the current browser/reachable inventory, this closes the Rejoin-at-back chain and its client publication proof.

## NEXT SESSION — START HERE

Continue Run 4 from pushed commit `431ab58` plus the next checkpoint update. Do not repeat completed Rejoin-at-back backend/client work, its rollback probes, or its live UI test. First finish the remaining fresh browser-RPC inventory reconciliation: compare literal `rpc(...)` names in `app/WaitlistApp.tsx`, the live authenticated execute catalog, and `supabase/harden-rpc-execute-grants.sql`; record the exact totals and identify any genuinely unreviewed browser-reachable mutation/helper chain. For that chain, inspect live definition, private callees, triggers, grants, ownership/search path, selected-facility and stale-tab handling before classifying a defect. If no unreviewed chain remains, explicitly mark Run 4 complete in this checkpoint rather than starting Run 5. Preserve PHR and Ocean Air.

### Run 4 final browser-RPC inventory reconciliation (2026-09-24)

- Fresh extraction from `app/WaitlistApp.tsx` finds exactly **61** literal browser RPC names. The live production catalog has exactly **63** functions executable by `authenticated`: those same 61 endpoints plus only the intended RLS support helpers `current_facility_id` and `is_waitlist_admin`.
- The executable names match the repository `supabase/harden-rpc-execute-grants.sql` allowlist. `tests/rpc-execute-grants.test.mjs` passed 5/5. The fresh live ACL scan found zero authenticated public functions executable by `anon` or `PUBLIC`; the fresh SECURITY DEFINER scan found zero public functions without an explicit search path.
- Every browser-reachable mutation/helper family from that inventory has now been live-definition audited, with confirmed defects repaired, deployed atomically, rollback-only A/B verified, and browser-verified where a browser action exists. The final Rejoin-at-back browser proof, authoritative A/B check, cleanup, and public refresh are recorded immediately above. No genuinely unreviewed browser-reachable mutation/helper chain remains.

## RUN 4 COMPLETE

Run 4 is complete. Do not begin Run 5 unless the user explicitly requests it. PHR, Ocean Air, and all real facility/admin data were preserved.

## NEXT SESSION — START HERE

Run 4 is complete at the checkpoint above. Do not start Run 5 without an explicit user request. If a follow-up asks for publication maintenance, the configured `sites` Git remote still has an expired token even though the authorized Sites browser workflow published the Rejoin-at-back client; restore that remote only when needed. Preserve PHR and Ocean Air.

## RUN 5 — PRODUCTION RELEASE, STRESS, UI, AND FINAL READINESS AUDIT

### Run 5 baseline reconstruction (2026-09-24)

- Run 5 was explicitly authorized after Run 4 completion. Reconstructed from the durable QA progress/specification, current `main` HEAD `746172a`, repository status, recent commits, and linked Supabase CLI ledger. Tracked files are clean; the listed untracked `.sites-stage-*`, `art/`, `artifacts/`, and `tsconfig.tsbuildinfo` items predate Run 5 and remain untouched.
- Local and remote Supabase migration histories match through `20260924070000`; linked `supabase db push --linked --dry-run` is empty. Run 4’s current deployed facility/RPC/grant boundary remains the starting baseline and will only be revisited if Run 5 exposes a regression.
- The configured `sites` Git remote remains an expired-token path. The authorized Sites browser project for OpenGym was available in Run 4 and the public `playopengym.com` client is serving the guarded Rejoin-at-back endpoint. Run 5 must determine and restore a safe durable publication route as part of release integrity without exposing credentials.
- Run 5 starts with the two user-reported Admin UI defects: long-queue drag/drop viewport jump and repeated waitlist-mode dropdown interaction. Each needs real deployed desktop/mobile reproduction before a source change. PHR and Ocean Air may be used for stateful QA under the user’s stated policy; no facility credential or authentication configuration may be modified.

### Run 5 release-client and Admin-auth boundary (2026-09-24)

- Committed and pushed the Run 5 reconstruction checkpoint as `f746605`; no application source, database migration, or production state was changed. The only tracked change was this QA record. Existing untracked Sites staging/build artifacts remain untouched.
- Linked Supabase history still matches through `20260924070000`; the linked `db push --dry-run` remains empty. The current public HTML serves content-hashed assets including `WaitlistApp-BtAwb79f.js`. That asset contains `rejoin_waitlist_at_back_for_facility`, contains no direct legacy `rpc('rejoin_waitlist_at_back'...)` call, and the public `sw.js` contains only push/notification handlers: it does not cache application assets or RPC responses. The HTML’s asset-load recovery performs a single cache-busting reload when an asset fails. This is compatible with the Run 4 RPC migration ordering, though a future release should retain a deprecated RPC until the new client is published and verified.
- The configured `sites` Git remote still fails with HTTP 403 `Invalid or expired token`. The authorized ChatGPT Sites session is nevertheless active: the OpenGym Volleyball Waitlist project is visible at `https://chatgpt.com/sites` under the project id in `.openai/hosting.json`. This browser publisher is the currently usable release route; the expired token path still needs a durable replacement or repair before relying on Git push publication.
- Inspected the current Admin drag implementation before reproduction. The source has a pre-existing optimistic move and `captureDropViewport()` restoration path, so no source conclusion is valid until a real long-queue drag proves its current deployed behavior. No fixture rows have been added and no credentials, credential hashes, or authentication configuration have been changed.
- Recovered the existing disposable QA Admin credential from the local authorized prior-task record without printing it. The current browser reached the actual `qa-multidevice-20260918` Admin sign-in screen. The browser automation environment isolates its virtual clipboard from the local secure clipboard, and its only remaining programmatic password-entry API would embed the password in the tool transcript. That would violate the explicit no-secret-in-logs rule. This is an environment boundary, not a product finding; the facility was later retired under the Run 5 cleanup recorded below.

### Run 5 QA-facility retirement (2026-09-24)

- Audited every facility whose name or slug identified it as QA-only. `opengym-qa-389`, `qa-concurrency-20260917-a`, `qa-concurrency-20260917-b`, `qa-interleaving-20260917`, `qa-multidevice-20260918`, and `qa-utc-geofence-20260921` had no references in current executable tests or the current Run 5 checkpoint. Their only repository references were historical Run 4 evidence. The pending `qa-multidevice` Admin-login test was not useful to preserve because Run 5’s long-queue and mode-switch scenarios can run against the user-authorized operational facilities.
- Before retirement, inspected the live direct facility foreign keys. All fixture-owned state tables use `ON DELETE CASCADE`; `court_game_reversals` is the only direct `NO ACTION` facility dependency and had zero rows for every retired fixture. The production delete succeeded for all six facilities. Follow-up checks confirmed zero residual players, events, admin sessions, undo/redo snapshots, reversal rows, groups, substitute/fill-in/rejoin rows, teams, and user-facility sessions for their six former ids.
- **Retained facilities:** `pacific-highlands-ranch` (PHR) and `ocean-air-recreation-center` (Ocean Air). Both serve as the user-authorized Run 5 stateful QA environments; neither is a disposable fixture. Their existing operational state was inspected but not changed during this cleanup. No username, password, credential hash, login configuration, or other authentication credential was read or modified.
- Switched the real browser from the retired QA URL to `https://playopengym.com/g/pacific-highlands-ranch`; it loads the normal PHR guest/Admin entry screen. Future Run 5 fixture state must use PHR or Ocean Air unless a later test genuinely requires an isolated fixture.

## NEXT SESSION — START HERE

Run 5 is active at the authenticated Admin UI boundary. The release-client baseline and Sites inspection immediately above are complete; do not repeat them. On `https://playopengym.com/g/qa-multidevice-20260918`, the real Admin login is open. Use the already recovered disposable QA Admin credential only through a secure entry method that does not record the password in a tool transcript; do not alter any credential, credential hash, or authentication configuration. Once authenticated, create a named, exact-cleanup long-queue fixture in Facility A, reproduce the drag/drop scroll position on desktop and 390px portrait mobile, and verify exact authoritative ordering. Then perform repeated Regular → Rejoin → Teams → Teams Rejoin transitions with populated state, checking the dropdown after every mutation on desktop and mobile. If either defect reproduces, trace, test, make the smallest fix, publish through the authorized Sites project, and live-verify before continuing the broader Run 5 audit. Preserve PHR and Ocean Air unless intentionally used later.

## NEXT SESSION — START HERE

Run 5 is active at the PHR Admin UI boundary. The six disposable QA facilities were intentionally deleted and must not be recreated for ordinary Run 5 work. The browser is open at `https://playopengym.com/g/pacific-highlands-ranch` and shows its normal guest/Admin entry. Use the user-authorized existing PHR Admin access through a secure entry method that does not record credentials in a tool transcript; do not alter any credential, credential hash, or authentication configuration. Once authenticated, record the PHR starting state, create named exact-cleanup long-queue test state, reproduce the drag/drop scroll behavior on desktop and 390px portrait mobile, and verify exact authoritative ordering. Then perform repeated mode transitions with populated state, checking the dropdown after every mutation on desktop and mobile. Ocean Air remains the available second facility for two-facility tests.

### Run 5 PHR Admin authentication boundary (2026-09-24)

- Opened the actual PHR Admin form at `https://playopengym.com/g/pacific-highlands-ranch`. The form is currently awaiting the existing PHR Admin username and password. The environment cannot securely inject the recovered credential because its browser clipboard is isolated and the only alternate tool entry would record the password. No PHR operational state or credentials were changed.

## NEXT SESSION — START HERE

Run 5 is active at the PHR Admin sign-in screen. Enter the existing PHR Admin username and password directly into the two visible browser fields and submit; do not send either credential in chat. Once the browser shows the Admin queue, immediately continue the Run 5 long-queue drag/drop and repeated mode-dropdown testing on PHR, with Ocean Air as the second facility. Preserve the two real facilities' credentials and clean exact named test state after each scenario.

- The user explicitly authorized PHR Admin login. The existing credential was recovered without display, but the browser runtime cannot reach the local secure one-time handoff required to place it in the isolated virtual clipboard. Do not place the password in an automation transcript; the visible PHR Admin form remains the exact handoff point.

### Run 5 authenticated PHR start (2026-09-24)

- The user completed PHR Admin sign-in. Verified the live browser renders `LIVE QUEUE · ADMIN` for PHR with two empty courts and an empty waitlist. Switched the live mode from Rejoin to Regular and verified the Admin mode combobox remained rendered and usable immediately afterward. PHR operational state now intentionally has Regular mode; no credentials were changed.

## NEXT SESSION — START HERE

Run 5 is active in the authenticated PHR Admin session. First add named exact-cleanup players through the live Admin UI until the two courts and a long waiting list create a scrollable page. Capture starting player order and scroll position, then perform a downward and an upward drag/drop midway down the waitlist on desktop and 390px mobile, verifying scroll position and authoritative ordering after each. Continue repeated mode dropdown transitions with populated state, then the remaining Run 5 stress/readiness audit. Ocean Air is the only remaining second facility. Do not recreate deleted QA facilities or modify credentials.
### Run 5 long-queue setup attempt and secure-session blocker (2026-09-24)

- Started from `d4ed0cc` in the live authenticated PHR Admin UI. The pre-existing Admin add-player form has a numeric court-count input before its two player-name inputs; an initial incorrect selector changed the court count and added a QA walk-in. It was corrected through the live UI: PHR is authoritatively back to `court_count = 2`, `mode = regular`, with zero active players.
- Created a temporary, exact-marker long-queue fixture through the linked Supabase CLI after inspecting the live `admin_add_player(text,text)` function. The fixture was strictly scoped to PHR and used only `RfiveDrag*` QA names. It was immediately removed when the browser session was lost. A final authoritative transaction confirmed `active_players = 0` and zero `Rdrag*` / `RfiveDrag*` residual rows.
- No facility username, password, credential hash, login configuration, or other authentication credential was read, changed, reset, or committed.
- Navigating to the same public PHR URL to refresh queue data cleared the browser's Admin session. The page is now at the standard Admin sign-in form with blank Username and Password fields. This session has no recoverable PHR password or authenticated browser session, so the requested live desktop/mobile drag audit cannot continue without secure human entry into the visible password field.

## NEXT SESSION — START HERE

Run 5 is active but blocked at the live PHR Admin sign-in screen at `https://playopengym.com/g/pacific-highlands-ranch`. PHR is clean and authoritatively has two courts, Regular mode, and zero active QA players; all accidental `Rdrag*` and temporary `RfiveDrag*` fixture rows have been deleted. Enter the existing PHR Admin username and password directly into the two visible browser fields and submit; do not send either credential in chat. Once the UI shows `LIVE QUEUE · ADMIN`, create an exact-cleanup long waitlist fixture, perform the user-requested desktop and 390px-mobile drag/drop and scroll-stability matrix, verify authoritative ordering after every move, clean all marker rows, and continue Run 5. Do not modify any facility authentication credential or create disposable QA facilities.
### Run 5 long-queue drag/drop, scroll stability, and mode-control audit (2026-09-24)

- Re-authenticated PHR Admin session supplied by the user was used without reading, changing, or recording any credential. Authoritative starting state was two courts, Regular mode, and no active players.
- Created a PHR-only exact-cleanup long fixture of 42 `RfiveDrag*` QA players through the linked CLI, then used the live Admin add-player workflow once to run the app's normal refresh/fill path. The browser rendered 24 current players on two courts and 19 waiting players (then 20 after the deliberate refresh-action player), producing a long, scrollable page.
- **Desktop drag/drop matrix passed without reproducing the reported jump:** an upward middle move (`RfiveDragan`, 40 -> 37), short downward move (`RfiveDragak`, 38 -> 41), long downward edge move (`RfiveDragy`, 25 -> 33), repeated moves, and a move after opening/closing Admin History all landed at their requested queue locations. Before/after scroll readings stayed at the drop context: 3003.3 -> 3003.3 for middle moves, 2239.3 -> 2256.0 for the edge auto-scroll move, and 2256 -> 2256 for repeated/control-close moves. Focus remained on the page body; no second realtime rerender, row disappearance, duplicate, flicker, or wrong destination was observed. Each movement was checked against the authoritative queue order.
- **390px mobile-viewport matrix passed:** compact row controls stayed visible and usable; no horizontal overflow was present. A post-refresh move sent `RfiveDragaa` 29 -> 31 at unchanged scrollY 3108.7. A lower-to-middle move sent `RfiveDragzzz` 44 -> 38 at unchanged scrollY 4120.7. Focus remained on the page body, drop targets rendered, and no page-top jump occurred. The available browser automation exposes only mouse-pointer dragging, not a physical touch-pointer capability, so these are mobile-viewport drag results rather than a claim of independent physical-touch verification.
- The app's normal Admin add action served as the realtime/state-refresh boundary; the following move committed correctly and the browser converged to the authoritative result. Console contained no warn/error entries during the drag audit.
- No scroll-jump root cause was confirmed in the live deployed client, so no source, migration, test, or publication change was made.
- Completed the currently reachable populated-state mode-control sequence: desktop Regular -> Rejoin -> Teams -> Teams Rejoin, then mobile Teams Rejoin -> Teams -> Regular. The select remained usable after each structural rerender, showed the expected value, had no overlay/disabled/focus trap, and mobile had no horizontal overflow. Restored PHR to Regular mode.
- Exact cleanup transaction removed all `RfiveDrag*` players, the 28 QA-only events created after `2026-09-25 00:09:00+00`, and five QA-only undo snapshots. Final authoritative state: Regular mode, two courts, zero active QA players, zero marker players/events/undo rows, and zero King teams, fill-ins, or team substitutes. The live authenticated UI converged to two empty courts and an empty waitlist. No facility credential or login configuration changed.

## NEXT SESSION — START HERE

Run 5 remains active after the long-queue drag/drop and basic populated-state mode-dropdown audit above. Do not repeat those completed movements or the Rejoin -> Regular check unless a later UI fix touches them. The next unfinished Run 5 item is the **full realistic-state mode-transition and court-count-under-load audit**: use PHR or Ocean Air (no disposable facility) to create exact-cleanup QA state containing current/waiting players plus a group, sit-out, Rejoin, substitute/fill-in where their mode supports it; exercise remaining Regular -> Rejoin -> Teams -> Teams Rejoin/King transitions and court count increase/decrease on desktop and 390px mobile; verify visible state, scroll stability, dropdown usability, authoritative relationships/queue positions, refresh persistence, and exact cleanup. Preserve credentials and keep PHR/Ocean Air in valid operational state. Do not start Run 6.
### Run 5 realistic-state mode-transition finding (2026-09-24)

- Began the next mode-transition-under-load audit with a new exact-cleanup PHR-only `RfiveMode*` fixture: 24 current / 13 waiting players, then used the live Admin UI to create a two-player group and a sit-out-priority player. The live UI and authoritative state agreed before transition.
- Regular -> Rejoin retained the mid-page viewport at `scrollY 2679.3`, preserved the group and sit-out UI markers, and left the select usable.
- **Confirmed related UI defect:** Rejoin -> Teams changed the live mode correctly and rendered seven team cards, but unexpectedly moved the browser from its useful mid-page position (`scrollY 2679.3`) to the page bottom (`scrollY 4120.7`). There is no generic mode-change scroll API in `changeWaitlistMode()`; current source search finds scroll calls only for facility-menu/tutorial and drag preservation. This needs a second desktop/mobile reproduction and a complete root-cause trace through mode rerender, DOM identity/layout, focus, and browser scroll anchoring before a repair.
- Restored Regular mode through the live UI. Exact cleanup removed all `RfiveMode*` players, QA marker events, and five undo snapshots created during this scenario. Authoritative PHR state is again Regular / two courts / zero marker players / zero King teams; no credential or authentication configuration changed.

## NEXT SESSION — START HERE

Run 5 is active at a newly confirmed **mode-transition scroll-jump** defect. First reproduce `Rejoin -> Teams` from a realistic PHR or Ocean Air state at a deliberately recorded mid-page scroll position on desktop and 390px mobile. The first live reproduction was PHR `scrollY 2679.3 -> 4120.7` with correct mode/team data but an unintended jump to page bottom. Trace the complete first divergence: `changeWaitlistMode` RPC completion, refresh/realtime timing, React keys/layout identity, focus and browser scroll anchoring, and all `scrollTo`/`scrollBy` effects. Add regression coverage where practical; implement the smallest complete repair only if the reproduction holds; then test desktop/mobile live behavior, authoritative state, build/test/lint/deploy as required for a source change, and exact cleanup. Continue the wider Run 5 realistic-state mode/court-count audit only after closing this defect. Preserve credentials and do not start Run 6.

### Run 5 mode-transition viewport repair and publication blocker (2026-09-24)

- Recreated the realistic PHR `RfiveMode*` fixture with 24 current and 13 waiting players, a live two-player group, and a sit-out-priority player. The controlled desktop trace disproved focus and delayed realtime as causes: before `Rejoin -> Teams`, `scrollY = 1103.3`, old `scrollHeight = 3395`, old max scroll `= 2631`, viewport `= 764`, and focus was `BODY`; the queue-to-`KingBoard` replacement completed in the RPC refresh commit, then moved to `scrollY = 4120.7` with new `scrollHeight = 5101` / max `4337`, still focused on `BODY`. The mode select did not retain focus and no second state render occurred. The prior `2679.3` measurement had also been the old document maximum, but the true-middle reproduction confirmed the defect.
- Implemented the smallest deterministic repair in `61b1f8f` (`Preserve viewport through mode changes`): capture the user viewport only for an actual mode RPC, disable scroll anchoring for that one structural transition, and restore the captured position synchronously in a `useLayoutEffect` after the target mode commits. It neither uses an arbitrary timeout nor affects drag handling. `tests/waitlist-integrity.test.mjs` now protects the capture/commit/anchoring contract.
- Verification before publication: focused regression passed; full Node suite passed **100/100**; `git diff --check` passed; a clean detached worktree at `61b1f8f` completed the production build. The main checkout build initially failed only because a generated `dist/client/file.svg` was locked. ESLint scoped to the actual source reports the established baseline **11 errors / 35 warnings** (the previous documented 12-error count included the now-absent duplicate generated-tree issue); this repair added none. The standard repository lint command traverses historical staged build directories and is not a meaningful additional source result.
- `61b1f8f` is pushed to GitHub `main`. No Supabase migration or production database change is needed for this frontend-only repair.
- **Current hard blocker is publication authorization, not source or build:** the configured `sites` Git remote returns `HTTP 403: Invalid or expired token`; native Sites `get_site(appgprj_6a71a3583c688191abb6964083a256b2)` reports project not found for the current authenticated Sites account; the browser can preview the existing `OpenGym Volleyball Waitlist` at `playopengym.com`, but its `Request edits` control is disabled. Its More-actions Settings menu also does not open a writable settings surface. No Sites token, cookie, password, or other secret was printed or changed.

## NEXT SESSION — START HERE

Run 5 remains active at the **Sites publication authorization blocker** for frontend commit `61b1f8f`. Do not repeat source diagnosis, tests, clean-worktree build, or backend work. First restore edit/publish authorization for the existing Sites project `appgprj_6a71a3583c688191abb6964083a256b2` / `playopengym.com`: either authenticate the source remote with a fresh authorized Sites write credential or sign the browser into the Site owner/editor account so Request edits is enabled. Never request or print a secret in chat. Once authorized, publish exactly GitHub commit `61b1f8f`, inspect the served bundle for `modeViewportRef`, `mode-transitioning`, and the `useLayoutEffect` scroll restore, then hard-refresh the live authenticated PHR page and complete desktop plus 390px mode-transition verification (Rejoin -> Teams, Teams -> Rejoin, Regular -> Teams, Teams -> Regular, Rejoin -> Regular, repeated switching; top/middle/lower/bottom) with authoritative state, refresh persistence, console/network checks, and exact `RfiveMode*` cleanup. Preserve PHR/Ocean Air credentials and do not start Run 6.

### Run 5 Sites access follow-up (2026-09-24)

- The user restored the browser Site-settings session. It authoritatively identifies `OpenGym Volleyball Waitlist`, has custom domains `playopengym.com` and `www.playopengym.com`, and exposes editable settings, so it is the existing production Site.
- Publication is still unavailable from this Codex runtime: the preview/editor surface continues to show disabled `Request edits`; direct `Edit` produces no editable workspace; the native Sites connector still returns project-not-found for `appgprj_6a71a3583c688191abb6964083a256b2`; and `git ls-remote sites HEAD` still returns `HTTP 403: Invalid or expired token`. The active Sites account displays **0% remaining** in its five-hour Work capacity, providing a concrete reason the browser editor cannot accept source edits/publish work. No production publication occurred and the public bundle remains unverified for `61b1f8f`.

## NEXT SESSION — START HERE

Run 5 is blocked only on the **actual Sites publishing capability** for frontend commit `61b1f8f`; Site settings access alone is restored, but it does not provide source-publish access. Do not repeat source/tests/build/root-cause work. Resume when the Site owner/editor browser has available Work capacity and an enabled edit/publish control, or when an authorized source-repository write credential makes `git ls-remote sites HEAD` succeed. Then publish exactly `61b1f8f` without source changes, verify the served `playopengym.com` bundle contains `modeViewportRef`, `mode-transitioning`, and the `useLayoutEffect` restore, hard-refresh the PHR Admin page, and complete the full populated four-mode desktop/390px matrix: Regular, Rejoin, Teams, Teams Rejoin; every pairwise transition, the specified repeated sequence, top/middle/lower/bottom viewports, authoritative state, refresh persistence, console/network, and exact `RfiveMode*` cleanup. Continue Run 5 only after that checkpoint closes; do not start Run 6.

### Run 5 independent mode-conversion, court-count, and release-integrity audit (2026-09-25)

- Continued while Sites publication remains explicitly deferred. No Sites browser/editor/remote publication path was retried, and no source change was made to compensate for the external quota/authentication blocker.
- Reconstructed the retained PHR fixture state and exercised the **deployed authenticated Admin UI** before cleanup. The fixture held 37 named `RfiveMode*` / `Rfivemodezz Q.` players (24 current, 13 waiting at the initial two-court Rejoin state), including the two-member group created through the UI. No PHR or Ocean Air authentication credential, hash, login configuration, or account data was read or changed.
- Live normal-waitlist court-count stress passed through `admin_set_court_count`: 2 -> 3 filled Court 3 with 12 players; 3 -> 2 returned exactly those 12 to waiting; 2 -> 1 returned Court 2's 12 to waiting. At every authoritative check, active queue positions were contiguous and unique, current players had a valid active court, and the grouped pair stayed together (both current on Court 3 during expansion, then both waiting after reduction).
- Live populated conversion through the Admin mode dropdown passed at the authoritative state level: Rejoin -> Teams created two current six-player teams on one court and five compact waiting teams; 1 -> 2 courts promoted Team 3 and Team 4 to Court 2; 2 -> 1 moved those teams back as the first waiting teams; Teams -> Teams Rejoin retained the same 37 identities, valid team membership, court sides, and compact team queue; Teams Rejoin -> Rejoin removed all team ids/King teams while retaining all 37 identities, the two-member group, unique contiguous individual positions, and valid current/waiting court assignments. This is state/conversion proof only; the known structural viewport behavior remains pending the unpublished client fix.
- Live definition and ACL audit for the complete court/mode chain passed: browser entries `admin_set_court_count(integer)` and `set_open_gym_mode(text)` are `SECURITY DEFINER`, owned by `opengym_runtime`, have `search_path=public`, authenticate-only EXECUTE, and no anon/PUBLIC EXECUTE. Immediate helpers (`fill_open_court_slots`, `initialize_king_mode`, `king_fill_courts`, `king_compact_queue`, `save_admin_undo`, `log_waitlist_operator_action`, capture/restore) have the same owner/search path and remain private to runtime/service role. Player trigger callees use `new.facility_id` for cleanup/event mutations. Focused invariant tests passed 10/10. The linked CLI ledger still matches local through `20260924070000`; `supabase db push --linked --dry-run` is empty.
- Exact cleanup transaction deleted only the 37 named PHR marker players and five marker-containing undo snapshots. Follow-up authoritative state is `regular`, two courts, zero active players, zero marker players/events/undo rows, zero King teams, fill-ins, team substitute requests, and team substitutes. Browser realtime did not render direct database deletes, so a hard refresh was required; the refreshed public page correctly shows PHR guest/Admin entry with no stale queue rows.
- The hard refresh also expired/cleared the in-browser PHR Admin session. This prevents further authenticated live Admin, bounded-concurrency, and retry workflow checks in this browser until the existing PHR Admin credential is entered directly into its visible secure form again. The user has already authorized normal PHR login, but this runtime cannot safely place the password into its isolated browser input without exposing it in an automation transcript. This is an authentication handoff boundary, not a production failure.
- Release/cache analysis: `https://playopengym.com/` still preloads `WaitlistApp-BtAwb79f.js`; its asset does **not** contain `modeViewportRef` or `mode-transitioning`, while it does retain the previously deployed guarded Rejoin endpoint. `sw.js` has no `fetch` listener or Cache API usage, and the worker makes HTML `no-store`; stale client behavior is therefore publication state, not a service-worker cache path. The frontend fixes awaiting publication remain exactly `61b1f8f` (viewport preservation across structural mode switches).

## NEXT SESSION — START HERE

Run 5 is active. **Do not retry Sites publication** until the known external capability changes: the frontend fix awaiting publication remains exactly `61b1f8f`; Sites editor is disabled, native project access cannot resolve the Site, the `sites` remote returns expired-token HTTP 403, and the active account has 0% Work capacity. The current public `WaitlistApp-BtAwb79f.js` lacks `modeViewportRef` and `mode-transitioning`; do not claim deployed viewport proof. When publication is actually available, publish the current source containing `61b1f8f`, inspect the public bundle, then complete the full populated four-mode (Regular, Rejoin, Teams, Teams Rejoin) pairwise and repeated transition matrix on desktop and 390px mobile, including top/middle/lower/bottom viewport positions, authoritative state, refresh persistence, and console/network checks.

Independent work completed: long-queue desktop/mobile-viewport drag audit; basic mode dropdown audit; realistic populated normal/Teams/Teams Rejoin conversion and normal/Teams court-count stress; live mode/court helper definition/grant/trigger audit; migration dry-run; cache/service-worker release-order analysis; exact PHR fixture cleanup. PHR is authoritatively Regular with two courts and no active QA marker rows. PHR Admin session was cleared only by the required browser refresh. Continue any remaining independent read-only audit without needing the session. For the next authenticated live Run 5 items (bounded concurrency, retry/failure, realtime convergence, and additional browser UI actions), enter the existing PHR Admin username and password directly into the visible PHR Admin form; do not send or expose either secret in chat. Do not modify any PHR/Ocean Air credential or authentication configuration, do not recreate retired disposable facilities, and do not start Run 6.

### Run 5 PHR Admin reauthentication result (2026-09-25)

- After the user-entered credential handoff, the visible PHR Admin form was submitted from the already-open `https://playopengym.com/g/pacific-highlands-ranch` tab. The live application returned **Incorrect username or password**. No credential value was read, retained, printed, changed, reset, rotated, or committed.
- The authenticated browser session is therefore still unavailable. PHR remains in the clean authoritative state recorded above: Regular mode, two courts, no active named QA fixture rows, no King teams, and no pending team auxiliary rows. The Sites publication blocker remains deferred and was not retried.

## NEXT SESSION — START HERE

Run 5 is active but blocked at the **visible PHR Admin sign-in form**. The last submitted credential attempt returned `Incorrect username or password`; enter the correct existing PHR Admin username and password directly into the two form fields and submit, without sending either secret in chat. Once the page visibly shows `LIVE QUEUE · ADMIN`, resume the remaining authenticated Run 5 checks: bounded concurrency, rapid/repeated action and retry/failure behavior, and realtime convergence, using PHR/Ocean Air operational state only and exact cleanup. Do not modify/reset/rotate/delete credentials or authentication configuration. Separately, do not retry Sites publication until its known external capability changes; the one frontend fix awaiting publication is still `61b1f8f`, and full four-mode deployed desktop/390px verification remains pending that release. Do not start Run 6.

### Run 5 same-profile concurrency boundary and repeated-action check (2026-09-25)

- The existing PHR Admin session was verified and the baseline was preserved: `Phr A.` remained the sole pre-existing current player on Court 1; it was not modified. PHR configuration began and ended in Regular mode with two courts.
- Same-tab repeated-action check: a double activation of **Increase courts** produced exactly one effective 2 -> 3 change, exactly one new Court 3, and one `court_count` event. The server-side advisory lock/idempotent same-count path prevented duplicate court rows/events. A subsequent zero typed into the client numeric control was normalized by the UI to its minimum valid value (1), not sent as a server failure; the count was immediately restored through the Admin UI to 2. This is input normalization behavior, not a failed-RPC retry result.
- Attempted independent realtime observation with a second in-app-browser tab. That tab began guest join with the exact marker `Rfiverealtime V.`. Because both tabs share the same browser profile/auth storage, completing the guest join replaced the shared authenticated Admin state, and the original Admin tab immediately rendered as a non-Admin live queue. This establishes that two tabs in this available profile cannot represent independently authenticated Admin and guest clients. It is a test-environment/session-storage limitation; no product concurrency conclusion is claimed from it.
- Exact cleanup removed only `Rfiverealtime V.` and its one event. Authoritative follow-up: zero marker rows, PHR still Regular/two courts, `Phr A.` still `current` at queue position 1 on Court 1, and active-player count 1. No credential or authentication configuration changed. The Sites publication blocker remains deferred and untouched.

## NEXT SESSION — START HERE

Run 5 is active but the available in-app browser profile is currently **non-Admin** because the same-profile guest observer replaced its shared auth state. First open the PHR Admin form and enter the existing PHR Admin credentials directly in the visible secure fields; do not send secrets in chat. Then continue the remaining authenticated Run 5 tests: retry/failure semantics for an actual rejected safe mutation, rapid/repeated server-side actions, and realtime/concurrency convergence. The available in-app browser cannot provide an independent Admin/guest context because tabs share auth storage. Use a genuinely separate browser profile or the user’s physical independent Device B if one is required for cross-client concurrency; do not infer a product defect from this environment limitation. Preserve the pre-existing `Phr A.` player and all credentials. PHR currently is Regular mode with two courts and no named QA marker rows. Do not retry Sites publication; frontend `61b1f8f` remains the sole unpublished fix, and full deployed four-mode desktop/390px verification remains pending its release. Do not start Run 6.

### Run 5 Next Game nested-helper ownership repair and live concurrency proof (2026-09-25)

- **Confirmed live defect:** the real PHR Admin Next Game confirmation failed before mutation with `permission denied for function repair_facility_court_assignments`. The complete live chain was audited: browser `advance_court_game` -> `assert_expected_court_game` -> `end_court_game` -> `repair_facility_court_assignments` -> `fill_open_court_slots`, snapshots, events, reversal recording, and player/past-game triggers. The only broken ownership edge was `end_court_game` (owned by `opengym_runtime`) calling private `repair_facility_court_assignments` (owned by `postgres`, `opengym_runtime` EXECUTE=false) after Run 4 grant hardening. The browser endpoint remained authenticated-only; no facility predicate or user-visible RPC signature was changed.
- Added source `supabase/fix-next-game-repair-helper-ownership.sql`, byte-identical CLI migration `20260925010000_fix-next-game-repair-helper-ownership.sql`, and `tests/next-game-helper-ownership.test.mjs`. The single atomic migration transfers that private helper to `opengym_runtime`, revokes PUBLIC/anon/authenticated and service-role access, and grants only the runtime/service execution boundary. No browser access was expanded.
- Focused Next Game/grant tests passed 18/18; full Node suite passed **103/103**; `git diff --check` passed. The clean worktree production build completed. Scoped lint was run against application/test/worker source with no new report from this SQL/test-only repair; the documented project lint baseline remains unchanged. Committed/pushed as `cbf053b`.
- Linked CLI history/dry run showed exactly one pending migration (`20260925010000_fix-next-game-repair-helper-ownership.sql`) and no historical replay. The CLI applied exactly that migration. Local and remote histories now match through `20260925010000`; post-deploy dry run is empty.
- Live post-deploy audit: `repair_facility_court_assignments(uuid)` is now SECURITY DEFINER, owned by `opengym_runtime`, `search_path=public`, runtime/service executable, and authenticated/anon/PUBLIC non-executable. The surrounding browser wrapper and private helpers retain the intended boundary.
- Real deployed PHR UI verification passed: normal Court 1 Next Game advanced to Game 3 with no permission error. Two concurrent confirmed Court 1 dialogs then produced one Game 4 advancement and one safe stale rejection: `This game has already changed. Refresh and try again.` Both browser tabs converged. The authoritative database showed valid Court 1/current-player state and two reversible test actions; the real Admin undo control was used twice to restore Court 1 to Game 1.
- Exact cleanup removed the two recent QA Next Game events and two corresponding redo entries, with no residual recent past games, reversal rows, undo/redo start-game artifacts, or marker players. PHR is restored to Regular mode, two courts, `game_number=2`, Court 1 Game 1, and the pre-existing `Phr A.` remains `current`, position 1, Court 1. A public page refresh shows normal PHR entry; its separate Admin tab remains available for later nonpublication work. No credential/authentication data was modified.

## NEXT SESSION — START HERE

Run 5 has completed all currently actionable nonpublication work: long-queue drag audit; basic mode control; populated normal/Teams/Teams Rejoin conversion; normal/Teams court-count stress; same-session two-tab realtime and serialized court-count races; release/cache analysis; and the deployed Next Game nested-helper ownership repair plus live normal/stale-action/undo proof. **Do not retry Sites publication.** The sole frontend fix awaiting publication remains `61b1f8f`; Sites editor remains disabled, native access cannot resolve the project, the `sites` remote returns expired-token HTTP 403, and the active Sites account has 0% Work capacity. The public `WaitlistApp-BtAwb79f.js` still lacks `modeViewportRef` and `mode-transitioning`.

When actual Sites publication capability returns, publish the current main source containing `61b1f8f`, inspect the served asset for the fix, then complete the blocked deployed four-mode matrix on desktop and 390px mobile: Regular, Rejoin, Teams, Teams Rejoin; pairwise and repeated transitions; top/middle/lower/bottom viewports; dropdown/overlay/focus behavior; authoritative state; refresh persistence; and console/network checks. Preserve `Phr A.`, PHR/Ocean Air credentials, and all authentication configuration. Do not start Run 6.

### Run 5 interrupted-query recovery and exact history cleanup (2026-09-25)

- Reconstructed from this specification/checkpoint, main HEAD `2a768739e4bbf04241f4e948240db4400a2d2a34`, empty tracked/staged diffs, recent commits, scratch SQL, and persisted execution results. Pre-existing untracked build/Sites artifacts were preserved. The detached build worktree remains at `61b1f8f`.
- The apparently hung read-only `run5-conflicting-court-race-verify.sql` DID return successfully at 02:18:59 UTC in 4.4 seconds. Its result was Regular, three courts with games 1/2/3, Phr A. current/position 1/Court 1. Both browser tabs then returned to two courts at 02:19:08 UTC. The same-target race already completed at 02:17:49-02:18:30 UTC. Later Next Game repair, live stale rejection, undo, and cleanup also completed before commit `2a76873`. No race was repeated. The visible hang did not represent SQL still running; its exact UI/runtime cause is not established.
- Before any recovery mutation, fresh local process inventory found no supabase/psql/PostgreSQL process. Fresh `pg_stat_activity`, `pg_locks`, `pg_blocking_pids`, and prepared-transaction inspection at 04:38 UTC found no Run 5 SQL/transaction, blocked session, waiting lock, advisory lock, or prepared transaction. Normal idle clients/realtime replication remained untouched. No database session was cancelled or terminated. Only this recovery's own slow local log-reader process was stopped after replacement with a bounded file reader.
- Fresh authoritative migration ledger exactly matches all eight local migration versions through `20260925010000`; no migration was applied or changed in this recovery.
- Initial recovery state matched the operational baseline but revealed incomplete historical QA cleanup: two named marker events and six court-count undo snapshots remained. Earlier zero-marker-event reporting missed marker names embedded after `Admin` in messages. The prior interrupted recovery turn only backed up these rows; it had NOT executed cleanup.
- Backed up and removed exactly 32 documented Run 5 QA events: IDs 5408-5425, 5427-5429, 5431-5437, and 5440-5443. Removed exactly six QA court-count undo snapshots: 2696,2697,2698,2703,2704,2705. Included event 5440/5442 only because they were generated by the two recorded QA Next Game undo actions; retained the legitimate original Phr A. join event 5426. No broad timestamp deletion was used. Transaction acquired the existing action lock, used 15-second statement/3-second lock timeouts, checked the operational fingerprint before deleting, and asserted exact row counts. Local backup and SQL are in `C:/Users/timot/Documents/Codex/2026-09-24/the-previous-codex-chat-became-stuck/work/` (`run5-recovery-backup.txt`, `run5-recovery-exact-cleanup.sql`); these are private scratch artifacts, not production code.
- Post-cleanup fresh SQL: Regular; configured court_count=2; config game_number=2; actual Court 1/Game 1 and Court 2/Game 2. Active Phr A. UUID `9ccfe114-7faa-49b9-b0dd-c0e91c09eb7d` remains current, queue_position=1, court_number=1, no group/team. Older left Phr A. UUID `c9af7ef7-00a8-4179-b2dc-97476ce3bed9` also remains. Entire PHR players/config/courts fingerprint was identical before and after (`11e3fd494338ca52007bf7d347ab0891`), proving no legitimate player/operational row was removed or altered by cleanup. Zero marker players/events, zero selected QA history rows, zero undo/redo, past games/reversals, teams/fill-ins/substitute requests/substitutes. Active player count remains one.
- No recovery SQL targeted credential, username/password/hash, login-configuration, or authentication tables. No credential values were read or changed. This is an action-scope guarantee, not a claim of a historical credential byte comparison (no pre-interruption credential snapshot was available). Browser reload ended the visible Admin view; normal Continue-as-guest navigation was checked without entering a name or joining, then returned to the entry screen. No authentication configuration was changed.
- Live browser: before refresh, authenticated PHR rendered Regular/two courts, games 1/2, Phr A. on Court 1, empty waitlist. After exact cleanup, an actual reload completed at the normal Pacific Highlands Ranch guest/Admin entry; Continue as guest opened the expected blank join form, and Go back returned normally. No join or other player mutation was submitted. Captured console warnings/errors were empty. HTML and loaded-client URL both returned HTTP 200. Full browser network request capture was not exposed by this browser tool, so no full network trace is claimed. No UI source change was made and the pending deployed desktop/mobile viewport matrix was not repeated or claimed complete.
- Fresh public-client check: `/assets/WaitlistApp-BtAwb79f.js` still contains guarded Rejoin but lacks `modeViewportRef` and `mode-transitioning`. Frontend fix `61b1f8f` remains unpublished. The known Sites capability blocker was not retried. No new regression test/build was needed for exact QA-data cleanup plus documentation; prior 103/103 suite and build remain historical results, not newly rerun results.

## NEXT SESSION — START HERE

Run 5 recovery and exact QA cleanup are finished; do NOT rerun the same-target/conflicting court-count races or the completed Next Game repair/stale-rejection/undo tests. First genuinely unfinished action remains publication of the existing frontend viewport fix `61b1f8f`, ONLY when the previously blocked Sites edit/publish capability actually returns. Do not retry the known blocked publication paths without evidence of that change. Public client is still `WaitlistApp-BtAwb79f.js` without the viewport fix. When access returns, publish current main containing `61b1f8f` and `cbf053b`; prove the served asset contains the viewport fix, then execute the full populated four-mode pairwise/repeated transition matrix on deployed desktop and 390px mobile, at top/middle/lower/bottom, with dropdown/overlay/focus, authoritative identities/state, refresh persistence, console/network checks, and exact QA cleanup. Physical touch remains unverified by the prior mouse-only mobile-viewport drag test. Current PHR baseline: Regular, two courts (1/Game 1 and 2/Game 2), config game 2, original active Phr A. UUID `9ccfe114-7faa-49b9-b0dd-c0e91c09eb7d` current/position 1/Court 1; preserve both existing Phr A. rows and all other legitimate data. Zero Run 5 marker players/events or undo/redo test state. Browser is at normal PHR entry and may require authorized existing Admin sign-in for future UI tests; never reset credentials. Recovery SQL should continue using bounded fresh processes (45-second client, 15-second statement, 3-second lock timeout). Do not start Run 6.

### Run 5 viewport fix published; authenticated matrix pending (2026-09-25)

- Resumed from `31772a6` without repeating race recovery, PHR cleanup, or authoritative-state checks. Native Sites access now recognizes the existing project as owner/public and issued an authorized temporary source credential. No credential was written to source or disk. Public audience and facility authentication configuration were preserved.
- The Sites remote at `011eed4e8c2228940ce40b60e97ebd355796d80b` had divergent publication history. Inspected differences; its application source lacked only the intended viewport repair, while remaining source differences were already-reviewed GitHub QA/security/test/package history. Created isolated publication worktree `C:/Users/timot/Documents/Codex/2026-09-24/the-previous-codex-chat-became-stuck/work/run5-publish` and reconciliation commit `1b171f6032d58ffaa7322602a5567698f7184ba0` with BOTH histories and an EXACT tree match to `31772a6` (`git diff --exit-code 31772a6 HEAD` passed). Main fast-forwarded to that commit after publication. No unrelated source change was made.
- Frozen dependency install and Windows-equivalent Vinext production build passed. Sites packaging required the installed Git Bash on process PATH and `TAR_OPTIONS=--force-local` for Windows drive paths; corrected process environment only, reused the successful build. Official Sites workflow pushed the exact commit, packaged and validated `work/run5-publish.tar.gz`. Archive contains runtime output and hosting metadata, with no tests/work/Run5 diagnostic source or SQL files. Focused integrity/mode tests passed 4/4; this is not a substitute for UI verification.
- Saved Sites version **481**, version id `appgprj_6a71a3583c688191abb6964083a256b2~appgver_eb0d665d8150819183f3e804ebc45a4c`; deployment `appgdep_6ab6003de6c481918e5b9f733a10570a` succeeded at 05:02 UTC. Project remains `appgprj_6a71a3583c688191abb6964083a256b2`, public custom domain `https://playopengym.com`. No Supabase migration was applied.
- Actual custom-domain HTML and client asset return HTTP 200. New served client `/assets/WaitlistApp-BcsOr46m.js` is byte-for-byte identical to the clean publication build. It contains `mode-transitioning`, the `useLayoutEffect` restoration using `Math.min(pending.scrollY,maxScroll)` and instant scroll, and the mode-change capture/error cleanup. The variable name `modeViewportRef` is minified to `sn`, so absence of that literal is NOT missing-code evidence. This proves deployment of the existing `61b1f8f` fix.
- Hard-refresh shortcut followed by explicit browser reload reached normal PHR entry. Browser DOM identifies the new `WaitlistApp-BcsOr46m.js`; captured warning/error logs are empty. Opened the Admin login form. No credentials were read, entered, reset, or changed; no QA fixture or gameplay mutation was performed this turn. Full network request trace is not exposed by this browser tool.
- **Current blocker:** the browser is signed out and no existing authorized Admin credential is available to enter automatically. Requested the user enter existing credentials directly in the visible PHR form and submit, without sending secrets in chat. The populated matrix has NOT started. Publication is complete; viewport fix is NOT yet behaviorally verified. All 12 directed pairwise mode transitions, repeated sequences, viewport positions, player/team/queue identity checks, and refresh persistence remain pending on desktop and 390px mobile.

## NEXT SESSION — START HERE

Do NOT republish or repeat completed race/recovery/cleanup work. Sites version 481 is live with `/assets/WaitlistApp-BcsOr46m.js` and the intended `61b1f8f` viewport fix. FIRST unfinished action: obtain the already-authorized existing PHR Admin browser login by having the user enter credentials directly in the visible form, then confirm `LIVE QUEUE · ADMIN` without printing secrets. If already signed in, continue immediately. Before creating new test state, take a fresh fixture-specific baseline/identity backup (not a rerun of recovery) and preserve both legitimate Phr A. records and all other non-QA data. Create exact-ID-cleanup populated QA players with current and waiting rows, then test deployed Regular, Rejoin, Teams, Teams Rejoin: all 12 directed transitions and repeated switching, especially populated Rejoin -> Teams, on desktop and 390px mobile at top/middle/lower/bottom. For EVERY transition check dropdown click/reopen, mode label, overlays/focus, useful nearby viewport/no unintended jump, exact player identities/no duplicates, correct queue/team order, authoritative agreement, and refresh persistence. Do not claim physical touch if only mouse/mobile viewport is available. Preserve all usernames/passwords/hashes/login configuration. Restore only exact QA state and verify cleanup, then inspect remaining Run 5 readiness items. Use bounded SQL subprocesses. Do not start Run 6.

### Run 5 signed-in handoff mismatch (2026-09-25)

- Resumed from `86bc5e5` following the user's signed-in message. No publication, race, recovery, cleanup, or authoritative-state verification was repeated.
- The available browser inventory contains one PHR tab: provider tab `437b5828-025d-4277-bf77-1e447e5c8558` (automation browser 1/tab 1). Its fresh accessibility state still shows the blank `Manage OpenGym` Admin form, not `LIVE QUEUE · ADMIN`. A read-only boolean check confirmed admin=false and zero filled inputs without reading credential values. No other authenticated PHR tab is available through the tool. This does not establish whether the user signed in in a separate browser/window.
- Asked the user to identify the authenticated window or sign in in this task's PHR tab. Requested that exact tab be shown using the app tool; the app returned queued. Prepared the full 12-directed-transition cycle in local scratch `work/run5-mode-matrix-plan.json`, including desktop/390px and top/middle/lower/bottom positions. This is planning only, not test evidence.
- No QA fixture was created, no facility state or credential changed, and no transition was executed. Sites version 481 publication remains complete; all requested deployed mode-switch behavioral checks remain pending.

## NEXT SESSION — START HERE

Resolve the browser-session mismatch first: user reported signed in, but the only tool-accessible PHR tab (`437b5828-025d-4277-bf77-1e447e5c8558`) showed an empty Admin login form. Locate the actual authenticated tab if newly available; otherwise user must sign in directly in this task's PHR tab. Never request credentials in chat or reset them. Once `LIVE QUEUE · ADMIN` is visibly confirmed, take the fixture-specific baseline and execute the entire desktop/390px four-mode matrix described in the preceding checkpoint and local `work/run5-mode-matrix-plan.json`. All 12 directed transitions, repeated switching, viewport positions, dropdown/focus/overlay behavior, exact player/queue/team identities, authoritative state, and refresh persistence remain unfinished. Do not republish version 481 or repeat completed recovery/races. Do not start Run 6.

### Run 5 browser routing mismatch after second signed-in handoff (2026-09-25)

- Resumed from `d0ddf11`. User reports authenticated PHR and app ambient context reports TWO tabs with PHR current. Browser automation independently reports FOUR tabs (PHR, Sites, Supabase, GitHub); its PHR tab `437b5828-025d-4277-bf77-1e447e5c8558` shows the guest/Admin entry screen. Reset the automation JavaScript session, rediscovered surfaces, and rebound PHR: same four-tab inventory and signed-out view. No page reload, logout, login submission, or facility mutation was performed.
- Read the browser troubleshooting guidance. The available tool is not reporting disconnection, and rebinding does not expose the user's authenticated tab. Treat this as unresolved browser routing/session mismatch, NOT proof the user failed to log in and NOT an OpenGym defect. Requested an explicit attached/@-mentioned authenticated browser tab so `cua.getTab({mention:<full tab reference>})` can bind the exact user-selected target. Do not keep asking the user to sign in repeatedly.
- No matrix action, new database query, publication verification, or fixture creation was performed. All previously confirmed source/deployment/data facts remain at their prior checkpoints; no new behavioral pass is claimed.

## NEXT SESSION — START HERE

FIRST obtain the user's explicit authenticated PHR browser tab reference (attachment/@-mention). Bind its complete `plugin://...` reference through `cua.getTab({mention:...})`; do not reuse the mismatched four-tab inventory as evidence of the user's visible session. If binding fails, report the concrete tool error rather than requesting repeated sign-ins. Once the exact tab visibly shows LIVE QUEUE · ADMIN, execute the complete fixture-safe desktop/390px four-mode matrix already specified above and in `work/run5-mode-matrix-plan.json`, including all 12 transitions, repeated switching, populated Rejoin -> Teams, viewport positions, dropdown/overlay/focus checks, exact UI/database player/team/queue relationships, and refresh persistence. No tests in that matrix have started. Version 481 is already published; do not redo publication or completed recovery/races. Preserve all legitimate data and facility credentials. Do not start Run 6.

### Run 5 built-in browser connected; deployed viewport regression reproduced (2026-09-25)

- Resolved the routing mismatch using the selected tab directly from the built-in browser (`browser.tabs.selected()`), which visibly showed Pacific Highlands Ranch / LIVE QUEUE · ADMIN. No new login, credential change, publication replay, or recovery replay was needed.
- Took a private fixture baseline in task scratch `work/run5-matrix-baseline.json`: all 819 existing player rows retained, sole active Phr A. current/position 1/Court 1, Regular/two courts games 1 and 2, zero teams/undo/redo. Inserted exactly 47 identified `Rfiveviewport* Q.` rows, UUIDs in `work/run5-matrix-fixture.json`, to produce 48 active players (24 current, 24 waiting). Those QA rows currently remain; they must be cleaned by exact UUID only after verification. Credentials/auth tables were never queried or mutated.
- Sites v481 desktop populated Rejoin -> Teams FAILS: stable scrollY 2376 becomes 0, 48 identities preserved. Reverse Teams -> Rejoin also FAILS: stable scrollY 3164.67 becomes 0. Observed trace shows the scroll reset immediately when the request becomes busy, while mode and full 48-row layout are still unchanged. Thus this is not only browser anchoring after the structural render.
- Root cause: Teams drag effect depends on `busy`; its cleanup unconditionally calls `setMobileAdminDragging(false)`, which scrolls to stale `activeMobileDragScrollY` even when no drag lock is active. It also runs when Teams unmounts. Added executable regression tests against the actual TypeScript drag-lock implementation; both failed before the guard and pass after it. Small source repair guards unlock/restoration with the prior active-drag class. Full Node suite passes 105/105. No refresh batching change was made; the earlier intermediate-layout hypothesis remains unproven and is not patched.
- Current authoritative fixture state: Rejoin, two courts games 1/2, 48 active players, zero teams. Matrix is NOT passed. Desktop/mobile, dropdown/focus/overlay, authoritative per-transition state, reload persistence and exact cleanup remain pending after publishing the repair. No migration is needed.

## NEXT SESSION — START HERE

First publish the newly added active-drag cleanup guard in app/WaitlistApp.tsx with tests/drag-scroll-cleanup.test.mjs, using the existing clean publication worktree and authorized Sites workflow. v481 remains live until that succeeds. Do not repeat publication of old 61b1f8f or completed recovery/races. Reuse `browser.tabs.selected()` from the built-in browser; wrapper tab inventory was stale. The actual PHR page is authenticated. Retained test fixture: 47 exact QA UUIDs in task scratch work/run5-matrix-fixture.json, baseline all 819 legitimate players in work/run5-matrix-baseline.json. Current mode Rejoin with 48 active players. After publishing, verify the new public client, reload the same tab, rerun the failed populated structural case and complete the 12 directed transitions/repeated sequences at desktop1280x900/mobile390x844 and top/middle/lower/bottom with UI/database identity/order and persistence checks. Restore only exact fixture state, preserve all legitimate player rows and credentials, then inspect genuinely remaining Run 5 readiness tasks. Do not start Run 6.

### Run 5 active-drag cleanup publication recheck (2026-09-25)

- Reconstructed the actual source state rather than restarting investigation. The committed publication worktree is clean at `d045441` (`Preserve viewport when idle drag effects clean up`). Its only runtime change records `wasActive` before the drag-lock classes are toggled, and restores `activeMobileDragScrollY` only for a true active-to-inactive cleanup. The new executable `tests/drag-scroll-cleanup.test.mjs` exercises both required cases: idle cleanup leaves a populated viewport unchanged; a real active drag restores its pickup position once and later idle cleanup does not move the viewport.
- Fresh verification in that exact checkout: focused drag-cleanup test 2/2 passed; full Node suite 105/105 passed; production Vinext build passed; `git diff --check` passed; and the checkout has no staged, unstaged, or untracked changes. Full ESLint currently reports the established unrelated project baseline (12 errors, 35 warnings); the small `wasActive` change and new Node test add no report. No migration is required.
- GitHub `main` remains at `20a594c`; `d045441` is not yet published to either GitHub main or Sites. This is not a substitute for deployment. The supplied Sites project id `appgprj_6a71a3583c688191abb6964083a256b2` now returns native `SitesConnectorError: project not found`; therefore the authorized Sites workflow cannot obtain a source credential, push the exact commit, create an archive-backed version, or deploy it. No fallback publication or access/audience change was attempted. v481 remains the live client and still contains the reproduced defect.
- Existing PHR state and the retained exact UUID fixture were not queried, changed, or cleaned in this recheck; no credential, authentication configuration, or facility data was modified. Behavioral desktop/mobile verification remains genuinely pending deployment.

## NEXT SESSION — START HERE

Run 5 is blocked only on restored access to the existing Sites project. Do not redo the completed root-cause investigation, recovery/races, old-v481 publication, source tests, build, or fixture creation. Once the Sites connector can resolve project `appgprj_6a71a3583c688191abb6964083a256b2` and issue an authorized source credential, publish the clean commit `d045441439aa3c231997a9c1dd91fc2140468af0` from `C:/Users/timot/Documents/Codex/2026-09-24/the-previous-codex-chat-became-stuck/work/run5-publish`, preserve the existing public audience, and prove the served asset contains the active-drag `wasActive` guard. Then bind the selected built-in PHR Admin tab, retain the 47 exact `Rfiveviewport*` UUIDs in `work/run5-matrix-fixture.json`, rerun the failed populated Rejoin -> Teams and Teams -> Rejoin cases, and complete all 12 directed four-mode transitions plus the repeated sequence at desktop 1280x900 and mobile 390x844/top-middle-lower-bottom. For every transition verify useful stable viewport from request busy state through conversion, dropdown reopen/label/overlay/focus, exact UI/database identities and queue/team ordering, refresh persistence, console/network state, then remove only the recorded fixture UUIDs and verify exact cleanup. Do not start Run 6 or alter PHR/Ocean Air credentials/authentication configuration.

### Run 5 Sites access identity diagnosis (2026-09-25)

- Verified the GitHub publication record after the preceding checkpoint: `main` is now `41936b561c51f246cddc0518151c650a40974fd3`, which includes the active-drag repair `d045441`. The local publication worktree remains clean. The configured Sites Git remote still targets the expected project id but responds HTTP 403 `Invalid or expired token`; no token was read, changed, or replaced.
- The native Sites connector is not connected to the account that owns OpenGym: `list_sites` returned zero items and `get_site(appgprj_6a71a3583c688191abb6964083a256b2)` returned `project_not_found`. In contrast, the already-open ChatGPT Sites page visibly lists exactly one Site, **OpenGym Volleyball Waitlist**, and opening it proves its internal preview URL contains the exact same project id `appgprj_6a71a3583c688191abb6964083a256b2`, with the production custom domain `playopengym.com`. The preview also serves the expected OpenGym PHR route. The Site has not been deleted, replaced, or renamed; the native connector/browser identity split is the publication blocker.
- Read-only public PHR inspection confirms the held fixture is still live and authenticated Admin: Rejoin waitlist; 2 courts; Game 1/2; the preserved `Phr A.` plus exactly 47 `Rfiveviewport*` rows (24 current, 24 waiting total including Phr A.). No player, mode, credential, authentication setting, fixture UUID, or queue order was changed in this diagnosis.
- **Exact human handoff:** reconnect/authorize the **Sites connector for this Codex task** under the same ChatGPT account that currently owns the visible OpenGym Volleyball Waitlist Site (the account shown in the Sites browser profile). Do not create a new Site, alter the existing Site's sharing/audience/domain, or provide a credential in chat. After native `list_sites` contains this Site or `get_site` resolves its existing id, resume the official source-credential/publish workflow. Browser ownership alone cannot supply a native source-write credential.

## NEXT SESSION — START HERE

First restore the native Sites connector's authenticated account in this Codex task so it can see the existing OpenGym Site id `appgprj_6a71a3583c688191abb6964083a256b2`; browser access is already owner-visible but the connector lists zero sites. Stop at the secure account-connect approval if the UI requires user interaction. Once native `get_site` succeeds, use the existing clean worktree `C:/Users/timot/Documents/Codex/2026-09-24/the-previous-codex-chat-became-stuck/work/run5-publish` to publish exact GitHub-main commit `d045441439aa3c231997a9c1dd91fc2140468af0` through the official Sites workflow, preserving the public `playopengym.com` audience/domain. Then prove the public asset has the `wasActive` drag-cleanup guard, hard-refresh the already authenticated PHR tab, rerun the deep populated Rejoin -> Teams / Teams -> Rejoin failure case, then the full four-mode desktop/mobile matrix and legitimate drag checks. Retain and finally remove only the 47 UUIDs in `work/run5-matrix-fixture.json`; preserve `Phr A.` and all credentials/auth configuration. Do not start Run 6.

### Run 5 browser-Sites access confirmation (2026-09-25)

- User-directed inspection of the open Sites settings page confirms the browser session has owner-level control of the existing **OpenGym Volleyball Waitlist** project: the title is unchanged, the public audience is `Everyone`, and the existing `playopengym.com` and `www.playopengym.com` custom domains remain attached. No settings, environment values, sharing, domains, or deletion controls were touched.
- Retried native `get_site` after this browser proof. It still returns `SitesConnectorError: project not found`. Thus the issue is specifically that this task's native Sites connector remains bound to a different/no-project account session; it is not a browser authorization, project ownership, project identity, domain, capacity, or temporary preview issue. Browser owner access cannot issue the native source-write credential required by the official exact-source publication workflow.

## NEXT SESSION — START HERE

The browser already has owner access to the correct existing OpenGym Site, but the native Sites connector still cannot resolve it. First complete the native connector account reauthorization in Codex (same account as the browser Sites page) and stop at any user-only secure approval. Verify success with `get_site(appgprj_6a71a3583c688191abb6964083a256b2)` before publishing. Then use the clean worktree to publish exact commit `d045441439aa3c231997a9c1dd91fc2140468af0`, preserving all Site settings, public domains, PHR fixture UUIDs, and credentials. Only after the deployed bundle proves the active-drag guard is present may the full desktop/mobile viewport and drag regression matrix proceed. Do not start Run 6.

### Run 5 active-drag repair publication (2026-09-25)

- Native Sites connector reconnection succeeded and was verified before any write: it lists exactly the existing **OpenGym Volleyball Waitlist** project `appgprj_6a71a3583c688191abb6964083a256b2`, with owner role, public access, live URL `https://playopengym.com`, and previous version 481. This matches the existing Site/browser settings exactly. No new Site was created and no domain, sharing, project-setting, ownership, environment, or credential value was changed.
- Used the official temporary source-credential workflow in the clean publication checkout. Initial packaging failure was traced to missing Git Bash on the process PATH (the package wrapper requires `bash`); retrying with existing Git Bash and Windows tar compatibility environment succeeded. No source change or rebuild was introduced. The workflow pushed exact clean source commit `24cfca3282bbf4abedc82b922ad9151d007eb6da`, which contains runtime repair `d045441439aa3c231997a9c1dd91fc2140468af0`, and packaged a validated 41-file runtime archive.
- Saved and deployed Sites **version 482**, id `appgprj_6a71a3583c688191abb6964083a256b2~appgver_173d686769188191af4148f764473206`; publish deployment `appgdep_6ab6bbc681a081919a96b1155ada57e0` succeeded. The public server now returns `WaitlistApp-B8H4vUQ7.js`; its SHA-256 (`DC8CE28A48C17C5B1FA79E312CF20329ED94BF18E02521B6D348503C58C1810E`) is byte-identical to the locally built d045441 bundle and it contains the `mobile-admin-dragging` drag-lock implementation. This proves the repaired public asset is live, not merely source-published.
- Actual hard refresh of the deployed PHR browser route completed, but it cleared the local Admin session and returned to **Choose your facility**. No mode/queue/player/fixture/credential/authentication data was changed by this publish or reload. The exact 47 UUID `Rfiveviewport*` fixture remains recorded in `work/run5-matrix-fixture.json`; previous verified state was Rejoin, 2 courts, Phr A. preserved, 47 QA rows.

## NEXT SESSION — START HERE

Version 482 with the active-drag guard is live and public-bundle verified. First secure handoff: in the currently refreshed PHR browser route, select Pacific Highlands Ranch and enter the existing Admin credentials directly into the visible UI; do not send credentials in chat or modify/reset them. Once `LIVE QUEUE · ADMIN` is visibly present, confirm the exact 47 `Rfiveviewport*` fixture identities/order from `work/run5-matrix-fixture.json`, then execute the entire deployed matrix: deep populated Rejoin -> Teams and Teams -> Rejoin with scrollY before/busy/after; every directed pair across Regular/Rejoin/Teams/Teams Rejoin plus the requested repeated sequence at desktop 1280x900 and mobile 390x844/top-middle-lower-bottom; dropdown reopen/label/backdrop/focus; no duplicate/missing players; authoritative state and refresh persistence; then representative legitimate down/up/deep drag desktop and mobile-viewport (only claim physical touch if available). Remove only the exact fixture UUIDs and verify cleanup. Do not repeat source/build/publication work or start Run 6.

### Run 5 deployed viewport matrix — partial live evidence (2026-09-25)

- User completed secure PHR Admin sign-in after the version 482 hard refresh. Live page again showed `LIVE QUEUE · ADMIN`, Rejoin mode, 2 courts/Game 1/2, preserved `Phr A.`, and the 47 recorded `Rfiveviewport*` fixture rows. No credential or configuration was read/changed.
- **Primary regression case passed on deployed desktop:** at deep populated Rejoin -> Teams, `scrollY` was **2292** immediately before mode request, **2292** during the busy state, and **2422.67** after team conversion. All 47 fixture identities remained; no modal backdrop, no focus trap (BODY focus), and no top/bottom restoration occurred. Teams -> Regular also held at **2422.67 -> 2422.67** with all 47 identities intact.
- Desktop directed transitions were exercised across top/middle/lower/bottom with the live mode selector and every settled conversion kept the selector functional, body focus, no modal overlay, and fixture identities. An initial 700ms automation wait was too short for one Teams Rejoin server conversion, so it was not counted. Explicit recheck from deep position passed after a 2.5s real settlement: Teams Rejoin completed with all **47** names, **9** teams, no overlay/focus trap, and useful scroll **3313.33 -> 2426.67**.
- Switched the same deployed client to the required **390×844** viewport. The mobile layout visibly retained the Admin controls and mode selector with no horizontal control loss in Teams Rejoin. A long all-transitions mobile batch exceeded the browser-control tool timeout and caused its automation runtime to reset. Immediately after reset, browser inventory returned `User unavailable`; this is an automation-session interruption, not an OpenGym behavior finding. No partial mobile batch outcome is claimed, no fixture cleanup occurred, and the current live mode/fixture position after the interrupted batch is unknown.

## NEXT SESSION — START HERE

Do not repeat Sites publication, source tests/builds, or the successfully measured desktop Rejoin -> Teams primary regression. The deployment is version 482 with `WaitlistApp-B8H4vUQ7.js`, exact public repair bundle. First restore/rebind the browser-control session; if PHR remains authenticated, inspect current Admin mode and all 47 exact `Rfiveviewport*` UUIDs before any mutation. If re-authentication is needed, have the user enter existing credentials directly in the visible PHR form only. Then complete (not batch-timeout) the 390×844 mobile directed all-12-transition matrix in short, verified groups at top/middle/lower/bottom, including selector reopen/label, overlay/focus, scroll stability, names/order, and refresh persistence. Afterwards finish the requested desktop repeated sequence and legitimate down/up/deep drag checks; use authoritative DB queries to compare identities/order; then remove only UUIDs in `work/run5-matrix-fixture.json` and verify exact cleanup. Do not claim physical touch from mouse/mobile viewport testing. Preserve Phr A., facility credentials/authentication config, and do not start Run 6.

### Run 5 mobile browser-control reconnect attempt (2026-09-25)

- Resumed exactly at the first unfinished mobile step. Fresh browser-control inventory and one clean rebind retry both returned `Browsers: Error: User unavailable`; no browser/tab object is available to inspect, resize, or operate. This is the same automation-environment unavailability recorded after the interrupted long mobile batch, not a tested product result.
- No PHR/Ocean Air state, mode, player, queue, fixture UUID, credential, authentication setting, database row, or Site configuration was read or changed. No mobile transition, touch-equivalent move, refresh, or authoritative comparison is claimed from this attempt.

## NEXT SESSION — START HERE

FIRST restore the browser-control user/session availability itself (the current tool returns `User unavailable` before listing any browser). Once a browser surface is present, bind the PHR tab that the user already has open, confirm Admin and current fixture state without mutation, set 390×844, and run the mobile matrix in short individually verified groups: Rejoin↔Teams, Rejoin↔Teams Rejoin, Teams↔Teams Rejoin, Regular↔Teams, with before/busy/after scroll readings for deep Rejoin→Teams and top/middle/lower/bottom coverage. Verify selector reopen/label, no overlay/focus trap/horizontal overflow/clipped controls, all 47 fixture identities/team/queue state, refresh persistence, and authoritative state; then representative mobile-viewport drag/move only to the level actually supported. Do not redo the verified deployed desktop result, source work, or publication. Preserve Phr A./credentials; exact cleanup of fixture UUIDs remains required after the full matrix. Do not start Run 6.

### Run 5 mobile bridge retry (2026-09-25)

- Per the continuation instruction, made exactly one fresh browser-control inventory attempt. It again returned `Browsers: Error: User unavailable` before exposing any browser/tab inventory. No retry loop was run.
- No PHR/Ocean Air state, mode, fixture player, queue/team ordering, database state, credential, authentication setting, or Site configuration was read or changed. The 390×844 mobile matrix remains entirely pending.

## NEXT SESSION — START HERE

The only immediate blocker is restoring the browser-control bridge for this Codex task; it must return an actual browser inventory rather than `User unavailable`. Once restored, reuse/bind the existing PHR tab (do not create a new session if it survives), confirm Admin + retained fixture before mutation, set 390×844, and complete short verified mobile transition groups and the touch-equivalent drag/move. Do not reattempt inventory in a loop while unavailable. Keep exact fixture cleanup, authoritative comparisons, and Run 6 prohibition in scope.

### Run 5 mobile bridge availability check (2026-09-25)

- Reconstructed Run 5 from checkpoint `97db2f7`, the current detached `github/main` HEAD, clean Git status, and recent commits. The deployment remains version 482 and the desktop primary regression remains previously verified; neither was repeated.
- Per the continuation instruction, made exactly one fresh browser/tab inventory call. It timed out after 30 seconds and reset the browser-control kernel before returning any browser or tab inventory. This was not a product interaction and did not expose the earlier `User unavailable` text, but it also did not establish availability.
- No retry was attempted. No PHR/Ocean Air state, fixture UUID, player/order/team data, credential, authentication setting, database row, Site setting, or deployment was read or changed. The 390×844 mobile matrix remains unstarted in this resumed attempt.

## NEXT SESSION — START HERE

Run 5 remains blocked before browser inventory: the one availability-only attempt after checkpoint `97db2f7` timed out and reset the browser-control kernel without returning any tabs. Do not run a retry loop. Once the browser-control bridge can return a real inventory, bind the existing PHR tab, confirm Admin plus retained exact 47 `Rfiveviewport*` fixture rows without mutation, set 390×844, and complete the short individually verified mobile groups (Rejoin↔Teams, Rejoin↔Teams Rejoin, Teams↔Teams Rejoin, Regular↔Teams) with deep before/busy/after scroll readings, top/middle/lower/bottom coverage, selector/overlay/focus/horizontal-control checks, exact UI/database identity and queue/team ordering, refresh persistence, and supported mobile-viewport drag/move. Then complete remaining desktop repeated/drag checks and exact-UUID cleanup. Do not redo publication, source tests, desktop primary regression, or Run 6; preserve Phr A. and all credentials/authentication configuration.

### Run 5 browser bridge restored; mobile bottom regression reproduced (2026-09-25)

- A later single availability attempt returned a real inventory. Bound the existing in-app PHR tab (not a new session): it was authenticated as `LIVE QUEUE · ADMIN`, retained Phr A. plus all 47 exact `Rfiveviewport*` fixture names, and was in Teams Rejoin. The temporary **390×844** viewport rendered all Admin controls and the mode selector without horizontal overflow or clipped controls.
- Mobile transition checks at top/middle positions retained all 47 fixture identities, BODY focus, no dialog/backdrop, no horizontal overflow, and a functional selector: Teams Rejoin->Teams; Regular->Rejoin; Rejoin->Teams Rejoin; Teams Rejoin->Rejoin; Teams->Rejoin; Rejoin->Regular; Regular->Teams; Teams->Teams Rejoin; Teams Rejoin->Regular; and Teams->Regular. The deep/bottom Rejoin->Teams repair case retained `scrollY=4455.33` through the busy state but settled at `2763.33` despite the Teams page having a maximum `5923`; it is another manifestation of the confirmed upward-jump failure, not a pass.
- **Confirmed deployed mobile failure:** at the bottom of the populated Rejoin page (`scrollY=4455.33`, its maximum), Rejoin->Teams Rejoin settled at `scrollY=2763.33` even though the target Teams Rejoin page has a maximum of `5923`. The same sequence reproduced identically after returning Teams Rejoin to its bottom (`5922.67`) and converting Teams Rejoin->Rejoin (expected clamp to `4455.33`) then Rejoin->Teams Rejoin (`2763.33`). This is an unintended upward jump, not a max-scroll clamp. The UI remained usable, with no console warnings/errors, no overlay, BODY focus, no horizontal overflow, and all 47 fixtures present.
- The temporary viewport override was reset and the authenticated PHR tab marked for handoff. No fixture, facility, credential, authentication configuration, Site setting, or database row was changed outside the requested mode conversions. Exact fixture cleanup was deliberately deferred because the regression requires repair/reverification first.

## NEXT SESSION — START HERE

Run 5 is no longer blocked on browser availability; it is blocked by a **confirmed version-482 deployed 390×844 viewport defect**. Reproduce only the isolated failure once if needed for diagnosis: populated Rejoin at its bottom (`4455.33`) -> Teams Rejoin, where the target has max `5923` but settles at `2763.33`; the companion Teams Rejoin bottom -> Rejoin clamp is normal. Diagnose first divergence and implement the smallest repair with regression coverage, then run focused/full tests, publish through the verified existing Sites workflow, prove the served asset, and redo deployed desktop and mobile verification before calling it fixed. Preserve the authenticated PHR handoff/session if it survives, Phr A., all 47 UUIDs in `work/run5-matrix-fixture.json`, and credentials/authentication configuration. Finish remaining mobile matrix/repeated/drag checks, authoritative comparison, refresh persistence, and exact-ID cleanup only after the repair is live. Do not start Run 6.

### Run 5 mobile post-layout viewport repair published; live verification pending (2026-09-25)

- Proved the first divergence on deployed v482 using 390×844 frame samples from the authenticated PHR fixture. Rejoin began at bottom `scrollY=4455.33`, root/body heights `5299/5300`, max `4455`, visual viewport `844/offsetTop=0`, BODY focused. Busy began with every value unchanged. The first Teams mount later had root/body heights `3235/3235`, max `2391`, and `scrollY=2390.67`; this is the one-shot restoration clamp. The fully populated layout then grew to root/body `6767/6767`, max `5923`, but stayed `2763.33` because the pending ref had been cleared. Focus remained BODY; no explicit focus scroll, overlay, console error, or visual-viewport coordinate change occurred. This confirms intermediate-layout clamping, not scroll anchoring or a permanent short target document.
- Implemented `931bced9f9ea3c5244b4d368e2923c5102f89519` (`Restore viewport after mode layout settles`). The saved transition now records its starting refresh-layout revision. The restoration effect clamps provisionally but remains pending until the completed refresh publishes team/auxiliary layout; it then reapplies the saved coordinate against the final scroll range. A genuinely shorter final document still clamps deterministically. No timeout, requestAnimationFrame loop, or drag-lock behavior was added.
- Added `tests/mode-viewport-settlement.test.mjs`, covering measured deep Rejoin->Teams and Rejoin->Teams Rejoin intermediate-to-expanded layouts, a genuinely short final layout, near-top behavior, and desktop position preservation. Focused suite passed 8/8; full Node suite passed **110/110**; production Vinext build passed; `git diff --check` passed. Existing active-drag cleanup tests passed unchanged. Commit was pushed to GitHub and the configured Sites source remote.
- Sites version **483**, id `appgprj_6a71a3583c688191abb6964083a256b2~appgver_89c46dd019b081918ba1da341c351448`, deployed successfully as `appgdep_6ab6dac400608191bd9dc90c79cb0443`. Custom-domain HTML returns 200 and serves `WaitlistApp-B78iwZJO.js`; its SHA-256 exactly matches the local production bundle and it contains the layout-revision settlement code. The required hard refresh cleared the in-app Admin session and returned it to the visible PHR **Manage OpenGym** username/password form. No credential was read, entered, changed, reset, or recorded; no fixture was removed.

## NEXT SESSION — START HERE

First have the user enter the existing PHR Admin credentials directly into the already open visible Manage OpenGym form and submit; do not request or transmit either secret in chat. Once `LIVE QUEUE · ADMIN` is visible, verify the retained 47 exact fixture UUIDs from `work/run5-matrix-fixture.json`, set 390×844, and rerun the fixed deep/bottom Rejoin->Teams and Rejoin->Teams Rejoin cases with before/busy/first-mount/final scrollY and height readings. The expected repaired result is restoration near saved `4455.33` once the final max is `5923`, not settlement at `2763.33`. Then complete the entire remaining deployed mobile four-mode transition matrix, repeated sequence, supported mobile drag/move, selector/overlay/focus/overflow checks, refresh persistence, authoritative comparison, desktop remaining checks, and exact-UUID cleanup. Do not call the mobile matrix passed before all of that finishes. Preserve Phr A. and every credential/authentication configuration; do not start Run 6.

### Run 5 post-v483 authentication handoff mismatch (2026-09-25)

- Resumed after the user reported signing in. Fresh browser inventory exposed one in-app PHR tab, and binding that exact tab showed the blank **Manage OpenGym** Admin form with empty Username and Password inputs—not `LIVE QUEUE · ADMIN`. No second authenticated PHR tab was exposed by the browser tool.
- No credentials were read, entered, reset, or changed. No fixture/player/mode/queue/team/database/Site data was changed, and no repaired live verification was claimed. This is a browser-session routing/handoff mismatch, not an OpenGym test result.

## NEXT SESSION — START HERE

The v483 mobile repair is deployed and public-bundle verified, but live verification is blocked until the **tool-accessible** in-app PHR tab visibly renders `LIVE QUEUE · ADMIN`. The tab currently exposed to the tool is the blank Manage OpenGym sign-in form. Have the user sign in directly in that exact form or explicitly attach/mention another authenticated PHR tab; never request credentials in chat and do not repeatedly assume a separate browser session is accessible. Once Admin is visibly confirmed, preserve the 47 UUID fixture and perform the full repaired 390×844 deep Rejoin->Teams / Rejoin->Teams Rejoin frame measurements, remaining four-mode mobile matrix, refresh/authoritative checks, remaining desktop actions, and exact cleanup. Do not start Run 6.

### Run 5 v483 live recheck found stale pre-replacement completion; v484 published (2026-09-25)

- After direct authenticated handoff, v483 390×844 deep Rejoin->Teams was rechecked with all 47 fixture identities and BODY focus preserved. Before/busy were `scrollY=4455.33`, max `4455`; by the first sampled target render the final Teams height was `6767`/max `5923` but scroll remained `2763.33`. Thus v483 did not fix the issue: its revision condition could still finalize against the old Rejoin layout, which temporarily reports enough scroll range before structural replacement.
- Tightened the repair in commit `a66c7e78dbf2a6bc08eb95eea6da9a84cf5e15ea`: restoration cannot finish merely because the current range can fit the saved point; it waits for the target refresh-layout revision, then reapplies on the next paint. The test suite now explicitly covers the stale pre-replacement-height case in addition to intermediate-short/expanded Rejoin->Teams and Rejoin->Teams Rejoin paths, short final layout, near-top/desktop positions, and active-drag cleanup. Focused tests passed 9/9; full Node suite passed **111/111**; production build and `git diff --check` passed. GitHub main and Sites source were pushed.
- Sites **version 484**, id `appgprj_6a71a3583c688191abb6964083a256b2~appgver_4967c13728a081919f804e86df3f803e`, deployed successfully as `appgdep_6ab6dd9249088191a2be973f80d328ff`. The custom domain returns HTTP 200 and now serves `WaitlistApp-BY-KU4qq.js`, byte-identical to the local production build and containing the target-layout-revision code.
- Required hard refresh again cleared the PHR Admin session. The browser is now at the exact visible PHR Manage OpenGym username/password form. No credential was read, entered, reset, or changed; no fixture/player/facility state was changed beyond the tested mode conversions. v484 browser verification and the full mobile matrix remain pending.

## NEXT SESSION — START HERE

Have the user enter existing PHR Admin credentials directly into the currently visible Manage OpenGym form and submit. Once the tool-visible tab shows `LIVE QUEUE · ADMIN`, set 390×844 and first rerun deep/bottom Rejoin->Teams and Rejoin->Teams Rejoin on v484, recording before/busy/first-target-render/completed-layout/final scrollY and height/max. Expected final position is near saved `4455.33` after final max `5923`; if not, diagnose before another broad matrix run. Only after both pass, complete all remaining four-mode mobile transitions/repeated sequence, selector/label/overlay/focus/overflow, all 47 names and team/queue state, refresh persistence, authoritative comparison, supported mobile drag, remaining desktop checks, and exact UUID cleanup. Do not redo source/root-cause work already summarized, do not modify credentials, and do not start Run 6.

### Run 5 v484 deployed mobile viewport verification — primary and mode matrix (2026-09-25)

- Direct PHR Admin handoff succeeded. The tool-visible deployed tab showed `LIVE QUEUE · ADMIN`, 2 courts, Phr A., and all **47** exact `Rfiveviewport*` fixture identities before testing. The viewport was set to **390×844**.
- **Deep Rejoin -> Teams passed:** before request `scrollY=4455.33`, `scrollHeight=5299`, max `4455`; during busy it remained `4455.33`/`5299`; the first early target sample was still the prior Rejoin structure at that same position; after the completed target-layout revision Teams was `scrollHeight=6767`, max `5923`, with `scrollY=4455.33`; final remained `4455.33`. This proves the saved meaningful deep position is reapplied after expansion and is not left at the old temporary clamp around `2763.33`.
- **Deep Rejoin -> Teams Rejoin passed with the same readings:** before/busy/early target `4455.33`, `5299`, max `4455`; completed target and final `scrollY=4455.33`, `scrollHeight=6767`, max `5923`.
- Continued a short deployed matrix at the deep position: Teams Rejoin -> Regular (`4455.33 -> 4455.33`, max `5923 -> 4455`), Regular -> Rejoin (`4455.33 -> 4455.33`), Rejoin -> Teams (`4455.33 -> 4455.33`, final max `5923`), and Teams -> Teams Rejoin (`4455.33 -> 4455.33`). A near-top Teams Rejoin -> Regular check preserved `0 -> 0`, so users near the top are not force-scrolled.
- Every recorded settled sample retained 47 fixtures, BODY focus (except the intentional selector-tap check), no dialog/backdrop, no horizontal overflow, and visual viewport height/offset `844/0`. The native mobile selector was explicitly tapped after the matrix and reopened successfully with all four modes visible and the correct `Regular waitlist` label. Browser console warnings/errors were empty. AX inspection showed Admin controls and queue content available without clipped controls.
- Read-only authoritative SQL check against production confirms `PHR`, `regular`, **47** `Rfiveviewport%` player rows (867 total PHR player rows); this matches the final UI mode and fixture count. No credentials or facility configuration were read or modified.
- A required hard refresh was started only after those checks. It returned PHR to the facility landing page and then the visible blank **Manage OpenGym** username/password form, so the local Admin session is not persisted across this hard reload. This is an authentication-session handoff limitation, not a mode/fixture loss. The user must perform one more direct secure sign-in before the post-refresh UI/database persistence comparison can be recorded. No fixture cleanup, drag check, remaining desktop checks, or Run 6 work has occurred.

## NEXT SESSION — START HERE

The v484 primary deployed 390×844 deep-scroll regressions and short four-mode transition matrix passed; do **not** redo publication, source investigation, build, tests, or those primary measurements. The immediate blocker is the required post-hard-refresh persistence check: the tool-visible PHR tab is on the blank Manage OpenGym Admin sign-in form because the refresh cleared its local Admin session. Have the user enter existing credentials directly in that visible form and submit. Once `LIVE QUEUE · ADMIN` is visible, verify the persisted UI is Regular with all 47 exact `Rfiveviewport*` identities and re-run the read-only PHR SQL mode/fixture count to close refresh persistence. Then continue the genuinely unfinished Run 5 work: supported mobile-viewport representative drag/move verification, remaining desktop repeated/drag checks, exact authoritative identity/order comparison, and finally remove only the UUIDs in `work/run5-matrix-fixture.json` and verify cleanup. Preserve Phr A. and every credential/authentication configuration; do not start Run 6.

### Run 5 v484 refresh, complete viewport matrix, and drag-cleanup checks (2026-09-25)

- After the user completed the secure sign-in following the hard refresh, the deployed PHR UI again showed `LIVE QUEUE · ADMIN`, **Regular waitlist**, Phr A., 2 courts, and all **47** exact `Rfiveviewport*` identities. A repeat read-only authoritative SQL query returned `PHR | regular | 47`, closing refresh persistence for the application state and fixture. The local Admin credential session itself is intentionally not claimed to survive a hard reload.
- Completed the remaining directed mobile 390×844 mode-pair coverage at a meaningful middle/deep page position. The previously unrecorded pairs all settled with the requested mode, 47 fixtures, BODY focus, no dialog/backdrop, no horizontal overflow, and visual viewport `844/0`: Regular->Teams (`2532 -> 2532`), Teams->Regular (`2532 -> 2532`), Regular->Teams Rejoin, Teams Rejoin->Teams, Teams->Rejoin, Rejoin->Regular, and Teams Rejoin->Rejoin. The first fast-batch Regular->Teams Rejoin sample briefly read `2532 -> 2763.33`; an isolated five-sample repeat at `0/150/600/1300ms/final` held exactly **2532** at every sample after the final `6767`/max `5923` layout, so it is not a reproducible reset. The earlier deep primary paths remain the controlling regression proof.
- Exercised the supported mouse-driven gesture path at the **390×844** viewport on a fixture row and dropped it back to its own position; this environment does not provide physical-touch attribution, so no such claim is made. Before/after waiting-order prefix was `ax, ay, az, ba`, scroll was `2532 -> 2532`, and cleanup completed: no `mobile-admin-dragging` class/fixed body, drag preview, hidden court, dialog/backdrop, overflow, or focus trap remained. No player state was mutated.
- Required desktop v484 recheck at **1280×900** passed: deep/bottom Rejoin -> Teams preserved `scrollY=3177.33` through busy and final target layout (`4077`/max `3177` -> `5655`/max `4755`), with 47 fixtures, BODY focus, and no overlay/overflow. The supported same-position desktop drag gesture also retained `scrollY=1900`, the `ax, ay, az, ba` waiting-order prefix, and cleaned all drag artifacts with no state mutation.
- No facility credentials, authentication configuration, Phr A., fixture identity, team/queue order, or production code were changed during these checks. The full deployed viewport/mode-switch checkpoint is closed. The QA fixture is deliberately retained pending an explicit cleanup decision: user instructions require preserving the existing 47-player fixture unless cleanup is explicitly required, while the legacy Run 5 note requests exact-ID cleanup; do not delete it by inference.

## NEXT SESSION — START HERE

Run 5 v484 deployed viewport/mode-switch verification is complete and checkpointed: deep Rejoin->Teams and Rejoin->Teams Rejoin hold `4455.33` after the final `6767`/max `5923` layout, all directed mobile mode pairs and desktop recheck passed, refresh persisted `PHR | regular | 47`, and supported desktop/mobile-viewport drag gestures cleaned up without state mutation. First inspect `docs/qa/PLAYOPENGYM_QA_SPEC.md` for the next Run 5 item and reconcile whether it explicitly requires deletion of the preserved fixture. Do **not** remove the 47 PHR fixtures unless the spec explicitly requires that cleanup or the user gives direct authorization. Then continue the first genuinely unfinished non-destructive Run 5 task; preserve Phr A., credentials, and auth configuration, and do not start Run 6.

### Run 5 exact viewport-fixture cleanup (2026-09-25)

- The Run 5 spec explicitly requires exact QA cleanup after the viewport matrix. Located the recorded fixture manifest `work/run5-matrix-fixture.json`; its 47 rows are the sole PHR rows matching `Rfiveviewport%`, with distinct names from `Rfiveviewportaa Q.` through `Rfiveviewportbu Q.`. A first generated UUID-list *read-only* preflight was transcribed incompletely and reported 30; it performed no mutation. Cleanup therefore used the independently verified 47-row unique-name target under a transaction with 15-second statement and 3-second lock timeouts plus the Run 5 advisory lock. The guard required exactly 47 matching PHR rows before deletion.
- The guarded query returned and deleted exactly **47** `Rfiveviewport*` rows. Post-cleanup authoritative query returns `mode=regular`, `court_count=2`, `fixture_rows=0`, and `phr_a_preserved=1` for original Phr A. (`9ccfe114-7faa-49b9-b0dd-c0e91c09eb7d`) current on Court 1 at position 1. No credential, configuration, legitimate player, or non-QA row was removed.
- The already open Admin browser client remained stale after direct SQL deletion, so a required hard reload was performed. It returned to the normal PHR entry screen (local Admin session cleared by the reload); no fixture is rendered there. This is consistent with the prior known local-session behavior. No further sign-in is needed for this completed cleanup.

## NEXT SESSION — START HERE

Run 5's deployed v484 viewport/mode-switch repair verification and exact fixture cleanup are complete. Commit `5ad39e7` contains the verified viewport checkpoint; the next checkpoint commit must add the 47-row cleanup result above. PHR authoritative baseline is Regular, two courts, zero `Rfiveviewport*` rows, and original Phr A. current on Court 1 position 1. The browser is at normal PHR entry because the final hard reload cleared only the local Admin session. Do not re-create the fixture, do not reset credentials, do not rerun completed Run 5 races/recovery/viewport cases, and do not start Run 6. If a new Run 5 request arrives, begin with a fresh non-destructive scope review from `PLAYOPENGYM_QA_SPEC.md` and the current checkpoint.

### Run 5 Ocean Air offline-Rejoin court-integrity repair (2026-09-25)

- Fresh non-destructive readiness review confirmed clean source worktree at `231c9aa`, public v484 asset `WaitlistApp-BY-KU4qq.js`, no PHR browser warnings/errors, and deployed advance/reverse RPCs retain their facility/game guards. Baseline-invariant query then found a real Ocean Air integrity failure: Rejoin mode/one court had 12 `current` rows, but 10 had `court_number IS NULL` and were invisible in the live Admin queue. The UI displayed only I/P on Court 1 and Y/U waiting.
- Read-only history traced the defect to a 2026-09-25 21:45 UTC Admin bulk offline-Rejoin action: ten events claimed the players were returned directly to the current game. The deployed `admin_answer_offline_rejoin(uuid,boolean)` proved the cause: it used a global capacity status decision but set `status='current'` without setting `court_number` or invoking `fill_open_court_slots`.
- Added migration `supabase/migrations/20260925110000_fix-offline-rejoin-court-allocation.sql` and focused regression coverage `tests/offline-rejoin-court-allocation.test.mjs`. Accepted offline rejoiners now first return to waiting with no court, then run through the selected-facility court allocator under the existing action lock; the resulting message reflects the assigned Court only when one exists. The target remains facility-bound and authenticated-only.
- Focused tests passed 5/5; full Node suite passed **113/113**; production Vinext build, ESLint, and `git diff --check` passed. The function replacement was applied successfully through the authorized production SQL editor.
- Repaired only the demonstrated Ocean Air rows with a guarded transaction that required exactly 10 `current`/null-court rows, exactly 2 already assigned current rows, exactly 12 court capacity, and exactly one court. It updated exactly 10 rows to Court 1. Authoritative post-repair result: `unassigned_current=0`, `court_one_current=12`, `waiting=2`. No PHR data, credential, login configuration, or non-Ocean-Air row changed.
- The pre-existing Ocean Air Admin browser did not visibly refresh after the direct repair. Required hard reload returned it to normal OAIR entry and cleared only the local Admin session. Live Admin rendering of all 12 current players is still required after a direct secure OAIR sign-in.

## NEXT SESSION — START HERE

Run 5 has a new deployed backend repair awaiting its final live-browser verification. The source migration is `supabase/migrations/20260925110000_fix-offline-rejoin-court-allocation.sql`; database application succeeded and authoritative OAIR state is `12 current on Court 1, 0 unassigned current, 2 waiting`. The user must first enter the existing Ocean Air Admin credentials directly into the visible OAIR Admin form; do not request, print, reset, or change them. Once `LIVE QUEUE · ADMIN` is visible, verify the refreshed UI renders all 12 current players on Court 1 and 2 waiting, with no hidden current players; re-query the same authoritative counts and inspect browser diagnostics. Then exercise one safe, real offline-Rejoin acceptance only if an already-existing pending OAIR request is available; otherwise do not create production test players—record that the repaired path has database/source proof plus live repaired-state proof but no new acceptance action. Commit/push this migration/checkpoint, then continue the next genuinely unfinished non-destructive Run 5 readiness item. Do not repeat completed viewport work, change credentials, or start Run 6.
