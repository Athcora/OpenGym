# Production migrations

This directory is intentionally empty until the remote Supabase migration ledger is reconciled. Follow `docs/qa/SUPABASE_CLI_MIGRATIONS.md` before adding or pushing migrations.
